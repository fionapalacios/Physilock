package com.example.physi_lock.ui.theme

import androidx.compose.ui.graphics.Color

// Prototype palette (Physi-Lock design tokens)
val DeepOlive = Color(0xFF3D4928)       // Primary brand color
val SageAccent = Color(0xFF9CB06D)     // Accent / success
val Orchid = Color(0xFFC4A8D0)         // Accent / highlights
val MutedText = Color(0xFF536136)      // Muted text color
val OverlayBackdrop = Color(0x593D4928) // rgba(61,73,40,0.35) approx

// Semantic colors
val Success = SageAccent

// System surfaces
val OnSurfaceLight = Color(0xFF1C1B1F)

// Additional tokens from the teammate's Figma-derived design (sprint-2-ui-navigation)
val BackgroundLight = Color(0xFFFEFEFE)
val SecondarySage = Color(0xFFBAC892)
val TertiaryTan = Color(0xFFDAD5BB)
val ErrorRed = Color(0xFFC0392B)

// Admin Console mockup (teammate's Figma, sent 2026-08-28)
val CardCream = Color(0xFFF5F3EB)
val SignOutBackground = Color(0xFFFEF2F2)
val PillTrackBackground = Color(0xFFEEEADE)

// Design-system palette export (real Figma color dictionary, pasted 2026-09-10)
val DeepForest = Color(0xFF1F2A14)     // Focus / Deep Work full-screen overlay bg
val MidnightForest = Color(0xFF0F1A09) // Bedtime full-screen overlay bg
val Olive = Color(0xFF6B7A4E)          // Gradient stop (Mindful Morning, progress-bar fill)
val Violet = Color(0xFF9B6DBF)         // Gradient stop (Eyes Need Rest)
val Fog = Color(0xFFDEDAD0)
val Clover = Color(0xFF27AE60)
val Ember = Color(0xFFE8854A)          // Streak flame fill/stroke
