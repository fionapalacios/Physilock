# Physi-Lock Module Progress Checker

Tracks progress against the module-based development lifecycle in `PROJECT_DOCUMENTATION.md` (see its "Development Lifecycle" table for the canonical module list). Check items off as they're built; update the module-level status line whenever a module's checklist changes meaningfully. Last synced: 2026-08-25.

**Note on scope:** the per-module checklists below map the manuscript's 21 User + 6 Admin functions (see FDD memory) onto modules by function name/semantics. That mapping is my best-effort inference, not a confirmed 1:1 copy of the manuscript's 13-submodule breakdown — cross-check against the manuscript FDD if exact submodule wording matters for grading/documentation.

---

## Summary

| Status | Modules |
|--------|---------|
| ✅ **Done** | Admin Governance Layer, 3. Motion-Responsive Locking, 4. Smart Intervention System, 5. Mental Health & Awareness, 6. Personalization & User Control, 7. Context-Aware AI |
| 🟢 **Most Advanced** (built, may need polish) | 0. Foundation, 1. Core Monitoring & Usage Awareness, 2. AI-Based Behavior Analysis |
| 🟡 **Partial / In Progress** | 8. Integration & QA (ongoing by nature) |
| 🔴 **Not Started** | — |

---

## UI Status (screen-by-screen cut)

Same underlying progress as the module checklists below, viewed by screen instead of by feature module — useful for "what does the app actually look like right now" questions. Last checked: 2026-08-03.

**Built**
- [x] `LandingScreen` — redesigned 2026-08-24 with the teammate's swipeable module carousel (was: text-based hero + static module list). See "Teammate UI re-sync, 2026-08-24" below.
- [x] `LoginScreen`/`CreateAccountScreen` — split login/register screens (replaced the old combined `AuthScreen` 2026-08-23), all fields, error display, "Continue with Google" button, real Student/Work Usage Mode picker (2026-08-24), wired into `MainActivity`'s `AUTH_LOGIN`/`AUTH_REGISTER` stages
- [x] `OnboardingScreen` — redesigned 2026-08-24 with the teammate's card-list permission layout (was: YPT-style "Continue X/N" list), real permission checks/settings-intents preserved underneath. See "Teammate UI re-sync, 2026-08-24" below.
- [x] `HomeScreen` (`ui/home/HomeScreen.kt`) — today's screen time (with real configured daily limit + progress ring), real risk-level ring (Module 2, was a lock-sensitivity placeholder ring until 2026-08-23), locked-apps summary, Usage Goals/Daily Reflection `GoalsRow`. Redesigned 2026-08-11, nav-wired 2026-08-23, see below.
- [x] `ReportsScreen` (`ui/reports/ReportsScreen.kt`) — real weekly bar chart (dynamic scale, over-limit coloring), stat cards, top apps, rule-based insights, real AI usage predictions card (Module 2, added 2026-08-24). Redesigned 2026-08-11, see below.
- [x] `MoveScreen` — redesigned 2026-08-25 with the teammate's XP/streak card + challenge-card list visuals; real Activity Challenges (Module 4) underneath. New `ui/challenge/ChallengeActiveScreen.kt` for the in-progress state. See "Full teammate UI sync" below.
- [x] `SettingsScreen` — redesigned 2026-08-25, corrected same day: their `SettingsScreen.kt` was wrongly logged as a 0-byte stub (it's real, 545 lines — `StudentModeScreen.kt`/`WorkModeScreen.kt` are the ones that are actually empty), so the first pass was a from-scratch restyle instead of an actual port; replaced with the real port (ProfileCard, Work/Student usage-mode card, icon-row section-card layout, theme tokens remapped) rewired onto real data — same real functionality as before plus this repo's real toggles their design didn't have (Overuse Alert, Doomscrolling Detection). New `StudentModeScreen.kt`/`WorkModeScreen.kt` (built from scratch, genuinely empty on their side) reachable from the mode cards.
- [x] `AppLockRulesScreen` — pick installed apps to lock
- [x] `LockScreen` / `LockActivity` (`ui/lock/`) — real Activity Challenges progress overlay (Module 4)
- [x] `BottomNavBar` / `NavGraph` — bottom nav (restyled 2026-08-25, pill-highlighted selected icon) + routing
- [x] `AdminHomeScreen` + 4 tab sections (Accounts/Categories/Defaults/Analytics)
- [x] `ReflectionScreen` (`ui/reflection/`) — built from scratch 2026-08-25 (Module 5, no teammate UI existed to port); daily rotating prompt, mood check-in, real persisted answer history
- [x] `ContextAlertsScreen` (`ui/settings/`) — built from scratch 2026-08-25 (Module 7, no teammate UI existed to port); Wi-Fi-network-name-matched alerts, reached from Settings' "Location Context" row

**File structure & naming alignment (2026-08-11):** two things folded into one session — (1) this repo's own internal folder organization was inconsistent (`HomeScreen.kt`/`ReportsScreen.kt` sat in a leftover `ui/screens/` folder while their ViewModels already lived in `ui/home/`/`ui/reports/`; `LockActivity.kt`/`LockScreen.kt` sat loose directly in `ui/` with no feature folder) — fixed by moving both screens into their ViewModels' folders and creating `ui/lock/` for the lock files, updating all imports and `AndroidManifest.xml`. (2) A teammate's separate UI repo (`github.com/fionapalacios/Physilock`, real work is on branch `sprint-2-ui-navigation` — `main` is just an empty scaffold) was cloned and compared. Package name differs (`com.prototype.physi_lock` vs this repo's `com.example.physi_lock`) and a real token collision was found: both theme files define a color named `DeepOlive`, but with *different hex values* (`#3D4928` here vs `#536136` there, which is what this repo separately calls `MutedText`) — a genuine naming collision, not just a style difference.

Decision: for the three screens with a functional overlap, take the teammate's visual design but keep this repo's real data layer (Firebase/Room/`UsageStatsManager`) — teammate's versions are UI-only mockups with **no backend**, in two cases entirely hardcoded fake data (teammate's `DashboardScreen.kt` ships a fixed list of fabricated notifications and a fake "AI" predictive-overuse claim; `ReportsScreen.kt` is 100% invented stats, no ViewModel at all). Per-screen outcome:
- **Home** — redesigned using teammate's visual language (progress rings via a new `ui/components/CircularProgressRing.kt`, restyled cards) wired to real `HomeViewModel` data. Added a real `dailyLimitMinutes` StateFlow (was previously hardcoded "240m"). Dropped the fabricated break-reminder banner, "predictive overuse AI" banner, and static goals row — none were backed by real data on either side.
- **Reports** — redesigned the same way, wired to real `ReportsViewModel` data (added a real `dailyLimitMinutes` StateFlow there too, used for over-limit bar coloring). Kept the honest "Insights" label rather than teammate's "AI-POWERED" badge, since these are rule-based, not ML. Skipped teammate's category-breakdown card and week/month toggle — no real per-category usage aggregation exists yet to back it (candidate for later, once Module 2 or a category-aware usage query exists).
- **Settings** — teammate's `SettingsScreen.kt`/`StudentModeScreen.kt`/`WorkModeScreen.kt` turned out to be empty (0-byte) stub files, never actually written. Kept this repo's existing `SettingsScreen` as-is; revisit once the teammate's branch has real content.
- **Move** — kept this repo's existing version (user's explicit choice); teammate has a visually different `MoveScreen` not adopted. **Onboarding/permissions and Landing were later adopted on 2026-08-24** — see "Teammate UI re-sync, 2026-08-24" further down.

New shared theme tokens added to `Color.kt` to support the above and future ports from teammate's branch: `BackgroundLight`, `SecondarySage`, `TertiaryTan`, `ErrorRed`. (Teammate's `PrimaryDark`/`PrimaryGreen`/`AccentLavender` already had equivalents here — `DeepOlive`/`SageAccent`/`Orchid` — reused rather than duplicated.)

**Ported 2026-08-23** (re-cloned teammate's repo at `sprint-2-ui-navigation`, `gradlew compileDebugKotlin` passes): `LoginScreen`/`CreateAccountScreen`/`ForgotPasswordScreen`/`VerifyEmailScreen` → `ui/auth/`, `FocusModeScreen`/`EndFocusSessionSheet` → new `ui/focus/`, `UsageGoalsScreen`/`UsageGoalsViewModel` → new `ui/goals/`, shared components `AuthComponents`/`DashboardCard`/`NotificationsPanel` → `ui/components/` (joining the existing `CircularProgressRing.kt`). Package renamed to `com.example.physi_lock`, theme tokens remapped (teammate's `PrimaryDark`/`PrimaryGreen`/`AccentLavender`/`DeepOlive`/`NunitoFontFamily` → this repo's `DeepOlive`/`SageAccent`/`Orchid`/`MutedText`/`Nunito`); copied `logo.png`/`ic_google.xml` drawables the auth screens need (didn't exist here before). **Deliberately left unwired** — none of these are routed in `NavGraph.kt` yet, per plan; ask before wiring.
- Auth screens were rewired away from the teammate's non-existent `data.AuthRepository`/`AuthResult` mock to callback params matching this repo's existing `AuthScreen.kt`/`AuthFormState`/`AuthViewModel` conventions (`onLogin`, `onCreateAccount: (AuthFormState) -> Unit`, etc.), so wiring later is a matter of passing real `AuthViewModel` calls in, not further rewiring. `ForgotPasswordScreen`/`VerifyEmailScreen` still have no real backend (no reset-code service, no email-verification service exists) — kept as UI-only with client-side format checks and outbound hooks (`onSendCode`, `onVerified`) for whichever service gets built later.
- `UsageGoalsViewModel` ported near-verbatim — its `UserConfiguration`/`UserConfigurationDao` field names (`dailyScreenTimeThresholdMs`, `getActiveConfiguration()`/`getActiveConfigurationOnce()`) already matched this repo exactly, so only the total-daily-limit goal is real; per-category goals (Social Media/Entertainment/Gaming) and the weekly goal card stay UI-local demo values, same as the teammate's original — no category-aware usage schema exists yet.
- Dropped one thing from the port rather than carrying it over: `UsageGoalsScreen`'s "AI Recommendation" card was a hardcoded fake risk-score claim ("lower your addiction risk score from 62 to ~45") — the same anti-pattern already rejected when the Home/Reports redesign dropped the teammate's fabricated "predictive overuse AI" banner (see 2026-08-11 entry below). Also dropped `CreateAccountScreen`'s Usage Mode picker — cosmetic only in the teammate's original (the selection was never actually passed into registration), and a real mode picker is a separate not-yet-built screen (see `StudentModeScreen`/`WorkModeScreen` below).
- **New discovery, not yet ported**: the teammate's `ui/screens/settings/` now also has real (non-stub) `EditProfileScreen.kt`/`ProfileViewModel.kt` that didn't exist as of the 2026-08-11 check. `StudentModeScreen.kt`/`WorkModeScreen.kt` are still confirmed 0-byte stubs, unchanged.

**Nav-wiring (2026-08-23, same-day follow-up):** `gradlew compileDebugKotlin` passes.
- **Auth**: `AuthScreen.kt` deleted, replaced by `LoginScreen`/`CreateAccountScreen` (`MainActivity`'s `AppStage.AUTH` split into `AUTH_LOGIN`/`AUTH_REGISTER`, navigating to each other). `AuthFormState` moved from the deleted file into `AuthViewModel.kt` (still used by `HybridAccountRepository.register`). `ForgotPasswordScreen`/`VerifyEmailScreen` are still NOT wired — `onNavigateToForgotPassword` stays a no-op like the old `AuthScreen`'s did, since there's still no reset-code backend and no follow-up password-reset screen to land on after verification; wiring them now would just dead-end.
- **Focus Mode**: `HomeScreen`'s existing "Focus Mode" tile (was `onClick = {}`, "Coming soon") now navigates to a new `Screen.Focus` route. Added `FocusModeRoute` in `FocusModeScreen.kt` — a thin wrapper that ticks a local `elapsedSeconds` counter (`LaunchedEffect` + 1s `delay` loop) since no real session/timer backend exists yet (Module 6). Timer resets on re-entry; nothing is persisted.
- **Usage Goals**: user pointed out the teammate's `DashboardScreen.kt` already had a home for this — a `GoalsRow` of two side-by-side `GoalCard`s ("Usage Goals" + "Daily Reflection") at the bottom of the dashboard, dropped during the 2026-08-11 Home redesign as an unbacked "static goals row". Ported that `GoalsRow`/`GoalCard` pair into `HomeScreen.kt` instead of the standalone tile tried first. "Usage Goals" card now shows a real subtitle (`todayMinutes`/`dailyLimitMinutes`, reusing the existing `formatMinutes` helper — teammate's was a hardcoded "5h / 7h today") and navigates to `Screen.Goals` → `UsageGoalsScreen`. "Daily Reflection" card is a non-clickable "Coming soon" placeholder (Module 5 is still unbuilt) — dropped the teammate's "Not done yet 💭" subtitle since it implies real reflection tracking that doesn't exist.
- **EditProfileScreen**: ported from the teammate's repo (re-cloned again this session) but NOT as-is — the original `ProfileViewModel` read/wrote a local `UserConfiguration` row, which isn't this repo's account data source (`Account` via `FirebaseAccountRepository`/`HybridAccountRepository`); porting it unmodified would've silently broken profile edits (writes go to the wrong table, never sync). Rewired to take `currentAccount: Account?`/`onSave: (Account) -> Unit` instead, matching the pattern the old inline editor already used. Also dropped two things from the port: the "Change Password" fields (teammate's version never wired them to anything — no password-change backend exists, so keeping them would be dead UI that looks functional) and the avatar picker (no photo-storage backend). Added an Occupation field back in (the teammate's original didn't have one, but the old inline editor did — dropping it would've been a real regression, not a fake-feature cleanup). Replaces `SettingsScreen`'s old inline `OutlinedTextField`-based account editor Card with a full-screen `EditProfileScreen`, toggled the same way `AppLockRulesScreen` already was (a boolean + early `return`).

**Corrections (2026-08-10):** two "not built" items below were stale — verified against current code, both already exist:
- **Logout** — `SettingsScreen`'s `AccountSummaryCard` already had a Log Out button, wired `MainActivity` → `NavGraph` → `SettingsScreen`, since before this entry was last written. It just didn't actually call Firebase — it only reset local UI state (`currentAccount = null`, navigate to `AUTH`), leaving the Firebase Auth session alive underneath. Fixed today: added `logout()` to `AccountRepository`/`FirebaseAccountRepository` (`auth.signOut()`) and `HybridAccountRepository` (delegates — purely local, no offline handling needed), `AuthViewModel.logout(context)` calls it plus best-effort clears Credential Manager's cached Google account state, and `MainActivity`'s `onLogout` now calls it before resetting UI state.
- **Occupation field** — was present in `SettingsScreen`'s old inline account editor; now lives in `EditProfileScreen` (2026-08-23 nav-wiring, see above). Not a gap.

**Wired 2026-08-24** (`gradlew assembleDebug` passes):
- **Forgot password** — adapted from the ported custom 6-digit-code UI to Firebase Auth's native link-based reset (user's explicit call — no Cloud Functions/email-service infra exists to build a real code backend). `FirebaseAccountRepository.sendPasswordResetEmail()` wraps `auth.sendPasswordResetEmail()`; `ForgotPasswordScreen` now has a `sent` confirmation state ("check your email") instead of navigating to a code-entry step — the reset itself happens on Firebase's own hosted page after the user taps the emailed link, nothing to do in-app after that. New `AppStage.FORGOT_PASSWORD` in `MainActivity`; `LoginScreen`'s "Forgot password?" link now wired (was a no-op). **Runtime-verified 2026-08-24** on the vivo V2109: real email sent (confirmed even a nonexistent address gets the same "sent" response — Firebase's email-enumeration protection, not a bug), confirmation screen renders with the entered address, "Back to Login" returns cleanly.
- **Email verification** — same link-based adaptation. `FirebaseAccountRepository.register()` now calls `sendEmailVerification()` (best-effort, after the username-uniqueness check passes) right after creating the Firebase Auth user. `VerifyEmailScreen` rewritten from a 6-digit code grid to a "we sent a link, tap it, then tap Continue" flow — `onCheckVerified` reloads the Firebase user and re-checks `isEmailVerified` (a link click doesn't push a signal into the app, so this is a manual recheck, not push-driven). New `AppStage.VERIFY_EMAIL`, inserted only on the email/password registration path (`AUTH_REGISTER` → `VERIFY_EMAIL` → `ONBOARDING`); **deliberately scoped out**: login and Google sign-in (Google accounts are pre-verified by Firebase) skip straight to Onboarding as before — existing accounts aren't retroactively gated on verification, only fresh signups. **Not yet runtime-verified** — needs an actual registration to exercise (user asked not to create a throwaway test account this session).
- **Notifications UI** — `NotificationsOverlay`/`NotificationEntry` (ported 2026-08-23) now has a real backing data source: new `NotificationLog` Room entity/DAO (`notification_logs` table, DB version 11→12), inserted from all 4 existing `AppMonitorService` alert paths (Break Reminder, Overuse Alert, Doomscroll Alert, Excessive Usage Prediction) right after each real system notification is actually posted — so the in-app history only ever reflects notifications that really fired, not a parallel/independent log. New `NotificationsViewModel` (`ui/components/`) exposes the log as a `Flow`; `HomeScreen`'s existing (previously decorative, no `onClick`) bell icon now opens the overlay and shows an unread-count dot, wired via `NotificationLog.toEntry()` mapping type→icon/color. **Runtime-verified 2026-08-24** on the vivo V2109: a real Overuse Alert appeared in the overlay with correct icon/title/description/timestamp, unread badge counted correctly, "Mark all read" cleared it, dismiss-on-backdrop-tap worked.
- [x] Focus Mode screens (Module 6) — `FocusModeScreen`/`EndFocusSessionSheet` ported 2026-08-23 to `ui/focus/`; real session/timer/app-blocking backend added 2026-08-25 (see Module 6 below).
- [x] Reflection Prompts screen (Module 5) — built from scratch 2026-08-25, no teammate UI to port (see Module 5 below).
- [x] Context Alerts screen (Module 7) — built from scratch 2026-08-25, no teammate UI to port (see Module 7 below).

**Teammate UI re-sync, 2026-08-24** (re-cloned `sprint-2-ui-navigation`, now 4 commits ahead of the 2026-08-23 port — all dated 2026-08-22, same day as the last sync but not yet pulled in). `gradlew assembleDebug` passes.
- **Registration Usage Mode picker** — the 2026-08-11 port had deliberately dropped the teammate's Student/Work picker from `CreateAccountScreen` as non-functional dead UI (their `usageMode` state was never passed into registration — still true in the teammate's latest). Re-added the same picker (emoji pill buttons), but real this time: `AuthFormState` gained a `usageMode` field, and `MainActivity` calls `settingsViewModel.setUserMode(form.usageMode)` right after a successful registration. This **superseded and replaced** the standalone post-onboarding `ModePickerScreen` built earlier the same day (2026-08-24) — asking for the mode twice in one signup flow (once in the form, once again right after onboarding) would've been redundant, so `ModePickerScreen.kt` and the `AppStage.MODE_PICKER` stage were deleted rather than kept as dead code. **Runtime-verified**: picker renders on the real Register tab, Student/Work selection toggles correctly.
- **Branding/icon refresh** — replaced the default Android-Studio-template webp launcher icons and generic green grid background with the teammate's layered PNG adaptive icon (foreground/background per density, `mipmap-anydpi-v26`) and Play Store icon; `res/drawable/logo.png` (the auth-header logo) was already byte-identical to the teammate's refreshed version from an earlier port, so no change needed there.
- **Landing screen rework** — `LandingScreen.kt` replaced with the teammate's swipeable `HorizontalPager` module carousel (logo, "Physi-Lock" header, 7-slide swipeable card with page-dot indicator, "Get Started"/"I already have an account" buttons) — purely cosmetic port, no real logic on this screen either way. **Fixed same day**: both buttons originally routed to the same place (Login); split so "Get Started" → `AUTH_REGISTER` and "I already have an account" → `AUTH_LOGIN` (`LandingScreen` gained a second `onSignIn` callback), matching the copy's actual intent. `CreateAccountScreen`'s Back button now returns to `AppStage.LANDING` (was `AUTH_LOGIN`) for the same reason. **Runtime-verified**: carousel swipes correctly between slides; button routing verified after the fix.
- **Onboarding/permissions rework** — `OnboardingScreen.kt` rebuilt with the teammate's visual layout (logo header, card-list permission rows, privacy note, single progress-counted Continue button) but keeping this repo's **real** permission logic — the teammate's version fakes all 5 permissions as local in-memory toggles with no actual settings-intent launches; this repo's real `isGranted(context)` checks and `context.startActivity(settingsIntent)` calls were preserved underneath the new visuals, same "their design, our data layer" pattern used for the Home/Reports port.
  - **Notifications now requests via Android's native runtime-permission dialog** (`ActivityResultContracts.RequestPermission()`, real Allow/Don't allow) instead of opening Settings — this is the one row where Android actually allows an app to show that dialog. Falls back to Settings only once the OS reports the permission permanently denied.
  - **Explained, not fixed (can't be — OS restriction)**: Usage Access, Display over other apps, and Accessibility Service are Android "special app access" grants with no request-dialog API for any app — every screen-time app (YPT, Digital Wellbeing, Forest included) has to send the user to Settings for these three specifically, by Google's design.
  - **Two real bugs found via on-device testing 2026-08-25 and fixed same day**: (1) Battery Optimization's row silently did nothing — `AndroidManifest.xml` never declared `android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`, so `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` had nothing to show; added the manifest permission. (2) The Continue button kept re-opening Accessibility Settings even after the user had already granted it — the cached `grantedStates` (only refreshed on `ON_RESUME`) could race with the OS finishing the accessibility service bind; Continue's click handler now re-checks `isGranted(context)` fresh at click time instead of trusting the cache. **Runtime-verified 2026-08-25** on the vivo V2109 (user drove the device directly after adb touch injection became unusable mid-session — see [[feedback_vivo_device_adb_latency]]): live permission state read correctly (Usage Access/Overlay/Notifications showed granted, Accessibility/Battery didn't, matching real device state); Accessibility Settings flow confirmed working end-to-end (Settings → Downloaded apps → Physi-Lock → toggle on → system "Allow" confirmation → back). Both fixes confirmed after rebuild: Battery Optimization now shows the real system Allow/Deny dialog, and Continue now completes onboarding (goes to Dashboard) immediately after granting Accessibility instead of re-opening Settings.
- **Typography scale**: pulled in this same day, see "Full teammate UI sync" below. Dark mode/dynamic-color from their `Theme.kt` stays not-pulled — every screen in this repo references static theme tokens (`DeepOlive`, `BackgroundLight`, etc.) directly rather than `MaterialTheme.colorScheme`, so adopting it would take an app-wide refactor, not a file port; flagged as a known gap.

**Full teammate UI sync completed, 2026-08-25** (user asked to fetch everything remaining). Final inventory pass against the teammate's full `ui/` tree:
- **Move hub (`MoveScreen.kt`) redesigned** — teammate's XP/streak card + challenge-card list visuals, wired to real `MoveViewModel` data throughout (`totalXp`, `streakDays`, `lockedApps`) plus one new real data source: `completedToday` (new `MotionInterventionLogDao.getCompletedChallengeTypesAfter` query + `MoveViewModel` StateFlow), so the "Completed" badge reflects challenges actually finished today instead of nothing. The teammate's hardcoded weekly streak-calendar (fixed "3-day streak", first 3 of 7 dots always lit) had no real per-day data behind it on either side; rather than fabricate specific days, it now lights the last `min(streakDays, 7)` slots off the real rolling count — honest simplification, not invented data. Challenge target labels (e.g. "100 steps", "3m") are now computed live from `ChallengeSensitivity` instead of the teammate's fixed strings, since this repo's difficulty is adjustable (Settings, or adaptive off the Module 2 risk score) and theirs wasn't. Real ACTIVITY_RECOGNITION permission request + locked-app picker dialog logic (already in this repo, absent from theirs) preserved underneath.
- **New `ChallengeActiveScreen.kt`** (`ui/challenge/`) — teammate's in-challenge visuals (icon badge, progress ring, italic quote card, reward chip, "Claim +XP" button), but real detection underneath: their version fakes all progress (time-based challenges just run a 1-second `delay` loop; the one rep-based challenge is a manual "Tap per Rep" button — no sensor read at all), while this port keeps `ChallengeProgressContent` — the same real accelerometer/gyroscope/step-sensor detection already runtime-verified on-device for Module 4. `onClaim` only fires once the real detector reports completion, not on a timer.
- **`BottomNavBar` restyled** — teammate's pill-highlighted selected-icon look ported in place of the stock Material3 `NavigationBar`; kept this repo's `NavController`-driven routing signature (theirs takes a plain enum + callback with no controller) so `NavGraph.kt`'s call site didn't need to change.
- **`MotionChallengeScreen.kt`/`ChallengeCompleteScreen.kt`/`ShakeDetector.kt` ported after all** (2026-08-25 follow-up — user asked not to skip any UI, even superseded designs). These are the teammate's *older* generic shake-challenge design (real accelerometer detection, not fake — ported as-is to `sensor/ShakeDetector.kt` and `ui/challenge/MotionChallengeScreen.kt`). **Deliberately still not wired into the live app**: this repo's real LockActivity/LockScreen already cover the same feature with 3 distinct, better-verified challenge types (Module 4) — routing a real lock trigger through this generic single-shake screen instead would be a regression, not an upgrade. Kept available in the codebase per the no-skip instruction, clearly kdoc'd as unwired-on-purpose rather than silently dead code.
- **Nothing else left to fetch from the teammate's repo**: `DashboardScreen.kt`/`DashboardViewModel.kt`/`ReportsScreen.kt` were already absorbed into this repo's Home/Reports redesign in an earlier session (mock ViewModel, not a literal-port candidate); `Theme.kt` dark mode flagged above as needing an app-wide refactor rather than a file port.
- `gradlew assembleDebug` passes; build installed on the vivo V2109 but not yet click-tested live (Move hub, a challenge run, and the new bottom nav bar styling all worth a pass next time on-device).

**Follow-up requests, 2026-08-25 (same day)** — `gradlew assembleDebug` passes:
- **Typography scale added** — full Material type scale (display/headline/title/body/label, all sizes) ported into `Type.kt` from the teammate's `Type.kt`, remapped to this repo's `Nunito` font token. Purely additive: the 5 styles this repo had already defined kept their exact existing values (so no already-visible text shifted), only the previously-missing slots were filled in. Default Material3 components this repo already uses elsewhere (`AlertDialog`, and the not-yet-restyled Admin sections/`AppLockRulesScreen`) pick the fuller scale up automatically via `MaterialTheme.typography`. Dark mode from the same file was explicitly excluded per the user's instruction.
- **Settings, Student Mode, and Work Mode screens built** — initial pass this session wrongly treated the teammate's `SettingsScreen.kt` as a 0-byte stub (stale/incorrect — see "corrected same day" note above, it's real, 545 lines) and did a from-scratch restyle instead of porting it; corrected later the same day into a real port of their design. `StudentModeScreen.kt`/`WorkModeScreen.kt` genuinely are 0-byte on their side, so those stay built-from-scratch — reachable by tapping either mode card in Settings, with a real "Switch to this mode" action and a real "your current settings" summary pulled from `UserConfiguration`. Per the no-skip-UI instruction, these aren't left as empty shells even though true per-mode-differentiated backend logic doesn't exist yet (`userMode` currently only labels which profile is active, all thresholds are shared/global) — each screen has an explicit, honestly-labeled "Planned for [Mode]" section listing what per-mode logic isn't built (e.g. class-schedule awareness, work-hours quiet mode) instead of pretending it already works.
- **Home's permission-status banner removed** — Onboarding already gates all 4 permissions before a user ever reaches Home, so the duplicate status card there was redundant. Removed `PermissionStatusBanner`/`PermissionRow` and the now-unused permission StateFlows from `HomeViewModel` (`hasUsageAccess`/`notificationsEnabled`/`hasOverlayPermission`/`hasAccessibilityAccess` and their settings-intent helpers) rather than leaving them as dead code; the on-resume refresh hook (`refreshPermissionState` → renamed `refreshOnResume`) still refreshes today's screen time and the Module 2 risk score, which shared that same hook.
- **Real app names instead of package names**: `AppLockRulesScreen` and `AdminCategoriesSection` were both showing a raw `app.packageName` (e.g. `com.google.android.youtube`) as a small subtitle line under the real app name — removed in both places. Audited the rest of the codebase for the same pattern (`grep` for `.packageName` reaching a `Text`) and found no other instances; every other app-name display already resolves through a real `PackageManager` label lookup (`UsageStatsRepository.getAppLabel`, `AppMonitorService.getAppName`, or `ResolveInfo.loadLabel` in `AdminViewModel`/`AppLockViewModel`'s installed-apps lists) rather than falling back to the package name.

**Second follow-up round, 2026-08-25 (same day)** — `gradlew assembleDebug` passes:
- **Persistent login** — closing/swiping the app away and reopening it always restarted at Landing, forcing a re-login every time, even though Firebase Auth already keeps its own session on disk. `stage` was `rememberSaveable` but that only survives configuration changes, not a real process kill from a swipe-away. Added `AccountRepository.currentUserId()`/`getCurrentAccount()` (implemented in `FirebaseAccountRepository`, with an offline fallback to the local `CachedAccountDao` cache in `HybridAccountRepository` — new `CachedAccountDao.findById` query — mirroring `login`'s existing offline-fallback shape). `MainActivity` now runs a one-time `LaunchedEffect` on cold start that resolves the current session and redirects straight to Home/AdminHome, showing a brief logo splash (`SessionCheckSplash`) instead of flashing Landing first. Only fires if nothing else has already navigated (`stage == LANDING`), so it can't hijack an in-progress flow after a config change.
- **"Medium" sensitivity renamed to "Moderate" (display only)** — the stored/compared value stays `"MEDIUM"` (`UserConfiguration.motionLockSensitivity`, `AdminDefaultsSection`'s defaults, etc. all still use it) since renaming the persisted string would be a data-format change for no real benefit; only the user-facing label changed. Added `ChallengeSensitivity.displayLabel()` and switched the 4 places that were title-casing the raw enum string (`SettingsScreen`, `AdminDefaultsSection`, `StudentModeScreen`, `WorkModeScreen`) to call it instead.
- **Landing carousel page-number removed** — dropped the "2/7"-style counter from each slide's header row in `LandingScreen.kt`; the dot indicator below the card already shows position, the number was redundant.
- **Logo update — resolved.** User placed a new file directly at `res/drawable/logo.svg`. It turned out to be a raster PNG (736×736px) wrapped in an SVG `<pattern>`/`<image>` (Figma-style export), not real vector art — and Android can't load a raw `.svg` as a drawable resource anyway (only its own VectorDrawable XML format). Extracted the embedded base64 PNG and saved it as `res/drawable/logo.png` (390KB, 736×736 — well above every prior asset size), deleted the `.svg` wrapper. Scope confirmed explicitly with the user: in-app header logo only (`MainActivity`, `LandingScreen`, `AuthComponents`, `OnboardingScreen` — all via `R.drawable.logo`); the launcher/Play Store icon (`mipmap-*/ic_launcher*`, the teammate's ported vector design) was explicitly left untouched.

**Bug-fix + teammate-UI-alignment round, 2026-08-25 (same day)** — `gradlew assembleDebug` passes, installed and runtime-verified on the vivo V2109 across 3 install cycles this round:
- **Root-caused and fixed: App Lock Rules was missing most installed apps (TikTok, Instagram, Duolingo, etc.), and Reports' "Top Apps This Week" was showing raw package names instead of app names.** Same root cause for both: `AndroidManifest.xml` had no `<queries>` element. This app targets SDK 36, so Android's package-visibility restrictions (API 30+) hide other installed apps from `PackageManager` by default — `AppLockViewModel`'s `queryIntentActivities(ACTION_MAIN/CATEGORY_LAUNCHER)` silently returned almost nothing, and `UsageStatsRepository.getAppLabel()`'s `getApplicationInfo()` call threw for invisible packages, falling back to the raw package name. Fixed with a `<queries>` block matching `ACTION_MAIN`/`CATEGORY_LAUNCHER` — one of Android's documented visibility exemptions, so it doesn't need the restricted `QUERY_ALL_PACKAGES` permission.
- **Home → "Lock Apps" now navigates directly to App Lock Rules.** It was routing to the Settings tab only (`onManageAppLock` → `Screen.Settings.route`), leaving the user to find App Lock Rules manually from there. Added a real `Screen.AppLockRules` nav route (`NavGraph.kt`) rendering `AppLockRulesScreen` directly; Home's "Lock Apps" tile and the app-usage card both point at it now.
- **Home: "Locked Apps Today" → "App Usage Today"**, matching the teammate's actual `AppUsageCard` design (their DashboardScreen, not previously ported). Was locked-apps-only with its own redundant "Manage Locks" button (redundant once "Lock Apps" navigates directly); now shows general top-5 app usage today from the same `UsageStatsManager` source Reports uses, no more locked-apps filter, no more separate manage button. `HomeViewModel.appUsageToday` replaces the old `lockedAppsToday` combine-flow.
- **Reports: added the "Daily"/"Weekly" period toggle** (renamed from the teammate's literal "This Week"/"This Month" labels, since that's what the toggle actually switches — a per-day chart vs. a not-yet-built per-week chart) — wasn't in this repo at all before. "Daily" stays fully real; "Weekly" shows an honest "Weekly view coming soon" placeholder rather than the teammate's own invented static numbers (consistent with this project's standing rejection of fabricated stats elsewhere).
- **Reports: Top Apps This Week now formats duration as "1h 30m"** instead of raw minutes (reused the screen's existing `formatMinutes` helper). Insights/Daily-Usage-chart day labels changed from a single letter to full `Mon`/`Tue`/`Wed`/... (`ReportsViewModel`'s `dayLabel` no longer truncates to 1 char).
- **Confirmed for the user: "Today's Usage Predictions" (Reports) is NOT ported UI** — checked the teammate's whole repo, no prediction/forecast feature exists anywhere in their Dashboard or Reports screens. It's this app's own Module 2 (Logistic Regression II) output, unrelated to the teammate-UI-fidelity work.
- **Settings: removed the "Usage & Motion" card entirely** — confirmed against the teammate's real `SettingsScreen.kt` that no such card, Daily Limit slider, Break Reminder interval slider, or Motion Lock Sensitivity picker exists in their design at all; user asked to strictly match their row-based list instead. Consequence flagged to the user: Daily Screen Time Limit is still separately editable via the Usage Goals screen (shares the same `UserConfiguration.dailyScreenTimeThresholdMs` field), but Break Reminder interval no longer has any editable UI anywhere (was slider-only, no equivalent row).
- **Settings/Admin: Motion Lock Sensitivity and Doomscrolling Detection reclassified as Admin-controlled, User-read-only** — per explicit instruction, users should view but never set their own sensitivity. Added a new `SettingsTrailing.Label` row type (plain status text, no Switch/chevron) and removed `SettingsViewModel.setDoomscrollingDetectionEnabled`/`setMotionLockSensitivity` (now unused — nothing calls them). `AdminDefaultsSection`'s existing Default Motion Lock Sensitivity picker and Default Doomscrolling Detection toggle are unchanged (Admin already owned these), just corrected its disclaimer text from "users can still override" to "users can view but not change." **Known gap, deliberately deferred to a future session per user request**: there's no real Doomscrolling *Sensitivity* scale anywhere (LOW/MEDIUM/HIGH) — only ever an on/off flag; the detector auto-derives its threshold from the Module 2 risk score. The read-only row currently just shows "On"/"Off". User plans to design the Admin UI for a real 3-level doomscroll sensitivity in a later session — **when that's built, use "Moderate" for the middle label, not "Medium"** (matches `ChallengeSensitivity.displayLabel()`'s existing MEDIUM→"Moderate" convention already used for Motion Lock).
- **Nav Bar icon/label size restored closer to pre-port sizing** — the teammate's ported pill-highlight bar had shrunk icons to 17dp (default/original was Material's standard 24dp via the old `NavigationBarItem`); bumped back to 24dp with a larger pill (56×40dp) and label text 12sp→13sp, keeping the pill-highlight visual style.

**Third follow-up round, 2026-08-25 (same day)** — `gradlew assembleDebug` passes, installed on the vivo V2109:
- **Home: "App Usage Today" duration now shows "1h 30m" format** instead of raw minutes (reused `AppUsageRow`'s existing `formatMinutes` helper).
- **Reports: added a real "Category Breakdown" card** — genuinely missing from the earlier "full teammate UI sync," found by re-diffing the teammate's `ReportsScreen.kt` more carefully. Real data, not their static mock: `ReportsViewModel.buildCategoryBreakdown()` buckets the same 7-day `packageTotals` map (already computed for `topApps`) by each app's Admin-curated category (`AppCategoryDao.getAll()`, joined by package name), falling back to "Other" for anything Admin hasn't categorized yet — honest, not fabricated. Empty state prompts to categorize apps in Admin.
- **Reports: "Today's Usage Predictions" renamed to "AI Insights" with the teammate's "AI-POWERED" badge treatment, and repositioned** to sit right after Category Breakdown — matching the teammate's actual layout order (chart → category → AI insights) rather than its previous ad-hoc spot after Top Apps. This badge is honestly earned here (unlike the teammate's original, which slapped "AI-POWERED" on entirely static mock data) — this genuinely is Module 2 Logistic Regression II output. The separate rule-based "Insights" card (no AI claim, by design) keeps its own honest, unbadged label and stays at the bottom, after Top Apps (which has no teammate equivalent).

**Fourth follow-up round, 2026-08-25 (same day)** — full audit of remaining teammate UI gaps (via a background research pass), then built the two highest-value ones. `gradlew assembleDebug` passes, installed on the vivo V2109.
- **Home: "App Usage Today" duration format fixed** — was still raw minutes despite Top Apps getting the same fix earlier; now reuses the existing `formatMinutes` helper.
- **Usage Goals: every "current usage" number is now real**, not the teammate's hardcoded mock (5.55h total, 3.23h Social Media, 1.87h Entertainment, 0.57h Gaming, 41.2h/week). New shared `categoryTotals()` helper (`data/CategoryUsageAggregator.kt`) factors out the package→category bucketing logic first built for Reports' Category Breakdown, now used by both `ReportsViewModel` and the new `UsageGoalsViewModel` real-data path. `UsageGoalsViewModel` computes today's total + today's per-category totals (for the daily-limit-comparable goal cards) and a separate real 7-day total (for the "Weekly Screen Time Goal" card, matching its own label). Goal *targets* (the slider values) still aren't persisted anywhere — no schema field exists for a per-category target — so those stay session-local UI state, same as before.
- **Home: two more teammate banners built with real data** — `PredictiveOveruseBanner` and `DoomscrollAlertBanner` (`HomeViewModel.refreshBanners()`), both previously dropped as fake (2026-08-11: "you'll hit your 7h limit by 8:22 PM", "23 min continuous session on TikTok" — neither backed by anything real). Deliberately reframed rather than faked verbatim: Predictive Overuse shows the next upcoming hour today Module 2's Logistic Regression II has actually flagged excessive (`ExcessiveUsagePredictionLog`), not an invented clock-time extrapolation the ML pipeline doesn't produce. Doomscroll alert shows the real app name + minutes-ago from the most recent `DOOMSCROLL_ALERT` row today (new `MotionInterventionLogDao.getMostRecentDoomscrollAlertAfter`), gated to the last 30 minutes so it doesn't sit stale all day. Both banners only render when a real signal exists — hidden otherwise, never an empty/fabricated state. Doomscroll banner is now tappable — new `onNavigateToMove` callback (`HomeScreen`/`NavGraph`) opens the Move hub as the "intervene" action, since no more specific per-alert intervention exists yet.
- **Break Reminder banner (teammate's "You've been online 47 min") and Edit Profile's "Change Password" fields were flagged in the same audit but deliberately not built this round** — first needs new live-session-duration plumbing in `AppMonitorService` (medium lift), the second needs a real Firebase reauth flow (real backend work, lower priority). Both still just correctly-dropped-as-fake, not yet upgraded to real.

---

## ✅ Admin Governance Layer (Done — 2026-08-02)

Not one of modules 0–8 (those map to User-facing feature groups); Admin is a separate cross-cutting governance layer per [[project_physi_lock_overview]].

- [x] Manage User Accounts — list/activate/deactivate/delete (`AdminAccountsSection`, `AdminViewModel`)
- [x] Curate App Categories — per-installed-app category master list (`AdminCategoriesSection`, `AppCategory`)
- [x] Configure Default Settings — global defaults for new accounts (`AdminDefaultsSection`, `DefaultSettings`)
- [x] View Usage Analytics — aggregate stat cards + top apps (`AdminAnalyticsSection`)
- [x] Export Research Data — plain-text share-sheet export
- [x] Authenticate Users — resolved as architectural framing only (Admin owns/governs the auth subsystem; no separate approval-gate code needed, see [[project_physi_lock_overview]])

**Known gap (2026-08-09), closed 2026-08-25: real error-state handling.** `AdminViewModel` previously swallowed failures silently — fixed without changing any of the safe-fallback *values* the UI already showed (0/emptyList), only adding a real signal that a failure happened:
- `accountsFlow()` no longer calls `close(error)` on a Firestore listener error (which killed the listener for good, silently going stale forever) — it now sets a new `accountsError` StateFlow and keeps listening; `AdminAccountsSection` shows a dismissible-by-retry banner over the last-known (possibly stale) account list instead of blanking the screen, and a new `retryAccounts()` (re-subscribes via a `flatMapLatest` retry trigger) backs a real "tap to retry" button.
- `loadAnalytics()`'s two already-existing per-query try/catches now also set a new `analyticsError` StateFlow (still falling back to the same `0`/`emptyList()` display values) — `AdminAnalyticsSection` shows the same banner style, wired to the existing "Refresh Analytics" button as its retry action.
- `setAccountActive`/`deleteAccount`/`setCategory`/`updateDefaults` (Admin's other 4 mutating actions) were fire-and-forget with no try/catch at all — each now catches failures into a shared `actionError` StateFlow, shown as a dismissible banner at the top of `AdminHomeScreen` (not a retry, since re-driving a specific past mutation would need re-capturing its original arguments; the user just retaps the action).
- New `AdminOfflineBanner`/real `isOnline` StateFlow (`NetworkConnectivityObserver.kt`, a live `ConnectivityManager.NetworkCallback`, new `ACCESS_NETWORK_STATE` manifest permission — normal, no runtime prompt) — shown at the top of `AdminHomeScreen` whenever the device has no internet-capable network, addressing "no offline fallback at all." This is an honest *degraded-mode signal*, not the User-side `HybridAccountRepository` pattern of a local cache Admin screens can still read from offline — Admin's accounts/categories/defaults/analytics remain 100% live-Firestore/Room-dependent when online; going offline now visibly says so instead of silently going stale with no indication.
New shared `AdminErrorBanner`/`AdminOfflineBanner` composables (`AdminBanners.kt`) keep all of the above visually consistent. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs an actual Firestore failure (e.g. airplane mode) to exercise the retry/offline paths on-device.

---

## 🟢 Module 0: Foundation

- [x] Database schema, DAOs, Room singleton
- [x] Permissions declared in manifest
- [x] AccessibilityService (`AppMonitorService`)
- [x] Navigation shell (`NavGraph`, `BottomNavBar`)
- [x] Register Account / Log In (`HybridAccountRepository` wrapping `FirebaseAccountRepository` — Firebase Auth + Firestore `users/{uid}` as primary, replacing the old local Room simulation)
- [x] Grant Permissions — YPT-style "Continue X/N" flow in `OnboardingScreen.kt`, wired into `MainActivity`'s navigation (2026-08-02): regular Users land there right after login/register, Admins skip straight to `ADMIN_HOME`
- [x] Manage Account Settings (username/fullName/occupation editable in `SettingsScreen`; email intentionally read-only — see caveat below)
- [x] Log Out — button already existed in `SettingsScreen`'s `AccountSummaryCard`; fixed 2026-08-10 to actually call `AccountRepository.logout()` (→ `auth.signOut()`) instead of only resetting local UI state. See runtime-verification entry below.
- [x] Firebase Auth migration (2026-08-07) — `Account`/`AccountDao` Room entity retired entirely; `FirebaseAccountRepository` (`data/auth/`) handles register/login/updateAccount against Firebase Auth + Firestore. **Correction (2026-08-09): a real Firebase project does exist** — `app/google-services.json` is present (project `physi-lock`, placed 2026-08-07) and processes cleanly (`default_web_client_id` resource confirmed generated in `app/build/generated/res/processDebugGoogleServices/`, including a web OAuth client for Google Sign-In). The earlier "no Firebase project exists yet" note was stale. **Still unverified: actual runtime behavior against that project.** Nobody has run the app against it yet, so unconfirmed: whether the Email/Password provider is enabled in the Firebase console, whether Firestore is created with usable security rules (default rules deny all reads/writes), and whether the demo Admin account was ever manually created (Auth user + Firestore doc with `role: ADMIN`) as this section previously instructed.
- [x] Google Sign-In (OAuth 2.0/OIDC) — Credential Manager + `GoogleIdTokenCredential` flow wired end-to-end in `AuthViewModel.signInWithGoogle`/`MainActivity` (2026-08-07). Same runtime-unverified status as above, plus one Google-Sign-In-specific risk: it needs the app's SHA-1 certificate fingerprint registered against the `physi-lock` Firebase project, or it fails at runtime with a DEVELOPER_ERROR even though the code/build config is otherwise correct — unconfirmed whether that's been done.
- [x] Offline login fallback (2026-08-09) — `HybridAccountRepository` (`data/auth/`) wraps `FirebaseAccountRepository`; every successful online login/register writes a salted-hash snapshot (`PasswordHasher`, PBKDF2WithHmacSHA256) to a new local Room cache (`CachedAccount`/`CachedAccountDao`, DB version 5→6). If a login call throws (Firebase/Firestore unreachable), it falls back to verifying against that cache. Scope is login-only — registration and profile updates still require Firebase; Google Sign-In is unaffected (still online-only). `AuthViewModel` now extends `AndroidViewModel` to reach the DB. **Verified:** `gradlew compileDebugKotlin` passes. **Not yet verified:** no runtime test of the actual offline path (e.g., killing network mid-session in an emulator).
- [x] **Runtime-verified 2026-08-09 (partial):** user ran the app and confirmed Google Sign-In works end-to-end — account visible in both Firebase Auth console and Firestore `users` collection. Email/password registration errored, root-caused and fixed same day: `FirebaseAccountRepository.register()` was querying Firestore (username uniqueness check) *before* `auth.createUserWithEmailAndPassword`, i.e. while still unauthenticated — fails under any Firestore rules that scope reads to signed-in users. Fixed by reordering: Auth now runs first, Firestore check second, with a rollback (`authResult.user?.delete()`) if the username turns out taken. Added `firestore.rules` at repo root (not auto-deployed — must be pasted into the Firebase Console manually, no CLI configured in this repo) with the corresponding access model: any signed-in user can read/query `users`, but can only write their own doc (Admin gets an update/delete bypass for account management). **Gap closed same day:** added a public `usernames/{username} → { email }` lookup collection (`firestore.rules`) so login-by-username keeps working under the auth-scoped rules. Deliberately minimal — the doc holds only `email`, and rules only permit exact-ID `get` (never `list`/enumeration), so it can't be used to browse all usernames, only resolve one you already know. `resolveEmail()` now does a `usernamesCollection.document(identifier).get()` (public, pre-auth-safe) instead of the old `whereEqualTo` query (which required auth and would've failed pre-login). Write-side: `register()`, `updateAccount()` (on username rename — deletes the old mapping, writes the new one), and `signInWithGoogleIdToken()`'s first-time provisioning all keep this collection in sync, best-effort (a failed lookup write doesn't fail the whole registration/update — it only degrades that account's future username-login, email login is unaffected). Rules scope create/update/delete to `request.auth.token.email` matching the mapping's own email, so you can't overwrite or delete someone else's username mapping. **Not yet runtime-verified** — same as the rest of this Firebase work, needs an actual login-by-username test once the updated rules are pasted into the console.
- [x] **Runtime-verified 2026-08-10:** user pasted the updated rules and confirmed login-by-username works. Also confirmed the offline-fallback design is behaving exactly as intended: registering a *new* account with no internet correctly fails (expected — registration has no offline path, needs Firebase to reserve the username/email); registering that same account with internet then worked; logging into that freshly-registered account with no internet then succeeded (the `HybridAccountRepository` cache written at registration time backing the offline login as designed). **Log Out fix same day** — see Foundation checklist above; not yet runtime-verified (need to confirm `auth.currentUser` is actually null after tapping it, e.g. via Logcat or by checking the Firebase Auth console's "last sign-in" doesn't show the app still active).

## 🟢 Module 1: Core Monitoring & Usage Awareness

- [x] View Usage Tracker (`HomeScreen`, `HomeViewModel`)
- [x] View Screen Time Breakdown (`ReportsScreen`, `ReportsViewModel`)
- [x] Foreground app tracking, usage aggregation, scroll-event field (counting logic itself deferred to Module 2)
- [ ] View Usage Patterns (`ReportsViewModel`'s "insights" are simple rule-based observations, explicitly not pattern-recognition)
- [x] Receive Overuse Alerts (2026-08-21) — shared deliverable with Module 4, see that section below for detail

**Bug found + fixed (2026-08-10): displayed screen time didn't match real device usage.** Root cause: `HomeViewModel`'s "today's screen time" and `ReportsViewModel`'s weekly chart/top-apps were entirely sourced from `AppUsageLog` — rows `AppMonitorService` (the AccessibilityService) only writes when it observes a foreground-app *switch* while actively running. That has no way to reflect usage from before the service started watching, so it structurally couldn't match the phone's real total; it'd read low or zero. The fix: `UsageStatsRepository` (already built, already gated by the "Usage Access" permission already requested in onboarding, but never actually called for this) wraps Android's real `UsageStatsManager` — the same source Digital Wellbeing/Settings' screen-time page reads. `HomeViewModel` now sums `getTodayUsage()` on every `ON_RESUME`. `ReportsViewModel` now queries one day at a time via the new `getUsageForRange(startMillis, endMillis)` (folds 7 single-day queries into the weekly chart + a client-side top-apps aggregate) rather than a single 7-day range query, since `UsageStatsManager`'s multi-day bucket-aggregation behavior isn't reliable enough to trust — see kdoc on that method. `AppUsageLog`/`AppMonitorService` are untouched and still needed for real-time, event-driven behavior (lock-triggering, per-session data, future scroll-event/doomscroll counting) — this fix only changes where the *displayed totals* come from. Also found and left alone: `UsageSync.kt`'s `syncTodayUsage()` was a half-built earlier attempt at this exact fix — dead code (never called), and even if wired up it writes to the legacy `UsageSession` table, not the one either ViewModel reads. Verified with `gradlew compileDebugKotlin`; not yet runtime-tested against real device usage.

## ✅ Module 3: Motion-Responsive Locking (Done — 2026-08-24)

- [x] Perform Move-to-Unlock (`MoveScreen`, `LockScreen` — via `RotationalArmDetector`/`StepChallengeDetector`, see Module 4's Activity Challenges entry; `ShakeDetector` was replaced, not extended)
- [x] Customize Lock Rules — per-app lock on/off (`AppLockRulesScreen`)
- [x] Motion Lock Sensitivity setting wired to the 3 challenge types (`SettingsScreen`, `ChallengeSensitivity`)
- [x] Trigger Adaptive Lock (2026-08-24) — `lockType` was always hardcoded to `"CUSTOM"` on write and never read anywhere (dead schema field, not actually a "not built" gap so much as a "never wired" one). Now real: `AppLockRulesScreen` gained a per-app "Adaptive difficulty" toggle (shown only when that app is locked), backed by `AppLockViewModel.setAdaptive`/`adaptivePackages`. `LockScreen.kt` checks the triggered app's `AppLockRule.lockType` — if `"ADAPTIVE"`, computes the challenge sensitivity from the real Module 2 risk score (`ChallengeSensitivity.fromRiskLevel`: LOW risk → LOW sensitivity, MODERATE → MEDIUM, HIGH → HIGH) instead of the static Motion Lock Sensitivity setting, falling back to that static setting if risk scoring throws for any reason. Shows "Difficulty adapted to your current risk level" on the lock screen when active. Deliberately scoped to the actual lock-trigger path only — `MoveViewModel`'s voluntary challenge hub still uses the static setting uniformly, since adaptive-per-specific-app doesn't map cleanly onto "pick which locked app to unlock voluntarily." `gradlew assembleDebug` verified. **Not yet runtime-verified.**

## ✅ Module 4: Smart Intervention System (Done — 2026-08-22)

- [x] Set Usage Limits — daily screen-time threshold slider (`SettingsScreen`, `AdminDefaultsSection`); not the goal-based/YPT-style limits from [[project_inspiration_apps]]
- [x] Complete Activity Challenges (2026-08-22) — replaced the old shake-to-unlock mechanic entirely with 3 real motion challenges: Rotational Arm Movements (+15 XP, new `RotationalArmDetector` using accelerometer+gyroscope fusion — first gyroscope use in this codebase), 5-Minute Walk (+20 XP), and Run/Jog (+40 XP, both via new `StepChallengeDetector` on `Sensor.TYPE_STEP_DETECTOR`, requires `ACTIVITY_RECOGNITION` permission requested lazily on first use). Available both at the actual lock-trigger moment (`LockActivity`/`LockScreen`, one random challenge per lock event) and in a voluntary hub (repurposed `MoveScreen`/`MoveViewModel` — previously an empty stub) gated to only show challenges when at least one app is currently locked, with a picker to choose which locked app to unlock. Built a genuine per-app timed unlock for the first time (`AppMonitorService.grantTemporaryUnlock`, in-memory packageName→expiry map) — the old mechanism was only ever an 8-second blanket re-lock-loop suppression, not a real "unlock for 20 min" reward. `MotionInterventionLog` (previously write-never dead code masking the streak display) now actually gets rows inserted from both completion paths, extended with `challengeType`/`xpEarned` columns; XP total is now real (`getTotalXp()` query). **Runtime-verified 2026-08-22** on the vivo V2109 device: Rotational Arm completed successfully via both the lock-trigger and voluntary-hub paths (confirmed in `motion_intervention_logs`), per-app unlock persistence confirmed (reopening the just-unlocked app doesn't re-trigger the lock screen), Run/Jog's live cadence tracking confirmed working against the device's real `STEP_DETECTOR` sensor (a UX gap — no visible feedback on cadence vs. target — was found and fixed mid-testing). **Not separately stress-tested**: Walk's full step-count completion, unlock expiry after the full 20-minute window, and the hub's empty-state gating with zero apps locked — all share code paths already proven correct via the Rotational Arm/Jog tests above, but weren't independently exercised end-to-end.
- [x] Receive Break Reminders (2026-08-21) — user-configurable continuous-foreground-usage timer (`UserConfiguration.breakReminderEnabled`/`breakReminderIntervalMs`, default on/30 min; toggle+slider in `SettingsScreen`), tracked in `AppMonitorService` (accumulates across accessibility events, resets on a >2 min idle gap — a proposed default, not manuscript-specified), posts a repeating Android notification via a new `break_reminder_channel` (`IMPORTANCE_HIGH`, heads-up banner). Deliberately independent of doomscroll pattern-detection (separate, still-unbuilt Module 2 classifier) — modeled on TikTok's own plain continuous-session break reminder instead. **Runtime-verified 2026-08-21** on a real device (vivo V2109, Android 13) with a temporary 1-min test interval — notification fired correctly with real (touch-driven) app switching, including the `IMPORTANCE_HIGH` heads-up banner (confirmed by manually bumping the already-created channel to "Urgent" in system Settings, since Android won't let an app change an existing channel's importance in code once created — a fresh install gets `IMPORTANCE_HIGH` automatically without this manual step). Two device-level gotchas found along the way, worth remembering for testing any other background-service feature on this same device: (1) no local emulator/AVD is configured on this machine — testing used the physical device over wireless ADB instead; (2) this vivo (Funtouch OS) build blocks `adb logcat` entirely, even for a manually shell-injected test log, so log-based debugging doesn't work here — dumpsys (`dumpsys notification`, `dumpsys accessibility`) is the only viable adb-based introspection; (3) vivo's per-app "Smart control" battery/autostart mode silently suppressed the background accessibility service's notifications even with the standard Android notification permission granted — had to be switched to explicit "No restrictions" in vivo's battery settings before anything fired. Also confirmed `adb shell am start`-simulated app switches do **not** reliably trigger the same accessibility events as real touch navigation on this device (the pre-existing App Lock feature also failed to trigger under simulated switches) — real on-device interaction is required to test any accessibility-event-driven feature here, adb alone isn't sufficient.
- [x] Receive Overuse Alerts (2026-08-21) — passive, at-most-once-per-day notification when today's real aggregate screen time (via `UsageStatsRepository`, same OS-backed source `HomeViewModel`/`ReportsViewModel` already use) crosses the existing `UserConfiguration.dailyScreenTimeThresholdMs` limit (`overuseAlertsEnabled` toggle in `SettingsScreen`, default on). Scoped to the aggregate limit only, not per-app (`AppLockRule.dailyLimitMs` stays unused dead schema — building real per-app limits would be a separate, bigger feature). Checked from `AppMonitorService` on app-switch events, throttled to at most once/60s (`UsageStatsManager` queries aren't free) with an in-memory once-per-day dedupe key. Separate notification channel (`overuse_alert_channel`, `IMPORTANCE_HIGH`) from Break Reminder's. **Runtime-verified 2026-08-21** on the same vivo V2109 device — notification fired correctly with real usage once the daily limit was temporarily lowered, confirmed no repeat on subsequent app switches the same day, heads-up banner confirmed working.

## 🟢 Module 2: AI-Based Behavior Analysis (All 3 models done 2026-08-23/24, UI consumers wired)

- [x] Random Forest risk scoring (2026-08-23) — real, trained, wired end-to-end. `ml/` holds the offline pipeline: `generate_dataset.py` (synthetic training data — no ground-truth risk labels exist anywhere, real or synthetic, so this bootstraps one via a weighted heuristic over the 6 manuscript features, tertile-balanced Low/Moderate/High labels), `train_risk_model.py` (`RandomForestClassifier`, 15 trees/max_depth 5 — see below), `README.md` (full pipeline explanation, why the training set is synthetic, why the transpiled output is Java not Kotlin, the model-size constraint, retraining steps). On the app side: `RiskModel.java` (m2cgen-transpiled, zero ML runtime dependency), `RiskScoringEngine.kt` (wraps it, returns a `RiskLevel` + continuous 0.0–1.0 score), `RiskFeatureExtractor.kt` (pulls today's real feature vector from Room). Wired into `HomeViewModel`/`HomeScreen` — replaced the `UserConfiguration.riskSensitivity` placeholder entirely (deleted from `UserConfiguration.kt`); the dashboard's risk card ("RISK LEVEL"/"Behavioral risk score", was mislabeled "LOCK SENSITIVITY"/"Motion challenge difficulty" — a leftover from when it literally displayed the motion-lock-sensitivity setting) now shows the real computed level and score, recomputed every Home resume. `gradlew assembleDebug` verified (not just `compileDebugKotlin` — needed to confirm the generated Java actually compiles/dexes, see below). **Not yet runtime-verified against real accumulated usage data** (needs a device with several real logged sessions/bypass attempts to see non-default output).
  - **Real feature plumbing added same day**, since 2 of the 6 features had no data source at all before this: **bypass attempts** — `LockActivity.onStop()` now logs a `MotionInterventionLog(userResponse = "DISMISSED")` row once per challenge shown-but-not-completed (covers back button, home button, recents swipe-away, screen-off — all route through `onStop()`); new `MotionInterventionLogDao.countBypassAttemptsAfter`/`countBypassAttemptsByPackageAfter` queries. **Scroll data** — `AppMonitorService.logScrollEvent` (previously an empty stub) now actually counts `TYPE_VIEW_SCROLLED` events per session and tracks the longest gap between them; `AppUsageLog` gained a `maxScrollGapMs` column (DB version 9→10, `fallbackToDestructiveMigration` — dev/test only, wipes local data on upgrade) alongside the already-existing-but-previously-always-0 `scrollEventCount`. New `AppUsageLogDao.getSessionCountByDate`/`getDurationByCategoryAndDate` queries support the launch-frequency and social-media-fraction features.
  - **Model-size constraint discovered and worked around**: m2cgen inlines an entire Random Forest as nested if/else in ONE Java method (no per-tree helper methods), and the JVM caps a single method at 64KB bytecode. A first attempt at 150 trees/depth 8 generated ~112,000 lines and would have failed to compile. Cut to 15 trees/max_depth 5 (~1,900 lines, compiles and dexes cleanly) — costs some accuracy (66.8% vs. an untested higher number on the bigger forest) but was judged an acceptable bootstrap-model tradeoff. Documented in `ml/README.md` for whoever retrains this.
- [x] Logistic Regression I — doomscroll detection (2026-08-23, same day as the Random Forest). Same synthetic-bootstrap approach: `generate_doomscroll_dataset.py`/`train_doomscroll_model.py` → `DoomscrollModel.java` (m2cgen). **Confirmed design respected**: the 3 raw inputs are scroll speed, pause pattern (`maxScrollGapMs`), and time of day — the Random Forest's risk level is explicitly NOT a 4th input; `DoomscrollDetector.kt` instead uses it to pick a detection threshold "recipe" (LOW risk → 0.75 threshold, MODERATE → 0.60, HIGH → 0.45), applied to the model's sigmoid-of-logit output. 80.5% test accuracy, 0.88 AUC-ROC. Wired into `AppMonitorService`: `checkDoomscrolling()` runs off the *live in-progress session's* in-memory scroll state (not a Room query — needs to catch doomscrolling while it's happening), throttled 15s between checks and a 10-min cooldown per alert; a new `doomscroll_alert_channel` notification fires and a `MotionInterventionLog(triggerType = "DOOMSCROLL_ALERT")` row gets logged on detection. Risk level is cached and refreshed every 5 minutes in a new background loop (`AppMonitorService.startRiskRefreshLoop`) — the first real implementation of the "every 5 minutes" cadence `PROJECT_DOCUMENTATION.md`'s pipeline diagram already described but nothing had actually built yet. **This closes yesterday's `doomscrollEpisodeCount` gap**: `RiskFeatureExtractor` now counts today's real `DOOMSCROLL_ALERT` rows instead of hardcoding 0 — all 6 Random Forest features are real as of today. `gradlew assembleDebug` verified. **Not yet runtime-verified.**
  - m2cgen's Java export differs by model type, worth knowing before retraining either: Random Forest → `double[]` of class probabilities; Logistic Regression → a single raw logit, sigmoid applied on the Kotlin side (`DoomscrollDetector.kt`). Discovered by inspecting the actual generated file rather than assuming.
  - **Known simplification**: `hourOfDay` is a single raw 0–23 feature, not cyclically encoded — a linear model can't represent midnight-wraparound (11pm/1am being "close"), so late-evening hours are somewhat under-flagged relative to early-morning ones. Documented in `ml/README.md`; fine for a synthetic bootstrap model, worth revisiting with real data.
  - **Trained unscaled** (no `StandardScaler`), unlike the reference notebook's LR models — avoids replicating scaler arithmetic by hand on the Kotlin side for only 3 features. See `ml/README.md`.
  - **Open discrepancy, flagged not resolved**: the manuscript's `DOOMSCROLLING_DETECTION` Data Dictionary table implies different inputs (`scrolling_duration_minutes`, `scroll_pause_frequency` as a count, no time-of-day column) than what got built. May come from a different manuscript section than whatever confirmed the "3 raw inputs" design originally — not necessarily wrong, just unreconciled. User said revisit later. See `ml/README.md` and project memory "module2-doomscroll-design".
- [x] Logistic Regression II — hourly excessive-usage prediction (2026-08-23/24). Manuscript's `EXCESSIVE_USAGE_PREDICTION` Data Dictionary table only specifies the output log schema (hour/predicted_usage_minutes/excessive_probability/is_excessive) — no input features enumerated, so a 4-feature set was proposed and confirmed instead of transcribed: `avgUsageThisHourMin` (30-day per-hour baseline), `cumulativeUsageTodayMin`, `usagePrevHourMin`, `isWeekend`. Label is real, not invented: whether the hour's usage crosses the already-existing `UserConfiguration.hourlyExcessiveUsageThresholdMs`. 86.8% test accuracy, 0.95 AUC-ROC. `generate_excessive_usage_dataset.py`/`train_excessive_usage_model.py` → `ExcessiveUsageModel.java` → `ExcessiveUsageDetector.kt` (plain 0.5 threshold — no risk-level "recipe" here, that pattern is LR-I-specific). New `ExcessiveUsagePredictionLog` Room entity mirrors the manuscript's output table almost field-for-field (DB version 10→11); `AppMonitorService.checkExcessiveUsagePrediction()` runs every 15 min, logs one row per hour, and reuses the existing **Overuse Alerts** Settings toggle for its notification rather than adding a new dedicated toggle (still fundamentally an overuse alert, just predictive). New `AppUsageLogDao.getAvgDurationForHourOfDay`/`getDurationInRange` queries. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs a device with enough real logged history for `avgUsageThisHourMin` to be non-zero.
- [x] m2cgen transpilation — to Java (not literally Kotlin; interops seamlessly, see `ml/README.md` for why)
- [x] View Risk Score — real as of 2026-08-23, see above (was: UI showed the now-deleted `UserConfiguration.riskSensitivity` placeholder)
- [x] Receive Overuse Predictions (2026-08-24) — new "Today's Usage Predictions" card in `ReportsScreen`/`ReportsViewModel`, reading `ExcessiveUsagePredictionLog.getPredictionsByDate` live (it's a Flow, so new hourly predictions from `AppMonitorService` appear without a manual refresh). Explicitly labeled "AI" — unlike the existing "Insights" card in the same screen (deliberately rule-based, no ML claim), this one genuinely is ML output, so the badge is honest rather than the fabricated "AI-POWERED" badge rejected during the 2026-08-11 Home/Reports redesign. Each row shows the hour, `predictedUsageMinutes`, and an Excessive/Normal badge. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs the app running long enough for `AppMonitorService`'s 15-minute prediction loop to actually produce rows.

## ✅ Module 6: Personalization & User Control (Done — 2026-08-25)

- [x] Select Usage Profile — Student/Work mode toggle (`SettingsScreen`)
- [x] Customize Lock Rules — covered under Module 3 above (shared deliverable)
- [x] Enable Focus Mode (2026-08-25) — real session backend replacing the old locally-ticking-counter mockup. New `FocusSession` Room entity/DAO (DB version 12→13, `endTimeMillis IS NULL` marks the active row) and `FocusModeViewModel` (`ui/focus/`): entering the screen starts a session if none is active (`startSessionIfNeeded`), `elapsedSeconds` is computed from the persisted `startTimeMillis` rather than a counter, so it survives navigating away and process death, not just recomposition. "End Focus Session" now actually surfaces the previously-unwired ported `EndFocusSessionSheet` ("End Session" / "Keep Going") instead of popping back immediately; "End Session" writes `endTimeMillis`/`creditMinutesEarned`, **and (2026-08-25 follow-up) is now actually spendable**: also credits a new `UserConfiguration.focusCreditBalanceMinutes` running balance (DB version 16→17). Redeemed from the Move hub — a new "Screen Credit" card (only rendered when the balance is positive) lets the user pick a locked app and spend the entire balance at once for that many minutes of real temporary unlock, via `MoveViewModel.redeemFocusCredit` reusing the exact same `AppMonitorService.grantTemporaryUnlock` mechanism Activity Challenges' XP-earned unlocks already use — same currency-into-unlock-minutes shape, just earned by focusing instead of moving. 1:1 exchange rate (no partial-amount picker, simplest honest mapping matching the literal "earned N min of screen credit" copy). `gradlew assembleDebug` verified. **Not yet runtime-verified.** Real enforcement in `AppMonitorService`: while a session is active, any foreground app whose Admin-curated category (`AppCategoryDao`) is Social Media or Entertainment gets kicked to the home screen via `performGlobalAction(GLOBAL_ACTION_HOME)` (new `focus_block_channel` notification, throttled 60s/logged to `NotificationLog` as `FOCUS_BLOCK`) — real curated data, not the teammate's hardcoded Instagram/TikTok/Twitter/YouTube/Reddit chip list (the "BLOCKED DURING FOCUS" chips now render real categorized app names instead, with an honest empty state if Admin hasn't categorized anything yet). App Lock's challenge-based lock (Module 3/4) takes priority over Focus Mode's plain block for any app that's both. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs a device with at least one app categorized Social Media/Entertainment to exercise the actual block.
- [x] Usage Goals screen (goal targets persisted 2026-08-25) — every goal target, not just "current usage," is now real: the weekly goal lives on a new `UserConfiguration.weeklyScreenTimeGoalMs` column, and the 3 per-category goals (Social Media/Entertainment/Gaming) live in a new `CategoryGoal` table (`categoryType` → `targetMs`, DB version 13→14) via `UsageGoalsViewModel.setWeeklyGoalHours`/`setCategoryGoalHours`. A category with no saved row yet falls back to the same preset the ported UI originally hardcoded as its slider's starting position (2h Social Media, 1.5h Entertainment, 0.5h Gaming) — not fabricated, just an honest "not customized yet" default. Sliders/presets save immediately on change (same pattern as the pre-existing daily-limit slider); "Save Goals" stays a plain "done" button, same as before. `gradlew assembleDebug` verified. **Not yet runtime-verified.**

## ✅ Module 5: Mental Health & Awareness (Done — 2026-08-25)

- [x] Answer Reflection Prompts — built from scratch this session (manuscript/`PROJECT_DOCUMENTATION.md` only specifies "reflection prompts, wellbeing check-ins," no Data Dictionary, so the shape below was proposed and built directly, same as Module 2 LR-II's undocumented feature set). New `ReflectionEntry` Room table (DB version 14→15) — one real row per calendar day (`dateKey` is the primary key), so re-opening the screen the same day edits that day's entry instead of creating a duplicate. `ReflectionViewModel`/`ReflectionScreen.kt` (`ui/reflection/`): a rotating daily prompt (10 static prompts, picked deterministically by day-of-year — written UI copy, same category as the existing `focusQuotes`/challenge-quote lists already in the app, not a stand-in for real data), a 5-point mood check-in (😞–😄), and a free-text answer field; "Save Reflection" upserts the day's row. A real "Past Reflections" list below shows the last 8 saved entries (date, mood emoji, answer) — genuine history, not sample data. Wired from Home's existing "Daily Reflection" `GoalCard` (previously a non-clickable "Coming soon" placeholder built 2026-08-23 specifically anticipating this module) — its subtitle now reads "Answered today"/"Tap to reflect" off a new real `HomeViewModel.hasReflectedToday` flow instead of the placeholder text. `gradlew assembleDebug` verified. **Not yet runtime-verified.**
- **Deliberately out of scope this round**: no integration with Module 2's risk scoring (mood/reflection data isn't fed into the Random Forest — would need retraining and wasn't asked for), and no streak/reminder mechanic (unlike Move's XP streak) — candidates for a later session if wanted.

## ✅ Module 7: Context-Aware AI (Done — 2026-08-25)

- [x] Receive Context Alerts — built from scratch this session, no teammate UI existed and this module wasn't built by them either. `PROJECT_DOCUMENTATION.md`'s own "Future Enhancements" section lists real GPS-based location locking as explicitly out of MVP scope, so this implements the lighter version its "Location-Based Locking: custom rules by WiFi network" line describes instead: match against the connected Wi-Fi network's *name*, not device coordinates — no new location SDK dependency (no Play Services Location added). New `UserConfiguration.contextAlertsEnabled`/`contextAlertWifiSsid` columns (DB version 15→16). `AppMonitorService` caches the current SSID every 30s (`WifiSsidReader.kt`, cheap local `WifiManager` read, no IPC) and, when enabled and the cached SSID matches the watched one, posts a passive notification (new `context_alert_channel`, logged to `NotificationLog` as `CONTEXT_ALERT`) the first time a Social/Entertainment-categorized app (same real Admin-curated categories Focus Mode blocks) opens — throttled 5 min, and deliberately never blocks or redirects, unlike Focus Mode, matching "Receive Context Alerts" being passive by name. Reading a real SSID needs `ACCESS_FINE_LOCATION` (an Android platform quirk since API 27, unrelated to GPS tracking) — the manifest already had this permission declared but dead/unused from an earlier scaffold; it's now actually requested, lazily, from the new `ContextAlertsScreen.kt` (`ui/settings/`) rather than added to Onboarding's permission set. That screen also lets the user type (or one-tap "Use this" off the live-read current network) the watched SSID, and is honest that this is Wi-Fi-name matching, not GPS geofencing, with a "Planned for Context-Aware AI" section listing real geofencing/multiple locations/per-location rules as future work. Wired from Settings' existing "Location Context" toggle row (previously a non-persisted local-only `remember` boolean with a "Coming soon" subtitle, one of the rows kept visible per the ported-UI-no-skip instruction) — its subtitle now reflects real state ("Off" / "On — set your Wi-Fi network below" / `Alerts on "<ssid>" Wi-Fi`). The separate "Location & Context AI" placeholder row (ACCOUNT section) was removed as a literal duplicate rather than wired to the same screen twice. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs a device connected to a real Wi-Fi network with at least one app categorized Social Media/Entertainment to exercise the actual alert.

All 9 modules plus the Admin Governance Layer are now built (Module 8's testing/QA backlog aside) — see the Summary table above.

## 🟡 Module 8: Integration & QA (Ongoing — cross-cutting)

- [x] Manual build verification (`gradlew compileDebugKotlin`, `gradlew assembleDebug`) after each code change this session
- [x] Unit tests, first pass (2026-08-25) — 20 tests across 6 files under `app/src/test/`, all pure-JVM (plain JUnit4 + `kotlinx.coroutines.runBlocking`, no Robolectric/device needed, no new dependencies added):
  - `ml/RiskScoringEngineTest.kt`, `ml/DoomscrollDetectorTest.kt`, `ml/ExcessiveUsageDetectorTest.kt` — AI inference. These test the hand-written decision logic *around* each generated model (level selection, sigmoid + threshold, the doomscroll risk-level "recipe"'s monotonicity), not the models' own trained accuracy — expected values are derived from each real model's own output for the given sample inputs, so a test fails if the wrapper logic drifts from what its own comments claim, not if the model's training changes.
  - `sensor/ChallengeSensitivityTest.kt` — `fromLabel`/`fromRiskLevel`/`displayLabel` mapping correctness, plus a monotonicity check (LOW < MEDIUM < HIGH effort).
  - `data/CategoryUsageAggregatorTest.kt` — `categoryTotals()`'s bucketing/summing/uncategorized-falls-into-OTHER behavior, against a hand-written `FakeAppCategoryDao` (implements the real `AppCategoryDao` interface in-memory — no Room/SQLite needed since the function only takes the DAO interface, not a concrete Room instance).
  - `ui/components/NotificationsPanelTest.kt` — timestamp calculations: `formatRelativeTime`'s minute/hour/day boundaries and the future-timestamp-never-negative clamp (visibility bumped `private`→`internal` to make it testable; behavior unchanged).
  - Deliberately not attempted this pass: DAOs' actual SQL query behavior (Room needs either a real device/emulator or Robolectric — this machine has neither an AVD configured nor Robolectric added yet, and this toolchain's AGP 9.2.1/Kotlin 2.2.10 versions are new enough that Robolectric compatibility isn't verified; a candidate for a later session, ideally proven with a quick spike before committing to it broadly).
- [ ] Integration tests (DB persistence, AccessibilityService, sensor lifecycle, UI state) — blocked on the same Room/Robolectric-or-device gap as DAO unit tests above; AccessibilityService/sensor lifecycle additionally need a real device (already known: no local emulator on this machine, see Module 4's Break Reminder entry).
- [ ] End-to-end tests (lock → shake → unlock, risk-scoring difficulty, settings persistence, rotation) — needs a real device.
- [ ] Performance/battery profiling against `PROJECT_DOCUMENTATION.md`'s target metrics — needs a real device.
- [ ] ISO/IEC 25010 validation pass
