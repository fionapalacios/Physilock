# Physi-Lock Module Progress Checker

Tracks progress against the module-based development lifecycle in `PROJECT_DOCUMENTATION.md` (see its "Development Lifecycle" table for the canonical module list). Check items off as they're built; update the module-level status line whenever a module's checklist changes meaningfully. Last synced: 2026-09-09.

**Note on scope:** the per-module checklists below map the manuscript's 21 User + 6 Admin functions (see FDD memory) onto modules by function name/semantics. That mapping is my best-effort inference, not a confirmed 1:1 copy of the manuscript's 13-submodule breakdown — cross-check against the manuscript FDD if exact submodule wording matters for grading/documentation.

---

## Summary

| Status | Modules |
|--------|---------|
| ✅ **Done** | Admin Governance Layer, 1. Core Monitoring & Usage Awareness, 3. Motion-Responsive Locking, 4. Smart Intervention System, 5. Mental Health & Awareness, 6. Personalization & User Control, 7. Context-Aware AI |
| 🟢 **Most Advanced** (built, may need polish) | 0. Foundation, 2. AI-Based Behavior Analysis |
| 🟡 **Partial / In Progress** | 8. Integration & QA (ongoing by nature) |
| 🔴 **Not Started** | — |

---

## UI Status (screen-by-screen cut)

Same underlying progress as the module checklists below, viewed by screen instead of by feature module — useful for "what does the app actually look like right now" questions. Last checked: 2026-08-03.

**Built**
- [x] `LandingScreen` — redesigned 2026-08-24 with the teammate's swipeable module carousel (was: text-based hero + static module list). See "Teammate UI re-sync, 2026-08-24" below.
- [x] `LoginScreen`/`CreateAccountScreen` — split login/register screens (replaced the old combined `AuthScreen` 2026-08-23), all fields, error display, "Continue with Google" button, real Student/Work Usage Mode picker (2026-08-24), wired into `MainActivity`'s `AUTH_LOGIN`/`AUTH_REGISTER` stages
- [x] `OnboardingScreen` — redesigned 2026-08-24 with the teammate's card-list permission layout (was: YPT-style "Continue X/N" list), real permission checks/settings-intents preserved underneath. See "Teammate UI re-sync, 2026-08-24" below.
- [x] `HomeScreen` (`ui/home/HomeScreen.kt`) — today's screen time (with real configured daily limit + progress ring), real risk-level ring (Module 2, was a lock-sensitivity placeholder ring until 2026-08-23), locked-apps summary, Usage Goals/Daily Reflection `GoalsRow`. Redesigned 2026-08-11, nav-wired 2026-08-23, see below.
- [x] `ReportsScreen` (`ui/reports/ReportsScreen.kt`) — real weekly bar chart (dynamic scale, over-limit coloring), stat cards, top apps, rule-based insights, real AI usage predictions card (Module 2, added 2026-08-24). Redesigned 2026-08-11, see below.
- [x] `MoveScreen` — redesigned 2026-08-25 with the teammate's XP/streak card + challenge-card list visuals; real Activity Challenges (Module 4) underneath. New `ui/challenge/ChallengeActiveScreen.kt` for the in-progress state. See "Full teammate UI sync" below.
- [x] `SettingsScreen` — redesigned 2026-08-25, corrected same day: their `SettingsScreen.kt` was wrongly logged as a 0-byte stub (it's real, 545 lines — `StudentModeScreen.kt`/`WorkModeScreen.kt` are the ones that are actually empty), so the first pass was a from-scratch restyle instead of an actual port; replaced with the real port (ProfileCard, Work/Student usage-mode card, icon-row section-card layout, theme tokens remapped) rewired onto real data — same real functionality as before plus this repo's real toggles their design didn't have (Overuse Alert, Doomscrolling Detection). New `StudentModeScreen.kt`/`WorkModeScreen.kt` (built from scratch, genuinely empty on their side) reachable from the mode cards.
- [x] `AppLockRulesScreen` — pick installed apps to lock
- [x] `LockScreen` / `LockActivity` (`ui/lock/`) — real Activity Challenges progress overlay (Module 4)
- [x] `BottomNavBar` / `NavGraph` — bottom nav (restyled 2026-08-25, pill-highlighted selected icon) + routing
- [x] `AdminHomeScreen` + 4 tab sections (Accounts/Categories/Defaults/Analytics)
- [x] `ReflectionScreen` (`ui/reflection/`) — built from scratch 2026-08-25 (Module 5, no teammate UI existed to port); daily rotating prompt, mood check-in, real persisted answer history
- [x] `ContextAlertsScreen` (`ui/settings/`) — built from scratch 2026-08-25 (Module 7, no teammate UI existed to port); Wi-Fi-network-name-matched alerts, reached from Settings' "Location Context" row

**File structure & naming alignment (2026-08-11):** two things folded into one session — (1) this repo's own internal folder organization was inconsistent (`HomeScreen.kt`/`ReportsScreen.kt` sat in a leftover `ui/screens/` folder while their ViewModels already lived in `ui/home/`/`ui/reports/`; `LockActivity.kt`/`LockScreen.kt` sat loose directly in `ui/` with no feature folder) — fixed by moving both screens into their ViewModels' folders and creating `ui/lock/` for the lock files, updating all imports and `AndroidManifest.xml`. (2) A teammate's separate UI repo (`github.com/fionapalacios/Physilock`, real work is on branch `sprint-2-ui-navigation` — `main` is just an empty scaffold) was cloned and compared. Package name differs (`com.prototype.physi_lock` vs this repo's `com.example.physi_lock`) and a real token collision was found: both theme files define a color named `DeepOlive`, but with *different hex values* (`#3D4928` here vs `#536136` there, which is what this repo separately calls `MutedText`) — a genuine naming collision, not just a style difference.

Decision: for the three screens with a functional overlap, take the teammate's visual design but keep this repo's real data layer (Firebase/Room/`UsageStatsManager`) — teammate's versions are UI-only mockups with **no backend**, in two cases entirely hardcoded fake data (teammate's `DashboardScreen.kt` ships a fixed list of fabricated notifications and a fake "AI" predictive-overuse claim; `ReportsScreen.kt` is 100% invented stats, no ViewModel at all). Per-screen outcome:
- **Home** — redesigned using teammate's visual language (progress rings via a new `ui/components/CircularProgressRing.kt`, restyled cards) wired to real `HomeViewModel` data. Added a real `dailyLimitMinutes` StateFlow (was previously hardcoded "240m"). Dropped the fabricated break-reminder banner, "predictive overuse AI" banner, and static goals row — none were backed by real data on either side.
- **Reports** — redesigned the same way, wired to real `ReportsViewModel` data (added a real `dailyLimitMinutes` StateFlow there too, used for over-limit bar coloring). Kept the honest "Insights" label rather than teammate's "AI-POWERED" badge, since these are rule-based, not ML. Skipped teammate's category-breakdown card and week/month toggle — no real per-category usage aggregation exists yet to back it (candidate for later, once Module 2 or a category-aware usage query exists).
- **Settings** — teammate's `SettingsScreen.kt`/`StudentModeScreen.kt`/`WorkModeScreen.kt` turned out to be empty (0-byte) stub files, never actually written. Kept this repo's existing `SettingsScreen` as-is; revisit once the teammate's branch has real content.
- **Move** — kept this repo's existing version (user's explicit choice); teammate has a visually different `MoveScreen` not adopted. **Onboarding/permissions and Landing were later adopted on 2026-08-24** — see "Teammate UI re-sync, 2026-08-24" further down.

New shared theme tokens added to `Color.kt` to support the above and future ports from teammate's branch: `BackgroundLight`, `SecondarySage`, `TertiaryTan`, `ErrorRed`. (Teammate's `PrimaryDark`/`PrimaryGreen`/`AccentLavender` already had equivalents here — `DeepOlive`/`SageAccent`/`Orchid` — reused rather than duplicated.)

**Ported 2026-08-23** (re-cloned teammate's repo at `sprint-2-ui-navigation`, `gradlew compileDebugKotlin` passes): `LoginScreen`/`CreateAccountScreen`/`ForgotPasswordScreen`/`VerifyEmailScreen` → `ui/auth/`, `FocusModeScreen`/`EndFocusSessionSheet` → new `ui/focus/`, `UsageGoalsScreen`/`UsageGoalsViewModel` → new `ui/goals/`, shared components `AuthComponents`/`DashboardCard`/`NotificationsPanel` → `ui/components/` (joining the existing `CircularProgressRing.kt`). Package renamed to `com.example.physi_lock`, theme tokens remapped (teammate's `PrimaryDark`/`PrimaryGreen`/`AccentLavender`/`DeepOlive`/`NunitoFontFamily` → this repo's `DeepOlive`/`SageAccent`/`Orchid`/`MutedText`/`Nunito`); copied `logo.png`/`ic_google.xml` drawables the auth screens need (didn't exist here before). **Deliberately left unwired** — none of these are routed in `NavGraph.kt` yet, per plan; ask before wiring.
- Auth screens were rewired away from the teammate's non-existent `data.AuthRepository`/`AuthResult` mock to callback params matching this repo's existing `AuthScreen.kt`/`AuthFormState`/`AuthViewModel` conventions (`onLogin`, `onCreateAccount: (AuthFormState) -> Unit`, etc.), so wiring later is a matter of passing real `AuthViewModel` calls in, not further rewiring. `ForgotPasswordScreen`/`VerifyEmailScreen` still have no real backend (no reset-code service, no email-verification service exists) — kept as UI-only with client-side format checks and outbound hooks (`onSendCode`, `onVerified`) for whichever service gets built later.
- `UsageGoalsViewModel` ported near-verbatim — its `UserConfiguration`/`UserConfigurationDao` field names (`dailyScreenTimeThresholdMs`, `getActiveConfiguration()`/`getActiveConfigurationOnce()`) already matched this repo exactly, so only the total-daily-limit goal is real; per-category goals (Social Media/Entertainment/Gaming) and the weekly goal card stay UI-local demo values, same as the teammate's original — no category-aware usage schema exists yet.
- Dropped one thing from the port rather than carrying it over: `UsageGoalsScreen`'s "AI Recommendation" card was a hardcoded fake risk-score claim ("lower your addiction risk score from 62 to ~45") — the same anti-pattern already rejected when the Home/Reports redesign dropped the teammate's fabricated "predictive overuse AI" banner (see 2026-08-11 entry below). Also dropped `CreateAccountScreen`'s Usage Mode picker — cosmetic only in the teammate's original (the selection was never actually passed into registration), and a real mode picker is a separate not-yet-built screen (see `StudentModeScreen`/`WorkModeScreen` below).
- **New discovery, not yet ported**: the teammate's `ui/screens/settings/` now also has real (non-stub) `EditProfileScreen.kt`/`ProfileViewModel.kt` that didn't exist as of the 2026-08-11 check. `StudentModeScreen.kt`/`WorkModeScreen.kt` are still confirmed 0-byte stubs, unchanged.

**Nav-wiring (2026-08-23, same-day follow-up):** `gradlew compileDebugKotlin` passes.
- **Auth**: `AuthScreen.kt` deleted, replaced by `LoginScreen`/`CreateAccountScreen` (`MainActivity`'s `AppStage.AUTH` split into `AUTH_LOGIN`/`AUTH_REGISTER`, navigating to each other). `AuthFormState` moved from the deleted file into `AuthViewModel.kt` (still used by `HybridAccountRepository.register`). `ForgotPasswordScreen`/`VerifyEmailScreen` are still NOT wired — `onNavigateToForgotPassword` stays a no-op like the old `AuthScreen`'s did, since there's still no reset-code backend and no follow-up password-reset screen to land on after verification; wiring them now would just dead-end.
- **Focus Mode**: `HomeScreen`'s existing "Focus Mode" tile (was `onClick = {}`, "Coming soon") now navigates to a new `Screen.Focus` route. Added `FocusModeRoute` in `FocusModeScreen.kt` — a thin wrapper that ticks a local `elapsedSeconds` counter (`LaunchedEffect` + 1s `delay` loop) since no real session/timer backend exists yet (Module 6). Timer resets on re-entry; nothing is persisted.
- **Usage Goals**: user pointed out the teammate's `DashboardScreen.kt` already had a home for this — a `GoalsRow` of two side-by-side `GoalCard`s ("Usage Goals" + "Daily Reflection") at the bottom of the dashboard, dropped during the 2026-08-11 Home redesign as an unbacked "static goals row". Ported that `GoalsRow`/`GoalCard` pair into `HomeScreen.kt` instead of the standalone tile tried first. "Usage Goals" card now shows a real subtitle (`todayMinutes`/`dailyLimitMinutes`, reusing the existing `formatMinutes` helper — teammate's was a hardcoded "5h / 7h today") and navigates to `Screen.Goals` → `UsageGoalsScreen`. "Daily Reflection" card is a non-clickable "Coming soon" placeholder (Module 5 is still unbuilt) — dropped the teammate's "Not done yet 💭" subtitle since it implies real reflection tracking that doesn't exist.
- **EditProfileScreen**: ported from the teammate's repo (re-cloned again this session) but NOT as-is — the original `ProfileViewModel` read/wrote a local `UserConfiguration` row, which isn't this repo's account data source (`Account` via `FirebaseAccountRepository`/`HybridAccountRepository`); porting it unmodified would've silently broken profile edits (writes go to the wrong table, never sync). Rewired to take `currentAccount: Account?`/`onSave: (Account) -> Unit` instead, matching the pattern the old inline editor already used. Also dropped two things from the port: the "Change Password" fields (teammate's version never wired them to anything — no password-change backend exists, so keeping them would be dead UI that looks functional) and the avatar picker (no photo-storage backend). Added an Occupation field back in (the teammate's original didn't have one, but the old inline editor did — dropping it would've been a real regression, not a fake-feature cleanup). Replaces `SettingsScreen`'s old inline `OutlinedTextField`-based account editor Card with a full-screen `EditProfileScreen`, toggled the same way `AppLockRulesScreen` already was (a boolean + early `return`).

**Corrections (2026-08-10):** two "not built" items below were stale — verified against current code, both already exist:
- **Logout** — `SettingsScreen`'s `AccountSummaryCard` already had a Log Out button, wired `MainActivity` → `NavGraph` → `SettingsScreen`, since before this entry was last written. It just didn't actually call Firebase — it only reset local UI state (`currentAccount = null`, navigate to `AUTH`), leaving the Firebase Auth session alive underneath. Fixed today: added `logout()` to `AccountRepository`/`FirebaseAccountRepository` (`auth.signOut()`) and `HybridAccountRepository` (delegates — purely local, no offline handling needed), `AuthViewModel.logout(context)` calls it plus best-effort clears Credential Manager's cached Google account state, and `MainActivity`'s `onLogout` now calls it before resetting UI state.
- **Occupation field** — was present in `SettingsScreen`'s old inline account editor; now lives in `EditProfileScreen` (2026-08-23 nav-wiring, see above). Not a gap.

**Wired 2026-08-24** (`gradlew assembleDebug` passes):
- **Forgot password** — adapted from the ported custom 6-digit-code UI to Firebase Auth's native link-based reset (user's explicit call — no Cloud Functions/email-service infra exists to build a real code backend). `FirebaseAccountRepository.sendPasswordResetEmail()` wraps `auth.sendPasswordResetEmail()`; `ForgotPasswordScreen` now has a `sent` confirmation state ("check your email") instead of navigating to a code-entry step — the reset itself happens on Firebase's own hosted page after the user taps the emailed link, nothing to do in-app after that. New `AppStage.FORGOT_PASSWORD` in `MainActivity`; `LoginScreen`'s "Forgot password?" link now wired (was a no-op). **Runtime-verified 2026-08-24** on the vivo V2109: real email sent (confirmed even a nonexistent address gets the same "sent" response — Firebase's email-enumeration protection, not a bug), confirmation screen renders with the entered address, "Back to Login" returns cleanly.
- **Email verification** — same link-based adaptation. `FirebaseAccountRepository.register()` now calls `sendEmailVerification()` (best-effort, after the username-uniqueness check passes) right after creating the Firebase Auth user. `VerifyEmailScreen` rewritten from a 6-digit code grid to a "we sent a link, tap it, then tap Continue" flow — `onCheckVerified` reloads the Firebase user and re-checks `isEmailVerified` (a link click doesn't push a signal into the app, so this is a manual recheck, not push-driven). New `AppStage.VERIFY_EMAIL`, inserted only on the email/password registration path (`AUTH_REGISTER` → `VERIFY_EMAIL` → `ONBOARDING`); **deliberately scoped out**: login and Google sign-in (Google accounts are pre-verified by Firebase) skip straight to Onboarding as before — existing accounts aren't retroactively gated on verification, only fresh signups. **Not yet runtime-verified** — needs an actual registration to exercise (user asked not to create a throwaway test account this session).
- **Notifications UI** — `NotificationsOverlay`/`NotificationEntry` (ported 2026-08-23) now has a real backing data source: new `NotificationLog` Room entity/DAO (`notification_logs` table, DB version 11→12), inserted from all 4 existing `AppMonitorService` alert paths (Break Reminder, Overuse Alert, Doomscroll Alert, Excessive Usage Prediction) right after each real system notification is actually posted — so the in-app history only ever reflects notifications that really fired, not a parallel/independent log. New `NotificationsViewModel` (`ui/components/`) exposes the log as a `Flow`; `HomeScreen`'s existing (previously decorative, no `onClick`) bell icon now opens the overlay and shows an unread-count dot, wired via `NotificationLog.toEntry()` mapping type→icon/color. **Runtime-verified 2026-08-24** on the vivo V2109: a real Overuse Alert appeared in the overlay with correct icon/title/description/timestamp, unread badge counted correctly, "Mark all read" cleared it, dismiss-on-backdrop-tap worked.
- [x] Focus Mode screens (Module 6) — `FocusModeScreen`/`EndFocusSessionSheet` ported 2026-08-23 to `ui/focus/`; real session/timer/app-blocking backend added 2026-08-25 (see Module 6 below).
- [x] Reflection Prompts screen (Module 5) — built from scratch 2026-08-25, no teammate UI to port (see Module 5 below).
- [x] Context Alerts screen (Module 7) — built from scratch 2026-08-25, no teammate UI to port (see Module 7 below).

**Teammate UI re-sync, 2026-08-24** (re-cloned `sprint-2-ui-navigation`, now 4 commits ahead of the 2026-08-23 port — all dated 2026-08-22, same day as the last sync but not yet pulled in). `gradlew assembleDebug` passes.
- **Registration Usage Mode picker** — the 2026-08-11 port had deliberately dropped the teammate's Student/Work picker from `CreateAccountScreen` as non-functional dead UI (their `usageMode` state was never passed into registration — still true in the teammate's latest). Re-added the same picker (emoji pill buttons), but real this time: `AuthFormState` gained a `usageMode` field, and `MainActivity` calls `settingsViewModel.setUserMode(form.usageMode)` right after a successful registration. This **superseded and replaced** the standalone post-onboarding `ModePickerScreen` built earlier the same day (2026-08-24) — asking for the mode twice in one signup flow (once in the form, once again right after onboarding) would've been redundant, so `ModePickerScreen.kt` and the `AppStage.MODE_PICKER` stage were deleted rather than kept as dead code. **Runtime-verified**: picker renders on the real Register tab, Student/Work selection toggles correctly.
- **Branding/icon refresh** — replaced the default Android-Studio-template webp launcher icons and generic green grid background with the teammate's layered PNG adaptive icon (foreground/background per density, `mipmap-anydpi-v26`) and Play Store icon; `res/drawable/logo.png` (the auth-header logo) was already byte-identical to the teammate's refreshed version from an earlier port, so no change needed there.
- **Landing screen rework** — `LandingScreen.kt` replaced with the teammate's swipeable `HorizontalPager` module carousel (logo, "Physi-Lock" header, 7-slide swipeable card with page-dot indicator, "Get Started"/"I already have an account" buttons) — purely cosmetic port, no real logic on this screen either way. **Fixed same day**: both buttons originally routed to the same place (Login); split so "Get Started" → `AUTH_REGISTER` and "I already have an account" → `AUTH_LOGIN` (`LandingScreen` gained a second `onSignIn` callback), matching the copy's actual intent. `CreateAccountScreen`'s Back button now returns to `AppStage.LANDING` (was `AUTH_LOGIN`) for the same reason. **Runtime-verified**: carousel swipes correctly between slides; button routing verified after the fix.
- **Onboarding/permissions rework** — `OnboardingScreen.kt` rebuilt with the teammate's visual layout (logo header, card-list permission rows, privacy note, single progress-counted Continue button) but keeping this repo's **real** permission logic — the teammate's version fakes all 5 permissions as local in-memory toggles with no actual settings-intent launches; this repo's real `isGranted(context)` checks and `context.startActivity(settingsIntent)` calls were preserved underneath the new visuals, same "their design, our data layer" pattern used for the Home/Reports port.
  - **Notifications now requests via Android's native runtime-permission dialog** (`ActivityResultContracts.RequestPermission()`, real Allow/Don't allow) instead of opening Settings — this is the one row where Android actually allows an app to show that dialog. Falls back to Settings only once the OS reports the permission permanently denied.
  - **Explained, not fixed (can't be — OS restriction)**: Usage Access, Display over other apps, and Accessibility Service are Android "special app access" grants with no request-dialog API for any app — every screen-time app (YPT, Digital Wellbeing, Forest included) has to send the user to Settings for these three specifically, by Google's design.
  - **Two real bugs found via on-device testing 2026-08-25 and fixed same day**: (1) Battery Optimization's row silently did nothing — `AndroidManifest.xml` never declared `android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`, so `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` had nothing to show; added the manifest permission. (2) The Continue button kept re-opening Accessibility Settings even after the user had already granted it — the cached `grantedStates` (only refreshed on `ON_RESUME`) could race with the OS finishing the accessibility service bind; Continue's click handler now re-checks `isGranted(context)` fresh at click time instead of trusting the cache. **Runtime-verified 2026-08-25** on the vivo V2109 (user drove the device directly after adb touch injection became unusable mid-session — see [[feedback_vivo_device_adb_latency]]): live permission state read correctly (Usage Access/Overlay/Notifications showed granted, Accessibility/Battery didn't, matching real device state); Accessibility Settings flow confirmed working end-to-end (Settings → Downloaded apps → Physi-Lock → toggle on → system "Allow" confirmation → back). Both fixes confirmed after rebuild: Battery Optimization now shows the real system Allow/Deny dialog, and Continue now completes onboarding (goes to Dashboard) immediately after granting Accessibility instead of re-opening Settings.
- **Typography scale**: pulled in this same day, see "Full teammate UI sync" below. Dark mode/dynamic-color from their `Theme.kt` stays not-pulled — every screen in this repo references static theme tokens (`DeepOlive`, `BackgroundLight`, etc.) directly rather than `MaterialTheme.colorScheme`, so adopting it would take an app-wide refactor, not a file port; flagged as a known gap.

**Full teammate UI sync completed, 2026-08-25** (user asked to fetch everything remaining). Final inventory pass against the teammate's full `ui/` tree:
- **Move hub (`MoveScreen.kt`) redesigned** — teammate's XP/streak card + challenge-card list visuals, wired to real `MoveViewModel` data throughout (`totalXp`, `streakDays`, `lockedApps`) plus one new real data source: `completedToday` (new `MotionInterventionLogDao.getCompletedChallengeTypesAfter` query + `MoveViewModel` StateFlow), so the "Completed" badge reflects challenges actually finished today instead of nothing. The teammate's hardcoded weekly streak-calendar (fixed "3-day streak", first 3 of 7 dots always lit) had no real per-day data behind it on either side; rather than fabricate specific days, it now lights the last `min(streakDays, 7)` slots off the real rolling count — honest simplification, not invented data. Challenge target labels (e.g. "100 steps", "3m") are now computed live from `ChallengeSensitivity` instead of the teammate's fixed strings, since this repo's difficulty is adjustable (Settings, or adaptive off the Module 2 risk score) and theirs wasn't. Real ACTIVITY_RECOGNITION permission request + locked-app picker dialog logic (already in this repo, absent from theirs) preserved underneath.
- **New `ChallengeActiveScreen.kt`** (`ui/challenge/`) — teammate's in-challenge visuals (icon badge, progress ring, italic quote card, reward chip, "Claim +XP" button), but real detection underneath: their version fakes all progress (time-based challenges just run a 1-second `delay` loop; the one rep-based challenge is a manual "Tap per Rep" button — no sensor read at all), while this port keeps `ChallengeProgressContent` — the same real accelerometer/gyroscope/step-sensor detection already runtime-verified on-device for Module 4. `onClaim` only fires once the real detector reports completion, not on a timer.
- **`BottomNavBar` restyled** — teammate's pill-highlighted selected-icon look ported in place of the stock Material3 `NavigationBar`; kept this repo's `NavController`-driven routing signature (theirs takes a plain enum + callback with no controller) so `NavGraph.kt`'s call site didn't need to change.
- **`MotionChallengeScreen.kt`/`ChallengeCompleteScreen.kt`/`ShakeDetector.kt` ported after all** (2026-08-25 follow-up — user asked not to skip any UI, even superseded designs). These are the teammate's *older* generic shake-challenge design (real accelerometer detection, not fake — ported as-is to `sensor/ShakeDetector.kt` and `ui/challenge/MotionChallengeScreen.kt`). **Deliberately still not wired into the live app**: this repo's real LockActivity/LockScreen already cover the same feature with 3 distinct, better-verified challenge types (Module 4) — routing a real lock trigger through this generic single-shake screen instead would be a regression, not an upgrade. Kept available in the codebase per the no-skip instruction, clearly kdoc'd as unwired-on-purpose rather than silently dead code. **Removed 2026-08-27** — user asked for the codebase to only carry real working logic, no superseded/placeholder designs; `MotionChallengeScreen.kt` (and the `ChallengeCompleteScreen` composable at its bottom) and `sensor/ShakeDetector.kt` deleted outright. Confirmed dead first (no NavGraph route, no other screen import, no test coverage) before deleting.
- **Nothing else left to fetch from the teammate's repo**: `DashboardScreen.kt`/`DashboardViewModel.kt`/`ReportsScreen.kt` were already absorbed into this repo's Home/Reports redesign in an earlier session (mock ViewModel, not a literal-port candidate); `Theme.kt` dark mode flagged above as needing an app-wide refactor rather than a file port.
- `gradlew assembleDebug` passes; build installed on the vivo V2109 but not yet click-tested live (Move hub, a challenge run, and the new bottom nav bar styling all worth a pass next time on-device).

**Follow-up requests, 2026-08-25 (same day)** — `gradlew assembleDebug` passes:
- **Typography scale added** — full Material type scale (display/headline/title/body/label, all sizes) ported into `Type.kt` from the teammate's `Type.kt`, remapped to this repo's `Nunito` font token. Purely additive: the 5 styles this repo had already defined kept their exact existing values (so no already-visible text shifted), only the previously-missing slots were filled in. Default Material3 components this repo already uses elsewhere (`AlertDialog`, and the not-yet-restyled Admin sections/`AppLockRulesScreen`) pick the fuller scale up automatically via `MaterialTheme.typography`. Dark mode from the same file was explicitly excluded per the user's instruction.
- **Settings, Student Mode, and Work Mode screens built** — initial pass this session wrongly treated the teammate's `SettingsScreen.kt` as a 0-byte stub (stale/incorrect — see "corrected same day" note above, it's real, 545 lines) and did a from-scratch restyle instead of porting it; corrected later the same day into a real port of their design. `StudentModeScreen.kt`/`WorkModeScreen.kt` genuinely are 0-byte on their side, so those stay built-from-scratch — reachable by tapping either mode card in Settings, with a real "Switch to this mode" action and a real "your current settings" summary pulled from `UserConfiguration`. Per the no-skip-UI instruction, these aren't left as empty shells even though true per-mode-differentiated backend logic doesn't exist yet (`userMode` currently only labels which profile is active, all thresholds are shared/global) — each screen has an explicit, honestly-labeled "Planned for [Mode]" section listing what per-mode logic isn't built (e.g. class-schedule awareness, work-hours quiet mode) instead of pretending it already works.
- **Home's permission-status banner removed** — Onboarding already gates all 4 permissions before a user ever reaches Home, so the duplicate status card there was redundant. Removed `PermissionStatusBanner`/`PermissionRow` and the now-unused permission StateFlows from `HomeViewModel` (`hasUsageAccess`/`notificationsEnabled`/`hasOverlayPermission`/`hasAccessibilityAccess` and their settings-intent helpers) rather than leaving them as dead code; the on-resume refresh hook (`refreshPermissionState` → renamed `refreshOnResume`) still refreshes today's screen time and the Module 2 risk score, which shared that same hook.
- **Real app names instead of package names**: `AppLockRulesScreen` and `AdminCategoriesSection` were both showing a raw `app.packageName` (e.g. `com.google.android.youtube`) as a small subtitle line under the real app name — removed in both places. Audited the rest of the codebase for the same pattern (`grep` for `.packageName` reaching a `Text`) and found no other instances; every other app-name display already resolves through a real `PackageManager` label lookup (`UsageStatsRepository.getAppLabel`, `AppMonitorService.getAppName`, or `ResolveInfo.loadLabel` in `AdminViewModel`/`AppLockViewModel`'s installed-apps lists) rather than falling back to the package name.

**Second follow-up round, 2026-08-25 (same day)** — `gradlew assembleDebug` passes:
- **Persistent login** — closing/swiping the app away and reopening it always restarted at Landing, forcing a re-login every time, even though Firebase Auth already keeps its own session on disk. `stage` was `rememberSaveable` but that only survives configuration changes, not a real process kill from a swipe-away. Added `AccountRepository.currentUserId()`/`getCurrentAccount()` (implemented in `FirebaseAccountRepository`, with an offline fallback to the local `CachedAccountDao` cache in `HybridAccountRepository` — new `CachedAccountDao.findById` query — mirroring `login`'s existing offline-fallback shape). `MainActivity` now runs a one-time `LaunchedEffect` on cold start that resolves the current session and redirects straight to Home/AdminHome, showing a brief logo splash (`SessionCheckSplash`) instead of flashing Landing first. Only fires if nothing else has already navigated (`stage == LANDING`), so it can't hijack an in-progress flow after a config change.
- **"Medium" sensitivity renamed to "Moderate" (display only)** — the stored/compared value stays `"MEDIUM"` (`UserConfiguration.motionLockSensitivity`, `AdminDefaultsSection`'s defaults, etc. all still use it) since renaming the persisted string would be a data-format change for no real benefit; only the user-facing label changed. Added `ChallengeSensitivity.displayLabel()` and switched the 4 places that were title-casing the raw enum string (`SettingsScreen`, `AdminDefaultsSection`, `StudentModeScreen`, `WorkModeScreen`) to call it instead.
- **Landing carousel page-number removed** — dropped the "2/7"-style counter from each slide's header row in `LandingScreen.kt`; the dot indicator below the card already shows position, the number was redundant.
- **Logo update — resolved.** User placed a new file directly at `res/drawable/logo.svg`. It turned out to be a raster PNG (736×736px) wrapped in an SVG `<pattern>`/`<image>` (Figma-style export), not real vector art — and Android can't load a raw `.svg` as a drawable resource anyway (only its own VectorDrawable XML format). Extracted the embedded base64 PNG and saved it as `res/drawable/logo.png` (390KB, 736×736 — well above every prior asset size), deleted the `.svg` wrapper. Scope confirmed explicitly with the user: in-app header logo only (`MainActivity`, `LandingScreen`, `AuthComponents`, `OnboardingScreen` — all via `R.drawable.logo`); the launcher/Play Store icon (`mipmap-*/ic_launcher*`, the teammate's ported vector design) was explicitly left untouched.

**Bug-fix + teammate-UI-alignment round, 2026-08-25 (same day)** — `gradlew assembleDebug` passes, installed and runtime-verified on the vivo V2109 across 3 install cycles this round:
- **Root-caused and fixed: App Lock Rules was missing most installed apps (TikTok, Instagram, Duolingo, etc.), and Reports' "Top Apps This Week" was showing raw package names instead of app names.** Same root cause for both: `AndroidManifest.xml` had no `<queries>` element. This app targets SDK 36, so Android's package-visibility restrictions (API 30+) hide other installed apps from `PackageManager` by default — `AppLockViewModel`'s `queryIntentActivities(ACTION_MAIN/CATEGORY_LAUNCHER)` silently returned almost nothing, and `UsageStatsRepository.getAppLabel()`'s `getApplicationInfo()` call threw for invisible packages, falling back to the raw package name. Fixed with a `<queries>` block matching `ACTION_MAIN`/`CATEGORY_LAUNCHER` — one of Android's documented visibility exemptions, so it doesn't need the restricted `QUERY_ALL_PACKAGES` permission.
- **Home → "Lock Apps" now navigates directly to App Lock Rules.** It was routing to the Settings tab only (`onManageAppLock` → `Screen.Settings.route`), leaving the user to find App Lock Rules manually from there. Added a real `Screen.AppLockRules` nav route (`NavGraph.kt`) rendering `AppLockRulesScreen` directly; Home's "Lock Apps" tile and the app-usage card both point at it now.
- **Home: "Locked Apps Today" → "App Usage Today"**, matching the teammate's actual `AppUsageCard` design (their DashboardScreen, not previously ported). Was locked-apps-only with its own redundant "Manage Locks" button (redundant once "Lock Apps" navigates directly); now shows general top-5 app usage today from the same `UsageStatsManager` source Reports uses, no more locked-apps filter, no more separate manage button. `HomeViewModel.appUsageToday` replaces the old `lockedAppsToday` combine-flow.
- **Reports: added the "Daily"/"Weekly" period toggle** (renamed from the teammate's literal "This Week"/"This Month" labels, since that's what the toggle actually switches — a per-day chart vs. a not-yet-built per-week chart) — wasn't in this repo at all before. "Daily" stays fully real; "Weekly" shows an honest "Weekly view coming soon" placeholder rather than the teammate's own invented static numbers (consistent with this project's standing rejection of fabricated stats elsewhere).
- **Reports: Top Apps This Week now formats duration as "1h 30m"** instead of raw minutes (reused the screen's existing `formatMinutes` helper). Insights/Daily-Usage-chart day labels changed from a single letter to full `Mon`/`Tue`/`Wed`/... (`ReportsViewModel`'s `dayLabel` no longer truncates to 1 char).
- **Confirmed for the user: "Today's Usage Predictions" (Reports) is NOT ported UI** — checked the teammate's whole repo, no prediction/forecast feature exists anywhere in their Dashboard or Reports screens. It's this app's own Module 2 (Logistic Regression II) output, unrelated to the teammate-UI-fidelity work.
- **Settings: removed the "Usage & Motion" card entirely** — confirmed against the teammate's real `SettingsScreen.kt` that no such card, Daily Limit slider, Break Reminder interval slider, or Motion Lock Sensitivity picker exists in their design at all; user asked to strictly match their row-based list instead. Consequence flagged to the user: Daily Screen Time Limit is still separately editable via the Usage Goals screen (shares the same `UserConfiguration.dailyScreenTimeThresholdMs` field). **Break Reminder interval's editable UI restored 2026-08-26** — see the follow-up entry near the bottom of this file (tap the "Break Reminders" row instead of reintroducing the dropped slider card).
- **Settings/Admin: Motion Lock Sensitivity and Doomscrolling Detection reclassified as Admin-controlled, User-read-only** — per explicit instruction, users should view but never set their own sensitivity. Added a new `SettingsTrailing.Label` row type (plain status text, no Switch/chevron) and removed `SettingsViewModel.setDoomscrollingDetectionEnabled`/`setMotionLockSensitivity` (now unused — nothing calls them). `AdminDefaultsSection`'s existing Default Motion Lock Sensitivity picker and Default Doomscrolling Detection toggle are unchanged (Admin already owned these), just corrected its disclaimer text from "users can still override" to "users can view but not change." **Known gap, deliberately deferred to a future session per user request**: there's no real Doomscrolling *Sensitivity* scale anywhere (LOW/MEDIUM/HIGH) — only ever an on/off flag; the detector auto-derives its threshold from the Module 2 risk score. The read-only row currently just shows "On"/"Off". User plans to design the Admin UI for a real 3-level doomscroll sensitivity in a later session — **when that's built, use "Moderate" for the middle label, not "Medium"** (matches `ChallengeSensitivity.displayLabel()`'s existing MEDIUM→"Moderate" convention already used for Motion Lock).
- **Nav Bar icon/label size restored closer to pre-port sizing** — the teammate's ported pill-highlight bar had shrunk icons to 17dp (default/original was Material's standard 24dp via the old `NavigationBarItem`); bumped back to 24dp with a larger pill (56×40dp) and label text 12sp→13sp, keeping the pill-highlight visual style.

**Third follow-up round, 2026-08-25 (same day)** — `gradlew assembleDebug` passes, installed on the vivo V2109:
- **Home: "App Usage Today" duration now shows "1h 30m" format** instead of raw minutes (reused `AppUsageRow`'s existing `formatMinutes` helper).
- **Reports: added a real "Category Breakdown" card** — genuinely missing from the earlier "full teammate UI sync," found by re-diffing the teammate's `ReportsScreen.kt` more carefully. Real data, not their static mock: `ReportsViewModel.buildCategoryBreakdown()` buckets the same 7-day `packageTotals` map (already computed for `topApps`) by each app's Admin-curated category (`AppCategoryDao.getAll()`, joined by package name), falling back to "Other" for anything Admin hasn't categorized yet — honest, not fabricated. Empty state prompts to categorize apps in Admin.
- **Reports: "Today's Usage Predictions" renamed to "AI Insights" with the teammate's "AI-POWERED" badge treatment, and repositioned** to sit right after Category Breakdown — matching the teammate's actual layout order (chart → category → AI insights) rather than its previous ad-hoc spot after Top Apps. This badge is honestly earned here (unlike the teammate's original, which slapped "AI-POWERED" on entirely static mock data) — this genuinely is Module 2 Logistic Regression II output. The separate rule-based "Insights" card (no AI claim, by design) keeps its own honest, unbadged label and stays at the bottom, after Top Apps (which has no teammate equivalent).

**Fourth follow-up round, 2026-08-25 (same day)** — full audit of remaining teammate UI gaps (via a background research pass), then built the two highest-value ones. `gradlew assembleDebug` passes, installed on the vivo V2109.
- **Home: "App Usage Today" duration format fixed** — was still raw minutes despite Top Apps getting the same fix earlier; now reuses the existing `formatMinutes` helper.
- **Usage Goals: every "current usage" number is now real**, not the teammate's hardcoded mock (5.55h total, 3.23h Social Media, 1.87h Entertainment, 0.57h Gaming, 41.2h/week). New shared `categoryTotals()` helper (`data/CategoryUsageAggregator.kt`) factors out the package→category bucketing logic first built for Reports' Category Breakdown, now used by both `ReportsViewModel` and the new `UsageGoalsViewModel` real-data path. `UsageGoalsViewModel` computes today's total + today's per-category totals (for the daily-limit-comparable goal cards) and a separate real 7-day total (for the "Weekly Screen Time Goal" card, matching its own label). Goal *targets* (the slider values) still aren't persisted anywhere — no schema field exists for a per-category target — so those stay session-local UI state, same as before.
- **Home: two more teammate banners built with real data** — `PredictiveOveruseBanner` and `DoomscrollAlertBanner` (`HomeViewModel.refreshBanners()`), both previously dropped as fake (2026-08-11: "you'll hit your 7h limit by 8:22 PM", "23 min continuous session on TikTok" — neither backed by anything real). Deliberately reframed rather than faked verbatim: Predictive Overuse shows the next upcoming hour today Module 2's Logistic Regression II has actually flagged excessive (`ExcessiveUsagePredictionLog`), not an invented clock-time extrapolation the ML pipeline doesn't produce. Doomscroll alert shows the real app name + minutes-ago from the most recent `DOOMSCROLL_ALERT` row today (new `MotionInterventionLogDao.getMostRecentDoomscrollAlertAfter`), gated to the last 30 minutes so it doesn't sit stale all day. Both banners only render when a real signal exists — hidden otherwise, never an empty/fabricated state. Doomscroll banner is now tappable — new `onNavigateToMove` callback (`HomeScreen`/`NavGraph`) opens the Move hub as the "intervene" action, since no more specific per-alert intervention exists yet.
- **Break Reminder banner and Edit Profile's "Change Password" — both built real 2026-08-25 (follow-up session), closing the gap flagged above.**
  - **Break Reminder banner** (teammate's "You've been online 47 min"): the needed live-session-duration plumbing now exists. `AppMonitorService` gained a companion `getContinuousUsageStartTime()` mirroring the same in-memory continuous-usage clock the real Break Reminder notification already fires off (no IPC — same process), updated everywhere the existing private instance field is. `HomeViewModel.refreshBanners()` reads it as a point-in-time snapshot (same pattern as the Predictive Overuse/Doomscroll banners — not a live-ticking timer) and only surfaces it once `breakReminderEnabled` is on and elapsed passes a 5-minute floor (avoids flashing for trivial usage blips; not manuscript-derived, a proposed default). New `BreakReminderBanner` in `HomeScreen.kt`, same visual family as the other two banners, with a "Focus" CTA reusing `onNavigateToFocus`.
  - **Change Password**: real Firebase reauth-then-update flow. New `FirebaseAccountRepository.changePassword()` reauthenticates with the current password (`EmailAuthProvider` credential — Firebase requires a recent sign-in before a sensitive op like `updatePassword()`) before applying the new one, returning a `ChangePasswordResult` (`Success`/`WrongCurrentPassword`/`NoPasswordProvider`/`Error`) so the UI can show an accurate message instead of a generic failure. New `hasPasswordProvider()` check scopes this to accounts that actually have an email/password credential — Google-only accounts see an honest "no password to change" message instead of a broken form, not a fabricated success. `EditProfileScreen` gained a real Change Password section (current/new/confirm fields, client-side min-length/match validation before hitting Firebase, fields clear on success). `gradlew testDebugUnitTest`/`assembleDebug` both verified. **Not yet runtime-verified** — needs an actual continuous-usage session and a real password change against Firebase to exercise both paths.

**Follow-up, 2026-08-26**: `gradlew testDebugUnitTest`/`assembleDebug` both verified.
- **Break Reminder interval editable UI restored** — closes the gap flagged in the 2026-08-25 "Usage & Motion" card removal above. Rather than reintroducing the dropped slider card, tapping the existing "Break Reminders" row (its `Switch` still toggles on/off independently) now opens a preset picker (15/30/45/60/90 min, marks the current selection) that calls the already-existing `SettingsViewModel.setBreakReminderIntervalMinutes` — that setter was already wired and unused since the slider was removed, only the UI to reach it was missing. Row subtitle updated to "· tap to change" as a discoverability hint. **Not yet runtime-verified.**

**Module 8 runtime-verification pass, 2026-08-27** (vivo V2109, real device — see [[feedback_vivo_device_adb_latency]]; app reinstalled from a stale 2026-08-26-02:09 build first, since the previously-installed build predated Student/Work Mode, the data/ package split, and the MEDIUM→MODERATE rename — confirmed via `dumpsys package`/DB schema diff before proceeding):
- **Doomscroll Sensitivity (2026-08-27 entry above) — runtime-verified.** Settings' Doomscrolling Detection and Motion Lock Sensitivity rows both showed "Moderate" on-device; pulled `physilock_db` via `run-as`/`exec-out` and confirmed `motionLockSensitivity`/`doomscrollingSensitivity` both store the literal `"MODERATE"` string, not a stale `"MEDIUM"` — closes the concern flagged when that rename shipped.
- **Persistent Login (Module 0) — runtime-verified as a side effect** of the reinstall: relaunching after `adb install -r` landed straight on Home as the existing account, no re-login prompt, confirming the Firebase Auth session (separate from the just-reset local Room DB) survives an app update.
- **Doomscrolling Detection UX correction** (user feedback during this pass): the 2026-08-25 Admin-controlled/User-read-only change had gone further than intended — it left Doomscrolling Detection with no user-facing on/off control at all (`setDoomscrollingDetectionEnabled` was removed as unused), unlike Break Reminder/Overuse Alert which kept a user toggle alongside their Admin-defaulted values. Restored `SettingsViewModel.setDoomscrollingDetectionEnabled` and gave the row back a real `Switch`; the sensitivity level itself stays Admin-only exactly as before, now shown in the subtitle text instead of the trailing slot ("· Moderate sensitivity" when on). **Runtime-verified**: toggled off on-device, confirmed `doomscrollingDetectionEnabled = 0` in the pulled DB with `doomscrollingSensitivity` unchanged at `"MODERATE"`.
- **Motion Lock Sensitivity relocated** — was sitting in "Notifications & Privacy," which doesn't fit a value that's about unlock-challenge difficulty, not alerts. Moved to the "Account" section directly under "App Lock Rules" (the row it actually governs), subtitle clarified to "· set by Admin" since it's still read-only. Confirmed on-device.
- **Answer Reflection Prompts (Module 5) — runtime-verified.** Saved a real entry on-device (mood 4/5, free-text answer); pulled `reflection_entries` and confirmed the row (`dateKey='2026-08-27'`, matching mood/text), and it appeared correctly in the "Past Reflections" list.
  - **UX gap flagged by user, deferred to next session, not fixed now**: after saving, the answer text field should visibly clear/reset rather than keep showing the just-submitted text — currently ambiguous whether re-entering the screen the same day shows "already answered" state or an editable field still holding old text. If it's already cleared correctly on re-entry, no change needed; if not, the field should reset after a successful save.
- **Usage Goals target persistence (Module 6) — runtime-verified.** Adjusted the Weekly Screen Time Goal slider, backed out and re-entered the screen, value held. Confirmed `weeklyScreenTimeGoalMs` in the pulled `user_configuration` row reflects a real saved value, not a session-local default.
- **Enable Focus Mode (Module 6) — partially runtime-verified.** Ran a real session end-to-end on-device (start → End Focus Session → "End Session"); confirmed 0 min credit shown, initially flagged then traced to `EndFocusSessionSheet.kt:38` (`creditMinutes = elapsedSeconds / 300`, i.e. 1 credit-minute per 5 real minutes, floored) — correct behavior for a sub-5-minute test session, not a bug. Session persistence/end mechanics confirmed on-device; the nonzero-credit path and Move-hub redemption (`MoveViewModel.redeemFocusCredit`) were **not** exercised on-device this pass, verified by code inspection only — user chose to skip the 5+ minute wait needed to exercise it live.
- **Real bug found + fixed: Weekly Reports showed 5 weeks, not 4.** `buildWeeklyBreakdown` fetched a fixed 28 calendar days back from "today," but that window only aligns to exactly 4 Monday-start weeks when run on a Sunday — any other day (this was found on a Thursday) straddles a 5th, partial week at the far end, which rendered as an extra bar. Fixed by capping the sorted week-bucket map to `weeksAgo < 4` before building `WeekUsage` rows, dropping the leftover partial bucket regardless of what day it's run. `gradlew assembleDebug` verified, **runtime-verified on-device**: reinstalled, confirmed Reports → Weekly now shows exactly 4 weeks.

---

## ✅ Admin Governance Layer (Done — 2026-08-02)

Not one of modules 0–8 (those map to User-facing feature groups); Admin is a separate cross-cutting governance layer per [[project_physi_lock_overview]].

- [x] Manage User Accounts — list/activate/deactivate/delete (`AdminAccountsSection`, `AdminViewModel`)
- [x] Curate App Categories — per-installed-app category master list (`AdminCategoriesSection`, `AppCategory`)
- [x] Configure Default Settings — global defaults for new accounts (`AdminDefaultsSection`, `DefaultSettings`)
- [x] View Usage Analytics — aggregate stat cards + top apps (`AdminAnalyticsSection`)
- [x] Export Research Data — plain-text share-sheet export
- [x] Authenticate Users — resolved as architectural framing only (Admin owns/governs the auth subsystem; no separate approval-gate code needed, see [[project_physi_lock_overview]])

**Known gap (2026-08-09), closed 2026-08-25: real error-state handling.** `AdminViewModel` previously swallowed failures silently — fixed without changing any of the safe-fallback *values* the UI already showed (0/emptyList), only adding a real signal that a failure happened:
- `accountsFlow()` no longer calls `close(error)` on a Firestore listener error (which killed the listener for good, silently going stale forever) — it now sets a new `accountsError` StateFlow and keeps listening; `AdminAccountsSection` shows a dismissible-by-retry banner over the last-known (possibly stale) account list instead of blanking the screen, and a new `retryAccounts()` (re-subscribes via a `flatMapLatest` retry trigger) backs a real "tap to retry" button.
- `loadAnalytics()`'s two already-existing per-query try/catches now also set a new `analyticsError` StateFlow (still falling back to the same `0`/`emptyList()` display values) — `AdminAnalyticsSection` shows the same banner style, wired to the existing "Refresh Analytics" button as its retry action.
- `setAccountActive`/`deleteAccount`/`setCategory`/`updateDefaults` (Admin's other 4 mutating actions) were fire-and-forget with no try/catch at all — each now catches failures into a shared `actionError` StateFlow, shown as a dismissible banner at the top of `AdminHomeScreen` (not a retry, since re-driving a specific past mutation would need re-capturing its original arguments; the user just retaps the action).
- New `AdminOfflineBanner`/real `isOnline` StateFlow (`NetworkConnectivityObserver.kt`, a live `ConnectivityManager.NetworkCallback`, new `ACCESS_NETWORK_STATE` manifest permission — normal, no runtime prompt) — shown at the top of `AdminHomeScreen` whenever the device has no internet-capable network, addressing "no offline fallback at all." This is an honest *degraded-mode signal*, not the User-side `HybridAccountRepository` pattern of a local cache Admin screens can still read from offline — Admin's accounts/categories/defaults/analytics remain 100% live-Firestore/Room-dependent when online; going offline now visibly says so instead of silently going stale with no indication.
New shared `AdminErrorBanner`/`AdminOfflineBanner` composables (`AdminBanners.kt`) keep all of the above visually consistent. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs an actual Firestore failure (e.g. airplane mode) to exercise the retry/offline paths on-device.

**Real crash fixed 2026-08-28: Admin login crashed the app with a NullPointerException.** `AdminViewModel`'s `init` block called `loadAnalytics()`, which touches `_analytics`/`_analyticsError` — but those two `StateFlow`s were declared textually *below* the `init` block. Since `viewModelScope.launch` runs on `Dispatchers.Main.immediate`, which executes synchronously (no re-dispatch) when already on the main thread — true during ViewModel construction — `loadAnalytics()`'s body ran immediately as part of the constructor, hitting `_analyticsError.value = null` before that property had been assigned. Fixed by moving both `StateFlow` declarations above `init {}`. Runtime-verified on the vivo V2109: Admin login no longer crashes.

**Auto-categorize apps added 2026-08-28** — Admin's "Curate App Categories" now has a one-tap "Auto-categorize uncategorized apps" button (`AdminCategoriesSection`/`AdminViewModel.autoCategorizeUncategorized`), reading Android's real `ApplicationInfo.category` (developer-declared, or Play Store-inferred) for any app Admin hasn't touched yet. Only fills in a confident mapping — `CATEGORY_GAME`→Games, `CATEGORY_SOCIAL`→Social Media, `CATEGORY_VIDEO`/`CATEGORY_AUDIO`→Entertainment — everything else (including `CATEGORY_PRODUCTIVITY`, deliberately excluded after on-device verification showed it sweeping in unrelated apps like banking/telecom/video-call/browsers) is left uncategorized for manual review. Every auto-filled app stays editable via the existing dropdown. Runtime-verified on the vivo V2109 (raw category codes confirmed via a temporary on-device diagnostic file, since this device throttles `adb logcat` for third-party app `Log.d`/`Log.e` calls — see [[feedback_vivo_device_adb_latency]] for the adb-tap caveat, this is a separate logging-specific restriction worth remembering too): TikTok correctly landed as Entertainment (real `CATEGORY_VIDEO`, matching its actual Play Store listing category, not Social despite being commonly thought of as "social media"), Instagram correctly as Social Media (`CATEGORY_SOCIAL`).

**Sign-out added to Admin Dashboard 2026-08-29** — Admin previously had no way to log out at all (a real gap, not a stale note). `AdminHomeScreen` gained a top-right icon button (`Icons.AutoMirrored.Filled.Logout`) next to the title; `MainActivity` wires it to the same `authViewModel.logout(context)` → reset `currentAccount` → `AppStage.AUTH_LOGIN` sequence the User-side Settings logout already uses. No schema change, no data wipe on install. Not yet runtime-verified (needs an actual Admin account to check).

**Auto-categorize button regression fixed 2026-09-10** — user reported the button described above as missing on-device. Root cause: the very next commit after it was added/verified (`1f07d0a`, "Merge teammate's Admin Console work") replaced `AdminCategoriesSection.kt` wholesale with a teammate's parallel rebuild of the same screen (the one that added the stat chips/search bar/filter chips/manual-add-app UI currently in place) — that rebuild didn't include the button, silently dropping it. `AdminViewModel.autoCategorizeUncategorized()` itself was untouched and still worked the whole time; only the UI trigger was gone. Re-added as a new top-of-section `Row` (full-width bordered pill, `AutoAwesome` icon + "Auto-categorize uncategorized apps", `DeepOlive`) matching the existing "Refresh Analytics" convention in `AdminAnalyticsSection.kt` — placed above the stat-chip row, without touching the search bar/filter chips/list/Add-App-Manually row already there. New `onAutoCategorize` param threaded through `AdminHomeScreen.kt`'s call site to the pre-existing `viewModel::autoCategorizeUncategorized`. `gradlew compileDebugKotlin` verified clean. **Not yet runtime-verified** — needs a real device pass to confirm the button appears and still behaves as originally verified (fills Games/Social Media/Entertainment only, leaves the rest for manual review).

---

## 🟢 Module 0: Foundation

- [x] Database schema, DAOs, Room singleton
- [x] Permissions declared in manifest
- [x] AccessibilityService (`AppMonitorService`)
- [x] Navigation shell (`NavGraph`, `BottomNavBar`)
- [x] Register Account / Log In (`HybridAccountRepository` wrapping `FirebaseAccountRepository` — Firebase Auth + Firestore `users/{uid}` as primary, replacing the old local Room simulation)
- [x] Grant Permissions — YPT-style "Continue X/N" flow in `OnboardingScreen.kt`, wired into `MainActivity`'s navigation (2026-08-02): regular Users land there right after login/register, Admins skip straight to `ADMIN_HOME`
- [x] Manage Account Settings (username/fullName/occupation editable in `SettingsScreen`; email intentionally read-only — see caveat below)
- [x] Log Out — button already existed in `SettingsScreen`'s `AccountSummaryCard`; fixed 2026-08-10 to actually call `AccountRepository.logout()` (→ `auth.signOut()`) instead of only resetting local UI state. See runtime-verification entry below.
- [x] Firebase Auth migration (2026-08-07) — `Account`/`AccountDao` Room entity retired entirely; `FirebaseAccountRepository` (`data/auth/`) handles register/login/updateAccount against Firebase Auth + Firestore. **Correction (2026-08-09): a real Firebase project does exist** — `app/google-services.json` is present (project `physi-lock`, placed 2026-08-07) and processes cleanly (`default_web_client_id` resource confirmed generated in `app/build/generated/res/processDebugGoogleServices/`, including a web OAuth client for Google Sign-In). The earlier "no Firebase project exists yet" note was stale. **Still unverified: actual runtime behavior against that project.** Nobody has run the app against it yet, so unconfirmed: whether the Email/Password provider is enabled in the Firebase console, whether Firestore is created with usable security rules (default rules deny all reads/writes), and whether the demo Admin account was ever manually created (Auth user + Firestore doc with `role: ADMIN`) as this section previously instructed.
- [x] Google Sign-In (OAuth 2.0/OIDC) — Credential Manager + `GoogleIdTokenCredential` flow wired end-to-end in `AuthViewModel.signInWithGoogle`/`MainActivity` (2026-08-07). Same runtime-unverified status as above, plus one Google-Sign-In-specific risk: it needs the app's SHA-1 certificate fingerprint registered against the `physi-lock` Firebase project, or it fails at runtime with a DEVELOPER_ERROR even though the code/build config is otherwise correct — unconfirmed whether that's been done.
- [x] Offline login fallback (2026-08-09) — `HybridAccountRepository` (`data/auth/`) wraps `FirebaseAccountRepository`; every successful online login/register writes a salted-hash snapshot (`PasswordHasher`, PBKDF2WithHmacSHA256) to a new local Room cache (`CachedAccount`/`CachedAccountDao`, DB version 5→6). If a login call throws (Firebase/Firestore unreachable), it falls back to verifying against that cache. Scope is login-only — registration and profile updates still require Firebase; Google Sign-In is unaffected (still online-only). `AuthViewModel` now extends `AndroidViewModel` to reach the DB. **Verified:** `gradlew compileDebugKotlin` passes. **Not yet verified:** no runtime test of the actual offline path (e.g., killing network mid-session in an emulator).
- [x] **Runtime-verified 2026-08-09 (partial):** user ran the app and confirmed Google Sign-In works end-to-end — account visible in both Firebase Auth console and Firestore `users` collection. Email/password registration errored, root-caused and fixed same day: `FirebaseAccountRepository.register()` was querying Firestore (username uniqueness check) *before* `auth.createUserWithEmailAndPassword`, i.e. while still unauthenticated — fails under any Firestore rules that scope reads to signed-in users. Fixed by reordering: Auth now runs first, Firestore check second, with a rollback (`authResult.user?.delete()`) if the username turns out taken. Added `firestore.rules` at repo root (not auto-deployed — must be pasted into the Firebase Console manually, no CLI configured in this repo) with the corresponding access model: any signed-in user can read/query `users`, but can only write their own doc (Admin gets an update/delete bypass for account management). **Gap closed same day:** added a public `usernames/{username} → { email }` lookup collection (`firestore.rules`) so login-by-username keeps working under the auth-scoped rules. Deliberately minimal — the doc holds only `email`, and rules only permit exact-ID `get` (never `list`/enumeration), so it can't be used to browse all usernames, only resolve one you already know. `resolveEmail()` now does a `usernamesCollection.document(identifier).get()` (public, pre-auth-safe) instead of the old `whereEqualTo` query (which required auth and would've failed pre-login). Write-side: `register()`, `updateAccount()` (on username rename — deletes the old mapping, writes the new one), and `signInWithGoogleIdToken()`'s first-time provisioning all keep this collection in sync, best-effort (a failed lookup write doesn't fail the whole registration/update — it only degrades that account's future username-login, email login is unaffected). Rules scope create/update/delete to `request.auth.token.email` matching the mapping's own email, so you can't overwrite or delete someone else's username mapping. **Not yet runtime-verified** — same as the rest of this Firebase work, needs an actual login-by-username test once the updated rules are pasted into the console.
- [x] **Runtime-verified 2026-08-10:** user pasted the updated rules and confirmed login-by-username works. Also confirmed the offline-fallback design is behaving exactly as intended: registering a *new* account with no internet correctly fails (expected — registration has no offline path, needs Firebase to reserve the username/email); registering that same account with internet then worked; logging into that freshly-registered account with no internet then succeeded (the `HybridAccountRepository` cache written at registration time backing the offline login as designed). **Log Out fix same day** — see Foundation checklist above; not yet runtime-verified (need to confirm `auth.currentUser` is actually null after tapping it, e.g. via Logcat or by checking the Firebase Auth console's "last sign-in" doesn't show the app still active).

## ✅ Module 1: Core Monitoring & Usage Awareness (Done — 2026-08-26)

- [x] View Usage Tracker (`HomeScreen`, `HomeViewModel`)
- [x] View Screen Time Breakdown (`ReportsScreen`, `ReportsViewModel`)
- [x] Foreground app tracking, usage aggregation, scroll-event field (counting logic itself deferred to Module 2)
- [x] View Usage Patterns (2026-08-26) — real recurring-behavior observations, distinct from `insights` above (which stays single-day rule-based comparisons — "today vs your 7-day average"). New `ReportsViewModel.usagePatterns`/`UsagePatterns` data class, rendered in a new "Usage Patterns" card in `ReportsScreen` after Insights: **peak usage hour** (new `AppUsageLogDao.getDurationByHourOfDay` query, groups real logged sessions by hour-of-day over the last 7 days and picks the max — reuses `AppUsageLog` rather than `UsageStatsManager` since only the former has per-session start-time granularity; this undercounts absolute totals the same way `AppUsageLog` always has, but that undercounting is roughly uniform across hours so the *relative* comparison stays meaningful) and **weekday vs weekend average** (reuses the same 7-day `DayUsage` list `insights` already computes, now carrying a real `isWeekend` flag). Either half renders nothing if there wasn't enough real data to say something honest (e.g. no weekend day yet in the current window) rather than a fabricated placeholder. `gradlew testDebugUnitTest`/`assembleDebug` both verified. **Not yet runtime-verified** — needs several real days of logged sessions spanning both a weekday and a weekend to exercise both halves.
- [x] Receive Overuse Alerts (2026-08-21) — shared deliverable with Module 4, see that section below for detail
- [x] Real Weekly Reports view (2026-08-27) — replaces the static "Weekly view coming soon" placeholder (`MonthPlaceholder`) with a real 4-week bar chart + stat cards. `ReportsViewModel`'s existing day-by-day `UsageStatsRepository.getUsageForRange` fetch (already 7 single-day calls, not one multi-day range call — see the existing kdoc on why `UsageStatsManager`'s multi-day aggregation isn't trusted) extended to 28 days; the daily view's Top Apps/Category Breakdown/Insights/Usage Patterns stay scoped to the original 7 (packageTotals only folds in the last 7 days), keeping the Weekly view chart-and-stats-only rather than duplicating every daily-scoped card per week. Weeks bucketed Monday-start via `ChronoUnit.WEEKS`, newest labeled "This Week"/"Last Week", older ones a date. 4 weeks (28 days) was a deliberate scope tradeoff bounding the number of `UsageStatsManager` calls per screen load, documented the same way the ML model-size tradeoff is in `ml/README.md`. Also renamed the private `ReportPeriod` enum `WEEK`/`MONTH` → `DAILY`/`WEEKLY` while touching this file — the old names had drifted out of sync with their own tab labels. `gradlew assembleDebug` verified. **Not yet runtime-verified.**

**Bug found + fixed (2026-08-10): displayed screen time didn't match real device usage.** Root cause: `HomeViewModel`'s "today's screen time" and `ReportsViewModel`'s weekly chart/top-apps were entirely sourced from `AppUsageLog` — rows `AppMonitorService` (the AccessibilityService) only writes when it observes a foreground-app *switch* while actively running. That has no way to reflect usage from before the service started watching, so it structurally couldn't match the phone's real total; it'd read low or zero. The fix: `UsageStatsRepository` (already built, already gated by the "Usage Access" permission already requested in onboarding, but never actually called for this) wraps Android's real `UsageStatsManager` — the same source Digital Wellbeing/Settings' screen-time page reads. `HomeViewModel` now sums `getTodayUsage()` on every `ON_RESUME`. `ReportsViewModel` now queries one day at a time via the new `getUsageForRange(startMillis, endMillis)` (folds 7 single-day queries into the weekly chart + a client-side top-apps aggregate) rather than a single 7-day range query, since `UsageStatsManager`'s multi-day bucket-aggregation behavior isn't reliable enough to trust — see kdoc on that method. `AppUsageLog`/`AppMonitorService` are untouched and still needed for real-time, event-driven behavior (lock-triggering, per-session data, future scroll-event/doomscroll counting) — this fix only changes where the *displayed totals* come from. Also found and left alone: `UsageSync.kt`'s `syncTodayUsage()` was a half-built earlier attempt at this exact fix — dead code (never called), and even if wired up it writes to the legacy `UsageSession` table, not the one either ViewModel reads. Verified with `gradlew compileDebugKotlin`; not yet runtime-tested against real device usage.

## ✅ Module 3: Motion-Responsive Locking (Done — 2026-08-24)

- [x] Perform Move-to-Unlock (`MoveScreen`, `LockScreen` — via `RotationalArmDetector`/`StepChallengeDetector`, see Module 4's Activity Challenges entry; `ShakeDetector` was replaced, not extended)
- [x] Customize Lock Rules — per-app lock on/off (`AppLockRulesScreen`)
- [x] Motion Lock Sensitivity setting wired to the 3 challenge types (`SettingsScreen`, `ChallengeSensitivity`)
- [x] Trigger Adaptive Lock (2026-08-24) — `lockType` was always hardcoded to `"CUSTOM"` on write and never read anywhere (dead schema field, not actually a "not built" gap so much as a "never wired" one). Now real: `AppLockRulesScreen` gained a per-app "Adaptive difficulty" toggle (shown only when that app is locked), backed by `AppLockViewModel.setAdaptive`/`adaptivePackages`. `LockScreen.kt` checks the triggered app's `AppLockRule.lockType` — if `"ADAPTIVE"`, computes the challenge sensitivity from the real Module 2 risk score (`ChallengeSensitivity.fromRiskLevel`: LOW risk → LOW sensitivity, MODERATE → MEDIUM, HIGH → HIGH) instead of the static Motion Lock Sensitivity setting, falling back to that static setting if risk scoring throws for any reason. Shows "Difficulty adapted to your current risk level" on the lock screen when active. Deliberately scoped to the actual lock-trigger path only — `MoveViewModel`'s voluntary challenge hub still uses the static setting uniformly, since adaptive-per-specific-app doesn't map cleanly onto "pick which locked app to unlock voluntarily." `gradlew assembleDebug` verified. **Not yet runtime-verified.**

## ✅ Module 4: Smart Intervention System (Done — 2026-08-22)

- [x] Set Usage Limits — daily screen-time threshold slider (`SettingsScreen`, `AdminDefaultsSection`); not the goal-based/YPT-style limits from [[project_inspiration_apps]]
- [x] Complete Activity Challenges (2026-08-22) — replaced the old shake-to-unlock mechanic entirely with 3 real motion challenges: Rotational Arm Movements (+15 XP, new `RotationalArmDetector` using accelerometer+gyroscope fusion — first gyroscope use in this codebase), 5-Minute Walk (+20 XP), and Run/Jog (+40 XP, both via new `StepChallengeDetector` on `Sensor.TYPE_STEP_DETECTOR`, requires `ACTIVITY_RECOGNITION` permission requested lazily on first use). Available both at the actual lock-trigger moment (`LockActivity`/`LockScreen`, one random challenge per lock event) and in a voluntary hub (repurposed `MoveScreen`/`MoveViewModel` — previously an empty stub) gated to only show challenges when at least one app is currently locked, with a picker to choose which locked app to unlock. Built a genuine per-app timed unlock for the first time (`AppMonitorService.grantTemporaryUnlock`, in-memory packageName→expiry map) — the old mechanism was only ever an 8-second blanket re-lock-loop suppression, not a real "unlock for 20 min" reward. `MotionInterventionLog` (previously write-never dead code masking the streak display) now actually gets rows inserted from both completion paths, extended with `challengeType`/`xpEarned` columns; XP total is now real (`getTotalXp()` query). **Runtime-verified 2026-08-22** on the vivo V2109 device: Rotational Arm completed successfully via both the lock-trigger and voluntary-hub paths (confirmed in `motion_intervention_logs`), per-app unlock persistence confirmed (reopening the just-unlocked app doesn't re-trigger the lock screen), Run/Jog's live cadence tracking confirmed working against the device's real `STEP_DETECTOR` sensor (a UX gap — no visible feedback on cadence vs. target — was found and fixed mid-testing). **Not separately stress-tested**: Walk's full step-count completion, unlock expiry after the full 20-minute window, and the hub's empty-state gating with zero apps locked — all share code paths already proven correct via the Rotational Arm/Jog tests above, but weren't independently exercised end-to-end.
- [x] Receive Break Reminders (2026-08-21) — user-configurable continuous-foreground-usage timer (`UserConfiguration.breakReminderEnabled`/`breakReminderIntervalMs`, default on/30 min; toggle+slider in `SettingsScreen`), tracked in `AppMonitorService` (accumulates across accessibility events, resets on a >2 min idle gap — a proposed default, not manuscript-specified), posts a repeating Android notification via a new `break_reminder_channel` (`IMPORTANCE_HIGH`, heads-up banner). Deliberately independent of doomscroll pattern-detection (separate, still-unbuilt Module 2 classifier) — modeled on TikTok's own plain continuous-session break reminder instead. **Runtime-verified 2026-08-21** on a real device (vivo V2109, Android 13) with a temporary 1-min test interval — notification fired correctly with real (touch-driven) app switching, including the `IMPORTANCE_HIGH` heads-up banner (confirmed by manually bumping the already-created channel to "Urgent" in system Settings, since Android won't let an app change an existing channel's importance in code once created — a fresh install gets `IMPORTANCE_HIGH` automatically without this manual step). Two device-level gotchas found along the way, worth remembering for testing any other background-service feature on this same device: (1) no local emulator/AVD is configured on this machine — testing used the physical device over wireless ADB instead; (2) this vivo (Funtouch OS) build blocks `adb logcat` entirely, even for a manually shell-injected test log, so log-based debugging doesn't work here — dumpsys (`dumpsys notification`, `dumpsys accessibility`) is the only viable adb-based introspection; (3) vivo's per-app "Smart control" battery/autostart mode silently suppressed the background accessibility service's notifications even with the standard Android notification permission granted — had to be switched to explicit "No restrictions" in vivo's battery settings before anything fired. Also confirmed `adb shell am start`-simulated app switches do **not** reliably trigger the same accessibility events as real touch navigation on this device (the pre-existing App Lock feature also failed to trigger under simulated switches) — real on-device interaction is required to test any accessibility-event-driven feature here, adb alone isn't sufficient.
- [x] Receive Overuse Alerts (2026-08-21) — passive, at-most-once-per-day notification when today's real aggregate screen time (via `UsageStatsRepository`, same OS-backed source `HomeViewModel`/`ReportsViewModel` already use) crosses the existing `UserConfiguration.dailyScreenTimeThresholdMs` limit (`overuseAlertsEnabled` toggle in `SettingsScreen`, default on). Scoped to the aggregate limit only, not per-app (`AppLockRule.dailyLimitMs` stays unused dead schema — building real per-app limits would be a separate, bigger feature). Checked from `AppMonitorService` on app-switch events, throttled to at most once/60s (`UsageStatsManager` queries aren't free) with an in-memory once-per-day dedupe key. Separate notification channel (`overuse_alert_channel`, `IMPORTANCE_HIGH`) from Break Reminder's. **Runtime-verified 2026-08-21** on the same vivo V2109 device — notification fired correctly with real usage once the daily limit was temporarily lowered, confirmed no repeat on subsequent app switches the same day, heads-up banner confirmed working.

## 🟢 Module 2: AI-Based Behavior Analysis (All 3 models done 2026-08-23/24, UI consumers wired)

- [x] Random Forest risk scoring (2026-08-23) — real, trained, wired end-to-end. `ml/` holds the offline pipeline: `generate_dataset.py` (synthetic training data — no ground-truth risk labels exist anywhere, real or synthetic, so this bootstraps one via a weighted heuristic over the 6 manuscript features, tertile-balanced Low/Moderate/High labels), `train_risk_model.py` (`RandomForestClassifier`, 15 trees/max_depth 5 — see below), `README.md` (full pipeline explanation, why the training set is synthetic, why the transpiled output is Java not Kotlin, the model-size constraint, retraining steps). On the app side: `RiskModel.java` (m2cgen-transpiled, zero ML runtime dependency), `RiskScoringEngine.kt` (wraps it, returns a `RiskLevel` + continuous 0.0–1.0 score), `RiskFeatureExtractor.kt` (pulls today's real feature vector from Room). Wired into `HomeViewModel`/`HomeScreen` — replaced the `UserConfiguration.riskSensitivity` placeholder entirely (deleted from `UserConfiguration.kt`); the dashboard's risk card ("RISK LEVEL"/"Behavioral risk score", was mislabeled "LOCK SENSITIVITY"/"Motion challenge difficulty" — a leftover from when it literally displayed the motion-lock-sensitivity setting) now shows the real computed level and score, recomputed every Home resume. `gradlew assembleDebug` verified (not just `compileDebugKotlin` — needed to confirm the generated Java actually compiles/dexes, see below). **Not yet runtime-verified against real accumulated usage data** (needs a device with several real logged sessions/bypass attempts to see non-default output).
  - **Real feature plumbing added same day**, since 2 of the 6 features had no data source at all before this: **bypass attempts** — `LockActivity.onStop()` now logs a `MotionInterventionLog(userResponse = "DISMISSED")` row once per challenge shown-but-not-completed (covers back button, home button, recents swipe-away, screen-off — all route through `onStop()`); new `MotionInterventionLogDao.countBypassAttemptsAfter`/`countBypassAttemptsByPackageAfter` queries. **Scroll data** — `AppMonitorService.logScrollEvent` (previously an empty stub) now actually counts `TYPE_VIEW_SCROLLED` events per session and tracks the longest gap between them; `AppUsageLog` gained a `maxScrollGapMs` column (DB version 9→10, `fallbackToDestructiveMigration` — dev/test only, wipes local data on upgrade) alongside the already-existing-but-previously-always-0 `scrollEventCount`. New `AppUsageLogDao.getSessionCountByDate`/`getDurationByCategoryAndDate` queries support the launch-frequency and social-media-fraction features.
  - **Model-size constraint discovered and worked around**: m2cgen inlines an entire Random Forest as nested if/else in ONE Java method (no per-tree helper methods), and the JVM caps a single method at 64KB bytecode. A first attempt at 150 trees/depth 8 generated ~112,000 lines and would have failed to compile. Cut to 15 trees/max_depth 5 (~1,900 lines, compiles and dexes cleanly) — costs some accuracy (66.8% vs. an untested higher number on the bigger forest) but was judged an acceptable bootstrap-model tradeoff. Documented in `ml/README.md` for whoever retrains this.
- [x] Logistic Regression I — doomscroll detection (2026-08-23, same day as the Random Forest). Same synthetic-bootstrap approach: `generate_doomscroll_dataset.py`/`train_doomscroll_model.py` → `DoomscrollModel.java` (m2cgen). **Confirmed design respected**: the 3 raw inputs are scroll speed, pause pattern (`maxScrollGapMs`), and time of day — the Random Forest's risk level is explicitly NOT a 4th input; `DoomscrollDetector.kt` instead uses it to pick a detection threshold "recipe" (LOW risk → 0.75 threshold, MODERATE → 0.60, HIGH → 0.45), applied to the model's sigmoid-of-logit output. 80.5% test accuracy, 0.88 AUC-ROC. Wired into `AppMonitorService`: `checkDoomscrolling()` runs off the *live in-progress session's* in-memory scroll state (not a Room query — needs to catch doomscrolling while it's happening), throttled 15s between checks and a 10-min cooldown per alert; a new `doomscroll_alert_channel` notification fires and a `MotionInterventionLog(triggerType = "DOOMSCROLL_ALERT")` row gets logged on detection. Risk level is cached and refreshed every 5 minutes in a new background loop (`AppMonitorService.startRiskRefreshLoop`) — the first real implementation of the "every 5 minutes" cadence `PROJECT_DOCUMENTATION.md`'s pipeline diagram already described but nothing had actually built yet. **This closes yesterday's `doomscrollEpisodeCount` gap**: `RiskFeatureExtractor` now counts today's real `DOOMSCROLL_ALERT` rows instead of hardcoding 0 — all 6 Random Forest features are real as of today. `gradlew assembleDebug` verified. **Not yet runtime-verified.**
  - m2cgen's Java export differs by model type, worth knowing before retraining either: Random Forest → `double[]` of class probabilities; Logistic Regression → a single raw logit, sigmoid applied on the Kotlin side (`DoomscrollDetector.kt`). Discovered by inspecting the actual generated file rather than assuming.
  - **Known simplification**: `hourOfDay` is a single raw 0–23 feature, not cyclically encoded — a linear model can't represent midnight-wraparound (11pm/1am being "close"), so late-evening hours are somewhat under-flagged relative to early-morning ones. Documented in `ml/README.md`; fine for a synthetic bootstrap model, worth revisiting with real data.
  - **Trained unscaled** (no `StandardScaler`), unlike the reference notebook's LR models — avoids replicating scaler arithmetic by hand on the Kotlin side for only 3 features. See `ml/README.md`.
  - **Real Doomscrolling Sensitivity (LOW/MODERATE/HIGH) built 2026-08-27**, closing the gap flagged since the 2026-08-25 Admin-read-only reclassification (there was only ever an on/off flag before this). New `UserConfiguration.doomscrollingSensitivity`/`DefaultSettings.doomscrollingSensitivity` (DB version 18→19), default `"MODERATE"`. Confirmed design (project memory "module2-doomscroll-design", Option A): a bias layered on top of the existing risk-level threshold recipe above, not a replacement for it — `DoomscrollDetector.detect()` gained a `sensitivity` parameter (defaults to `"MODERATE"` so every pre-existing call site, including the existing test file, keeps compiling and passing unchanged) that shifts the selected threshold by `SENSITIVITY_BIAS` (LOW +0.10 → fewer alerts, MODERATE 0.0 → today's exact tested behavior, HIGH −0.10 → more alerts), clamped to `[0.30, 0.95]`. `AppMonitorService` mirrors the new config field and passes it into `checkDoomscrolling()`'s detect call. Same Admin-controlled/User-read-only treatment as Motion Lock Sensitivity: new "Default Doomscrolling Sensitivity" segmented-button card in `AdminDefaultsSection`/`AdminViewModel.setDefaultDoomscrollingSensitivity`; `SettingsScreen`'s Doomscrolling Detection row now shows the real sensitivity label (via `ChallengeSensitivity.displayLabel`) instead of just "On," with "Off" unchanged. Two new `DoomscrollDetectorTest` cases verify MODERATE is a true no-op against the pre-existing assertions and that flags are monotonic across sensitivity levels (LOW ⊆ MODERATE ⊆ HIGH), same wrapper-logic-not-model-accuracy testing pattern as the rest of Module 8's ML tests. **Same-day follow-up: "MEDIUM" retired everywhere, not just display-renamed.** The earlier convention (kept the stored value `"MEDIUM"`, only title-cased to "Moderate" for display) was explicitly overridden by the user — every stored/compared sensitivity value is now literally `"MODERATE"`, including the `ChallengeSensitivity` enum constant itself (`ChallengeSensitivity.MEDIUM` → `ChallengeSensitivity.MODERATE`, rippling through `RotationalArmDetector`/`StepChallengeDetector`/`MoveViewModel`'s default params, `AdminDefaultsSection`'s `sensitivityLevels` list, and both sensitivity fields' defaults in `UserConfiguration`/`DefaultSettings`). `ChallengeSensitivity.displayLabel()` simplified as a result — the old `"MEDIUM" -> "Moderate"` special case is gone since title-casing `"MODERATE"` already produces "Moderate" with no special-casing needed. This is a global rename, so it applies to Motion Lock Sensitivity too, not just the new Doomscroll Sensitivity field — a `"MEDIUM"` string anywhere in this codebase (code or persisted data) is now stale. `gradlew compileDebugKotlin`/`assembleDebug`/`testDebugUnitTest` all verified after the rename. **Not yet runtime-verified** — needs a device to confirm the Admin picker actually changes on-device alert frequency, and that no stale `"MEDIUM"` value survives from a pre-rename install (this repo's `fallbackToDestructiveMigration()` wipes local data on any DB version bump, so a fresh install is unaffected either way).
  - **Open discrepancy, flagged not resolved**: the manuscript's `DOOMSCROLLING_DETECTION` Data Dictionary table implies different inputs (`scrolling_duration_minutes`, `scroll_pause_frequency` as a count, no time-of-day column) than what got built. May come from a different manuscript section than whatever confirmed the "3 raw inputs" design originally — not necessarily wrong, just unreconciled. User said revisit later. See `ml/README.md` and project memory "module2-doomscroll-design".
- [x] Logistic Regression II — hourly excessive-usage prediction (2026-08-23/24). Manuscript's `EXCESSIVE_USAGE_PREDICTION` Data Dictionary table only specifies the output log schema (hour/predicted_usage_minutes/excessive_probability/is_excessive) — no input features enumerated, so a 4-feature set was proposed and confirmed instead of transcribed: `avgUsageThisHourMin` (30-day per-hour baseline), `cumulativeUsageTodayMin`, `usagePrevHourMin`, `isWeekend`. Label is real, not invented: whether the hour's usage crosses the already-existing `UserConfiguration.hourlyExcessiveUsageThresholdMs`. 86.8% test accuracy, 0.95 AUC-ROC. `generate_excessive_usage_dataset.py`/`train_excessive_usage_model.py` → `ExcessiveUsageModel.java` → `ExcessiveUsageDetector.kt` (plain 0.5 threshold — no risk-level "recipe" here, that pattern is LR-I-specific). New `ExcessiveUsagePredictionLog` Room entity mirrors the manuscript's output table almost field-for-field (DB version 10→11); `AppMonitorService.checkExcessiveUsagePrediction()` runs every 15 min, logs one row per hour, and reuses the existing **Overuse Alerts** Settings toggle for its notification rather than adding a new dedicated toggle (still fundamentally an overuse alert, just predictive). New `AppUsageLogDao.getAvgDurationForHourOfDay`/`getDurationInRange` queries. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs a device with enough real logged history for `avgUsageThisHourMin` to be non-zero.
- [x] m2cgen transpilation — to Java (not literally Kotlin; interops seamlessly, see `ml/README.md` for why)
- [x] View Risk Score — real as of 2026-08-23, see above (was: UI showed the now-deleted `UserConfiguration.riskSensitivity` placeholder)
- [x] Receive Overuse Predictions (2026-08-24) — new "Today's Usage Predictions" card in `ReportsScreen`/`ReportsViewModel`, reading `ExcessiveUsagePredictionLog.getPredictionsByDate` live (it's a Flow, so new hourly predictions from `AppMonitorService` appear without a manual refresh). Explicitly labeled "AI" — unlike the existing "Insights" card in the same screen (deliberately rule-based, no ML claim), this one genuinely is ML output, so the badge is honest rather than the fabricated "AI-POWERED" badge rejected during the 2026-08-11 Home/Reports redesign. Each row shows the hour, `predictedUsageMinutes`, and an Excessive/Normal badge. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs the app running long enough for `AppMonitorService`'s 15-minute prediction loop to actually produce rows.

## ✅ Module 6: Personalization & User Control (Done — 2026-08-25)

- [x] Select Usage Profile — Student/Work mode toggle (`SettingsScreen`); real per-mode enforcement built 2026-08-27, see below.
- [x] Customize Lock Rules — covered under Module 3 above (shared deliverable)
- [x] Real Student/Work Mode enforcement (2026-08-27) — closes the gap flagged since the 2026-08-25 UI sync, where both mode screens were an honest "Planned for [Mode]" placeholder with no differentiated backend. New `ScheduleBlock`/`AllowlistedApp` Room entities (DB version 17→18) back a shared `ScheduleBlockSection` UI (`StudentModeScreen.kt`/`WorkModeScreen.kt`): recurring weekly time windows, day-of-week + on-the-hour start/end (schema stores minute granularity for a future finer picker, UI doesn't expose it yet). `AppMonitorService.activeScheduleBlock()` checks the cached block list against `Calendar` day/time (same `Calendar`-based convention the file already used for doomscroll/excessive-usage hour checks) on every foreground-app-switch event, slotted into the existing lock > ??? > Focus Mode > Context Alert priority chain in `handleWindowStateChange`: **Student Mode** sends any non-allowlisted, non-system app (`isSystemPackage` via `ApplicationInfo.FLAG_SYSTEM`, a safety filter so the inverted allowlist logic can never kick the launcher/dialer/Settings to home) to the home screen during an active block — a real "Study App Allowlist" picker (`AllowlistedAppDao`) reuses `AppLockRulesScreen`'s exact row pattern. **Work Mode** reuses the *existing* `focusBlockedPackages` set (Admin-curated Social Media/Entertainment categories, already live-populated for Focus Mode) to block distracting apps during Work Hours — this is what makes "meeting-aware locking" honestly buildable without a Calendar API integration: it's schedule-triggered category blocking, not real meeting detection, and the screen copy says so explicitly rather than overclaiming. Work Mode also gets real "quiet hours": a new `isQuietHours()` check holds Break Reminder/Overuse Alert/Excessive Usage Prediction notifications during an active Work block (Doomscroll Alert, Context Alert, and the new Schedule Block notification itself are unaffected — those are active-intervention signals, not passive nudges); the once-per-day/once-per-hour dedupe keys are deliberately left unset during quiet hours rather than consumed, so the alert can still fire once the block ends instead of being silently lost for the rest of the day/hour. New shared `schedule_block_channel` notification, `NotificationLog` type `"SCHEDULE_BLOCK"`. **Per-mode default limit**: `SettingsViewModel.setUserMode` now also applies a preset `dailyScreenTimeThresholdMs` on every explicit "Switch to X Mode" tap (Student 360 min, Work 600 min, bracketing the 480 min global default) — a proposed default, not manuscript-specified, re-applied on every switch rather than a one-time value. `gradlew compileDebugKotlin`/`assembleDebug`/`testDebugUnitTest` all verified. **Not yet runtime-verified** — needs a real device to confirm the home-kick/allowlist/quiet-hours behavior against real accessibility events and real wall-clock schedule windows.
- [x] Enable Focus Mode (2026-08-25) — real session backend replacing the old locally-ticking-counter mockup. New `FocusSession` Room entity/DAO (DB version 12→13, `endTimeMillis IS NULL` marks the active row) and `FocusModeViewModel` (`ui/focus/`): entering the screen starts a session if none is active (`startSessionIfNeeded`), `elapsedSeconds` is computed from the persisted `startTimeMillis` rather than a counter, so it survives navigating away and process death, not just recomposition. "End Focus Session" now actually surfaces the previously-unwired ported `EndFocusSessionSheet` ("End Session" / "Keep Going") instead of popping back immediately; "End Session" writes `endTimeMillis`/`creditMinutesEarned`, **and (2026-08-25 follow-up) is now actually spendable**: also credits a new `UserConfiguration.focusCreditBalanceMinutes` running balance (DB version 16→17). Redeemed from the Move hub — a new "Screen Credit" card (only rendered when the balance is positive) lets the user pick a locked app and spend the entire balance at once for that many minutes of real temporary unlock, via `MoveViewModel.redeemFocusCredit` reusing the exact same `AppMonitorService.grantTemporaryUnlock` mechanism Activity Challenges' XP-earned unlocks already use — same currency-into-unlock-minutes shape, just earned by focusing instead of moving. 1:1 exchange rate (no partial-amount picker, simplest honest mapping matching the literal "earned N min of screen credit" copy). `gradlew assembleDebug` verified. **Not yet runtime-verified.** Real enforcement in `AppMonitorService`: while a session is active, any foreground app in the blocked set gets kicked to the home screen via `performGlobalAction(GLOBAL_ACTION_HOME)` (new `focus_block_channel` notification, throttled 60s/logged to `NotificationLog` as `FOCUS_BLOCK`) — real curated data, not the teammate's hardcoded Instagram/TikTok/Twitter/YouTube/Reddit chip list. App Lock's challenge-based lock (Module 3/4) takes priority over Focus Mode's plain block for any app that's both. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs a device with at least one app selected to exercise the actual block.
  - **Corrected 2026-08-28: blocked-app set is now User-owned, not Admin's app categories.** The original design (above) derived the block set from Admin-curated Social Media/Entertainment categories — user feedback this session: "Admin will only categorize, not the one who chooses what is blocked." New `FocusBlockedApp` Room entity/DAO (DB version 19→20), new `FocusBlockedAppsScreen.kt` (`ui/focus/`, visual/behavior twin of `AppLockRulesScreen`'s per-app toggle picker) reachable from Settings → "Focus Mode Blocked Apps". `FocusModeViewModel`/`AppMonitorService` both now read `FocusBlockedAppDao` instead of `AppCategoryDao`. The in-session "BLOCKED DURING FOCUS" chip list and its empty-state copy ("ask your Admin to curate categories") were also updated — no more Admin reference. Admin's category curation (see Admin Governance Layer below) still exists and still feeds Reports/Usage Goals/risk scoring; it's just no longer wired to Focus Mode blocking. **Runtime-verified 2026-08-28/29** on the vivo V2109: toggled TikTok on in the new picker (confirmed in `focus_blocked_apps` via DB pull), started a session, opened TikTok — got kicked to the home screen as expected.
  - **Real lock-screen gaps found + fixed 2026-08-29, via on-device testing:** the bottom nav bar was still visible while a Focus session was active, letting the user just tap "Home" and leave with no confirmation — the point of a Focus "lock" screen. `NavGraph.kt`'s `Scaffold` now hides `BottomNavBar` specifically on `Screen.Focus.route` (checked via `currentBackStackEntryAsState()`). Same gap existed for the system back button/gesture (no `BackHandler` on the route at all) — `FocusModeRoute` now has one that surfaces the existing "End Focus Session" confirmation sheet instead of silently popping back to Home. Together these make ending a session the only way out, matching the user's explicit ask ("user shouldn't be able to go back to the physi-lock home screen" / "when user tries to exit, end focus session should display"). `gradlew compileDebugKotlin`/`assembleDebug` verified. **Not yet independently re-verified after this specific fix** (the block-enforcement test above predates it).
  - **Persistent session-timer notification added 2026-08-29** — closes a real design gap raised by the user: Focus Mode intentionally lets the user leave to use non-blocked apps (matches Digital Wellbeing's actual model, confirmed against YPT/DW as reference apps), but the in-app countdown was the *only* place elapsed time was visible, so it was meaningless the moment they left. New `focus_session_channel` (`IMPORTANCE_LOW`, ongoing/`setOnlyAlertOnce`) in `AppMonitorService`: while a `FocusSession` is active, a ticking `collectLatest` loop (60s interval) posts/updates "Focus session running · Xm focused so far · tap to return", canceled the moment the session ends. Tapping it opens `MainActivity` generically (no special deep-link needed — since leaving Focus is now only possible via ending the session per the fix above, nothing else could have navigated the app away from the Focus screen in the meantime). Modeled on YPT's floating timer overlay's *purpose* (always-visible running time while blocking runs in the background) without building a full always-on-top overlay widget, which was judged not worth the added complexity for this project. `gradlew compileDebugKotlin`/`assembleDebug` verified. **Not yet runtime-verified.**
  - **Real bug found + fixed 2026-08-29, via on-device testing of the notification above**: tapping the persistent Focus-session notification returned to Physi-Lock's own Home screen instead of the Focus screen. Root cause not fully isolated but consistent with the background process being recreated (killed for memory while backgrounded, or similar) — the notification's `PendingIntent` only launches `MainActivity` generically, and Compose Navigation's back-stack restoration across a full recreation isn't something to lean on here. Fixed with a ground-truth check instead of trusting restored nav state: `NavGraph.kt` resolves its actual `startDestination` (Home vs. Focus) from the real `FocusSessionDao` *before* the graph ever renders, so there's no Home-then-correct flash either.
  - **Follow-up bug found + fixed same day**: the first version of the above fix used `navigate(Focus) { popUpTo(startDestination){ inclusive = true } }` to correct post-mount, which stripped Home out of the back stack entirely — user then reported ending a session left them stuck on the Focus screen, because `popBackStack()` (Focus's old `onEndFocusClick`) had nothing left underneath to pop to and silently no-op'd. Fixed by (1) resolving the start destination once, before rendering, instead of correcting via `navigate()` after the fact (see above), and (2) making "End Focus Session" always `navigate(Home) { popUpTo(Focus){inclusive=true} }` explicitly rather than relying on `popBackStack()`, so it works correctly whether Focus was pushed on top of Home normally or was the graph's actual start destination. **Runtime-verified 2026-08-29** on the vivo V2109: notification tap now lands directly on Focus with no flash, and "End Focus Session" correctly returns to Home.
  - **Blocked-app notification wording improved 2026-08-29**, per user request for a clearer "you're in a session, X is blocked" message: `postFocusBlockNotification`'s title/text changed from "$appName blocked" / "$appName is blocked while Focus Mode is active" to "Focus Mode Active" / "You're in a Focus Mode session — $appName is blocked." Same `IMPORTANCE_HIGH` channel, same 60s throttle — this was a copy change, not a new mechanism. **Runtime-verified 2026-08-29**: notification content confirmed correct in the shade. **Known gap, not fixed, user said defer**: no heads-up banner popup shown despite `IMPORTANCE_HIGH` — matches the same vivo (Funtouch/OriginOS) per-app "Smart control" battery/autostart restriction already documented for Break Reminder (see that section above) suppressing heads-up display for background-service-posted notifications; likely fixable the same way (switch this app's battery mode to "No restrictions" in vivo Settings), not a code bug. Revisit if asked.
  - **Bedtime Mode built for real (2026-09-04)**, from a teammate Figma "BedtimeModePage" the user pasted directly (real design, not self-built like Schedule Block/Whitelist Manager originally were). New `UserConfiguration.bedtimeStartMinute`/`bedtimeEndMinute` columns (DB version 20→21, defaults 11 PM–7 AM). `AppMonitorService.isWithinBedtimeWindow()` — a daily minutes-since-midnight window (not day-of-week/`userMode`-scoped like `ScheduleBlock`), with wrap-past-midnight handling since the default window crosses midnight — sends any non-allowlisted, non-system app to the home screen, reusing the *same* `AllowlistedApp`/Whitelist Manager set Class Mode already uses (the mockup's own copy said "only whitelisted apps remain accessible," and this repo already has exactly one such list). Slotted as its own priority-chain branch in `handleWindowStateChange`, checked ahead of the `userMode`-specific Class/Work Mode branches since it isn't scoped to either. New `BedtimeModeScreen.kt` (`ui/settings/`): hour-only start/wake pickers (`DropdownMenu`, matching `ScheduleBlockSection`'s existing picker convention rather than the mockup's native minute-precision time input, for consistency with the rest of the app's time pickers). Settings' "Bedtime Mode" row now shows the real configured range as its subtitle and navigates to the new screen, replacing the old "Coming soon" dead row. `gradlew compileDebugKotlin` verified. **Not yet runtime-verified** — needs a real device to confirm the home-kick behavior against a real wall-clock bedtime window.
  - **Real enable/disable toggle added 2026-09-07** (user request, on-device testing) — the mockup's own "Enable Bedtime Mode" toggle had originally been dropped as redundant with "active purely by falling inside the window." User asked for it back, to match every other Settings row's toggle-plus-tap-to-customize pattern (Context Alerts, Wellness Nudges). New `UserConfiguration.bedtimeModeEnabled` (DB 26→27, default `true` so existing window-based behavior isn't silently lost for anyone already relying on it). `AppMonitorService`'s Bedtime branch now gated on `bedtimeModeEnabled` in addition to the existing window check. Settings' "Bedtime Mode" row gained a real `Switch` (subtitle now "On — 11:00 PM – 7:00 AM" / "Off"), and `BedtimeModeScreen.kt` itself gained the same `SettingsCard`+`Switch` toggle `ContextAlertsScreen` uses at the top, with the hour pickers/info text hidden while disabled (`if (config.bedtimeModeEnabled) { ... }`). `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified.**
  - **Known limitation, deliberately deferred (2026-09-07, user's own framing):** Bedtime Mode's actual enforcement mechanism is still the original hard-block-via-allowlist (every non-whitelisted app sent home) — a genuinely different model from Android's own Digital Wellbeing Bedtime Mode, which minimizes notifications/grayscales the screen rather than blocking app access outright. User explicitly asked for this redesign as a *future* item, not now — today's change is the toggle only. Revisit when asked; will need a real design decision on what "minimize notifications" means concretely for this app (which notification channels, grayscale via an accessibility overlay vs. a real system Do Not Disturb rule, etc.) before it can be built.
  - **Deep Work Mode built for real (2026-09-04)**, resolving the open question above: user decided a stricter tier than Focus Mode, own separate Home entry point (not folded into Focus Mode), no early-exit credit penalty (earns the same rate as completing normally). Ended up blocklist-based like Focus Mode after all, not the allowlist approach floated above — but deliberately Admin-category-derived (ALL Social Media/Entertainment apps, `AppCategoryDao`), not the User-owned `FocusBlockedApp` set Focus Mode uses, so it's a genuinely broader/uncustomizable block, matching the ported Figma "DeepWorkPage" mockup's own "ALL SOCIAL & ENTERTAINMENT BLOCKED" copy. New `DeepWorkSession` entity/DAO (DB 22→23, fixed-duration commitment, default 60 min) and `DeepWorkViewModel`/`DeepWorkScreen.kt`/`DeepWorkRoute` (`ui/focus/`) mirroring `FocusSession`'s real-elapsed-time pattern. New `AppMonitorService.deepWorkBlockedPackages` cache + priority-chain branch, ahead of Bedtime/Class/Work Mode (a user-committed session outranks a passive schedule). Exit gate is a real 5x-shake requirement — reuses `RotationalArmDetector` (the same accelerometer+gyroscope detector Move-to-Unlock challenges use) via a new `repsOverride` param, not the mockup's "tap to simulate shake" placeholder. Wired into `NavGraph.kt` (new `Screen.DeepWork`, bottom nav hidden like Focus) and `HomeScreen.kt` (new "Deep Focus" tile between Focus Mode and Lock Apps, per instruction). `gradlew assembleDebug` verified. **Not yet runtime-verified.**
  - **Shake detection tightened same session**: user flagged the shake/arm-swing challenge (`RotationalArmDetector`, used by both Move-to-Unlock and Deep Work's exit gate) as registering "slight movements." Root cause: a 300ms debounce timer gated repeat counts, but nothing required the signal to actually *drop* between counts, so one sustained motion or hand jitter that happened to stay above threshold could rack up multiple reps. Added real hysteresis — a rep now only re-arms once accel+gyro magnitude drop back below a rearm fraction of their thresholds, requiring genuine back-and-forth motion. Investigated the walking challenge (`StepChallengeDetector`) for the same complaint and found it already uses Android's built-in `TYPE_STEP_DETECTOR` hardware/software pedometer sensor (accel+gyro fusion internally, designed for real gait detection including phone-in-pocket) rather than naive accelerometer-variance "swaying" detection — no change needed there, confirmed already real.
  - **Arm-movement detection root-caused and fixed for real, and split into 6 named exercises (2026-09-09)**, following up on the item above — user reported slight movements/swaying were *still* completing the challenge. Real root cause this time: `RotationalArmDetector`'s trigger check compared two independently-latched "last known" accel/gyro magnitude fields updated by unsynchronized sensor event streams — at the slow `SENSOR_DELAY_UI` sampling rate, a stale high reading from one sensor could sit latched for hundreds of ms and combine with an unrelated later spike from the other sensor to falsely fire, even though the two never actually peaked together. The 2026-09-04 hysteresis fix only guarded against *repeated* counting from one sustained motion, not this. Fixed with a real timestamp co-occurrence gate (default 200ms window — extracted into a new pure, unit-testable `RotationalArmRepCounter`/`RotationalArmRepConfig`, mirroring `DoomscrollDetectorTest`'s plain-JUnit4 convention since `SensorEvent` has no public constructor to mock). Also scaled `gyroThresholdRadS` per `ChallengeSensitivity` tier (new `armGyroThresholdRadS`: LOW 2.3f/MODERATE 3.0f (unchanged)/HIGH 3.6f) — previously a flat 3.0f regardless of tier, unlike the accel threshold.
    - **Scope then expanded per user request**: rather than one generic "Rotate your arm" challenge, `ChallengeType.ROTATIONAL_ARM` split into 6 named arm-exercise variants — Front-to-Back Swing, Full Arm Rotation, Bicep Curl, Side Arm Raise, Arm Sway, Arm Stretch — each with its own instruction copy (`LockScreen`/`MoveScreen`/`ChallengeActiveScreen`) and its own motion-intensity threshold tier via a new `armVariant` param threaded through `RotationalArmRepConfig.forSensitivity` (vigorous swing/rotation/raise = full threshold; curl = 0.85x; sway/stretch = 0.55x, plus a wider debounce/co-occurrence window since they're naturally slower). **Explicit user-confirmed scope decision**: this is amplitude + label, not real per-exercise shape detection — the phone's accelerometer/gyroscope only report motion in the device's own body-fixed axes, and how someone holds the phone relative to their arm is unknown/inconsistent, so there's no way to actually distinguish a curl from a side raise without knowing device orientation and hold. Real shape detection was scoped out and flagged below as a future item. `LockScreen`'s random challenge pick restructured to a two-stage selection (category first, then arm variant) so Walk/Jog/Arm keep roughly equal odds instead of the arm category becoming 6x likelier. **Deliberately unchanged**: Deep Work's shake-exit gate and Overuse Intervention's Shake/Rotate steps both construct `RotationalArmDetector` directly without `armVariant` (bypassing `ChallengeType` entirely) — they silently inherit only the co-occurrence bug fix, not the new variety, since they're quick safety/urgency confirmations, not exercise challenges. New `RotationalArmRepCounterTest.kt` (7 tests: co-occurrence firing/rejection incl. exact window boundary, sway/jitter never counting, rearm-required-between-reps, per-tier and per-variant threshold monotonicity, a same-magnitude-sample-fires-gentle-but-not-vigorous case); `ChallengeSensitivityTest`'s monotonicity test extended for `armGyroThresholdRadS`. `gradlew compileDebugKotlin`/`assembleDebug`/`testDebugUnitTest` (including the new tests) all verified. **Not yet runtime-verified** — needs an on-device pass per variant: confirm vigorous variants (Swing/Rotation/Side Raise) still reliably count real reps without being completed by incidental sway, confirm the gentle variants (Sway/Stretch) count their own deliberate gentle motion without being so loose that ambient jitter completes them, and spot-check the Lock screen's two-stage random pick roughly preserves Walk/Jog/Arm odds over repeated triggers.
    - **Future consideration, not built, flagged per explicit user request** ("note the full orientation+classifier build for future purposes, in case it's needed to be more real and accurate"): real per-exercise shape detection would need `Sensor.TYPE_ROTATION_VECTOR`-based world-frame orientation compensation, a fixed or calibrated phone-hold protocol, and a real gesture classifier (template matching or a small trained model) built from labeled real motion data — a materially bigger effort needing its own data-collection/tuning pass, similar in spirit to Module 2's ML work but for motion gestures. Revisit only if asked.
  - **Also flagged by the user for a later session, not started**: a similar accuracy pass for the Run/Jog challenge (`StepChallengeDetector`) — raise this next time arm-movement/challenge-detection work comes up.
- [x] Usage Goals screen (goal targets persisted 2026-08-25) — every goal target, not just "current usage," is now real: the weekly goal lives on a new `UserConfiguration.weeklyScreenTimeGoalMs` column, and the 3 per-category goals (Social Media/Entertainment/Gaming) live in a new `CategoryGoal` table (`categoryType` → `targetMs`, DB version 13→14) via `UsageGoalsViewModel.setWeeklyGoalHours`/`setCategoryGoalHours`. A category with no saved row yet falls back to the same preset the ported UI originally hardcoded as its slider's starting position (2h Social Media, 1.5h Entertainment, 0.5h Gaming) — not fabricated, just an honest "not customized yet" default. Sliders/presets save immediately on change (same pattern as the pre-existing daily-limit slider); "Save Goals" stays a plain "done" button, same as before. `gradlew assembleDebug` verified. **Not yet runtime-verified.**

## ✅ Module 5: Mental Health & Awareness (Done — 2026-08-25)

- [x] Answer Reflection Prompts — built from scratch this session (manuscript/`PROJECT_DOCUMENTATION.md` only specifies "reflection prompts, wellbeing check-ins," no Data Dictionary, so the shape below was proposed and built directly, same as Module 2 LR-II's undocumented feature set). New `ReflectionEntry` Room table (DB version 14→15) — one real row per calendar day (`dateKey` is the primary key), so re-opening the screen the same day edits that day's entry instead of creating a duplicate. `ReflectionViewModel`/`ReflectionScreen.kt` (`ui/reflection/`): a rotating daily prompt (10 static prompts, picked deterministically by day-of-year — written UI copy, same category as the existing `focusQuotes`/challenge-quote lists already in the app, not a stand-in for real data), a 5-point mood check-in (😞–😄), and a free-text answer field; "Save Reflection" upserts the day's row. A real "Past Reflections" list below shows the last 8 saved entries (date, mood emoji, answer) — genuine history, not sample data. Wired from Home's existing "Daily Reflection" `GoalCard` (previously a non-clickable "Coming soon" placeholder built 2026-08-23 specifically anticipating this module) — its subtitle now reads "Answered today"/"Tap to reflect" off a new real `HomeViewModel.hasReflectedToday` flow instead of the placeholder text. `gradlew assembleDebug` verified. **Not yet runtime-verified.**
- **Deliberately out of scope this round**: no integration with Module 2's risk scoring (mood/reflection data isn't fed into the Random Forest — would need retraining and wasn't asked for), and no streak/reminder mechanic (unlike Move's XP streak) — candidates for a later session if wanted.
- **UX gap closed 2026-08-28**: the answer text field previously kept showing the just-typed text after tapping "Save Reflection" (looked like an unsaved draft, not a recorded entry) — flagged 2026-08-27, fixed this session. `ReflectionScreen.kt`'s Save button now clears `answerText` locally right after `saveReflection()` (the `hasLoadedExisting` guard stops the pre-fill `LaunchedEffect` from immediately refilling it from the just-updated `todayEntry`); the back button also explicitly resets `answerText`/`moodRating`/`justSaved` before navigating out, on top of Compose Navigation's own disposal of the screen. Not yet runtime-verified on-device.

## ✅ Module 7: Context-Aware AI (Done — 2026-08-25)

- [x] Receive Context Alerts — built from scratch this session, no teammate UI existed and this module wasn't built by them either. `PROJECT_DOCUMENTATION.md`'s own "Future Enhancements" section lists real GPS-based location locking as explicitly out of MVP scope, so this implements the lighter version its "Location-Based Locking: custom rules by WiFi network" line describes instead: match against the connected Wi-Fi network's *name*, not device coordinates — no new location SDK dependency (no Play Services Location added). New `UserConfiguration.contextAlertsEnabled`/`contextAlertWifiSsid` columns (DB version 15→16). `AppMonitorService` caches the current SSID every 30s (`WifiSsidReader.kt`, cheap local `WifiManager` read, no IPC) and, when enabled and the cached SSID matches the watched one, posts a passive notification (new `context_alert_channel`, logged to `NotificationLog` as `CONTEXT_ALERT`) the first time a Social/Entertainment-categorized app (same real Admin-curated categories Focus Mode blocks) opens — throttled 5 min, and deliberately never blocks or redirects, unlike Focus Mode, matching "Receive Context Alerts" being passive by name. Reading a real SSID needs `ACCESS_FINE_LOCATION` (an Android platform quirk since API 27, unrelated to GPS tracking) — the manifest already had this permission declared but dead/unused from an earlier scaffold; it's now actually requested, lazily, from the new `ContextAlertsScreen.kt` (`ui/settings/`) rather than added to Onboarding's permission set. That screen also lets the user type (or one-tap "Use this" off the live-read current network) the watched SSID, and is honest that this is Wi-Fi-name matching, not GPS geofencing, with a "Planned for Context-Aware AI" section listing real geofencing/multiple locations/per-location rules as future work. Wired from Settings' existing "Location Context" toggle row (previously a non-persisted local-only `remember` boolean with a "Coming soon" subtitle, one of the rows kept visible per the ported-UI-no-skip instruction) — its subtitle now reflects real state ("Off" / "On — set your Wi-Fi network below" / `Alerts on "<ssid>" Wi-Fi`). The separate "Location & Context AI" placeholder row (ACCOUNT section) was removed as a literal duplicate rather than wired to the same screen twice. `gradlew assembleDebug` verified. **Not yet runtime-verified** — needs a device connected to a real Wi-Fi network with at least one app categorized Social Media/Entertainment to exercise the actual alert.
- [x] **Real GPS added on top, 2026-09-04** — user's explicit ask, knowingly beyond the manuscript's stated MVP scope quoted above (their call, not a scope drift). New `UserConfiguration.contextAlertLatitude`/`contextAlertLongitude`/`contextAlertRadiusMeters` columns (DB 21→22, default 100m). New `data/context/LastKnownLocation.kt` — plain `LocationManager.getLastKnownLocation`, no Play Services Location dependency added, same "no new location SDK" stance the Wi-Fi approach took. `AppMonitorService.isNearWatchedLocation()` mirrors `isOnWatchedWifi()`'s passive-alert-only semantics (never blocks); the Context Alert trigger is now `isOnWatchedWifi() || isNearWatchedLocation()`. `ContextAlertsScreen.kt` gained a `LeafletMapPicker` — a `WebView` loading Leaflet.js + OpenStreetMap tiles from their public CDN/tile servers (per the user's explicit "leaflet.js" ask, not a native Compose/Play-Services-Maps widget), tap-to-drop-pin with a JS↔Kotlin `@JavascriptInterface` bridge, radius presets (50/100/250/500m) — kept alongside the existing Wi-Fi section rather than replacing it. Settings' "Location Context" row (which had been reverted back to "Coming soon" pending exactly this kind of user-supplied approach) reconnected with a real subtitle reflecting both trigger types. The screen's own "Planned for Context-Aware AI" card updated to drop "GPS geofencing" from the not-built list. `gradlew compileDebugKotlin` verified. **Not yet runtime-verified** — needs a real device with location permission granted and a real GPS fix to exercise the actual proximity alert; the Leaflet/OSM CDN loads also need a real internet connection to test (untested in this session).

All 9 modules plus the Admin Governance Layer are now built (Module 8's testing/QA backlog aside) — see the Summary table above.

## 🟡 Module 8: Integration & QA (Ongoing — cross-cutting)

- [x] Manual build verification (`gradlew compileDebugKotlin`, `gradlew assembleDebug`) after each code change this session
- [x] Unit tests, first pass (2026-08-25, +2 more 2026-08-27 for Doomscroll Sensitivity) — 22 tests across 6 files under `app/src/test/`, all pure-JVM (plain JUnit4 + `kotlinx.coroutines.runBlocking`, no Robolectric/device needed, no new dependencies added):
  - `ml/RiskScoringEngineTest.kt`, `ml/DoomscrollDetectorTest.kt`, `ml/ExcessiveUsageDetectorTest.kt` — AI inference. These test the hand-written decision logic *around* each generated model (level selection, sigmoid + threshold, the doomscroll risk-level "recipe"'s monotonicity), not the models' own trained accuracy — expected values are derived from each real model's own output for the given sample inputs, so a test fails if the wrapper logic drifts from what its own comments claim, not if the model's training changes.
  - `sensor/ChallengeSensitivityTest.kt` — `fromLabel`/`fromRiskLevel`/`displayLabel` mapping correctness, plus a monotonicity check (LOW < MEDIUM < HIGH effort).
  - `data/CategoryUsageAggregatorTest.kt` — `categoryTotals()`'s bucketing/summing/uncategorized-falls-into-OTHER behavior, against a hand-written `FakeAppCategoryDao` (implements the real `AppCategoryDao` interface in-memory — no Room/SQLite needed since the function only takes the DAO interface, not a concrete Room instance).
  - `ui/components/NotificationsPanelTest.kt` — timestamp calculations: `formatRelativeTime`'s minute/hour/day boundaries and the future-timestamp-never-negative clamp (visibility bumped `private`→`internal` to make it testable; behavior unchanged).
  - Deliberately not attempted this pass: DAOs' actual SQL query behavior (Room needs either a real device/emulator or Robolectric — this machine has neither an AVD configured nor Robolectric added yet, and this toolchain's AGP 9.2.1/Kotlin 2.2.10 versions are new enough that Robolectric compatibility isn't verified; a candidate for a later session, ideally proven with a quick spike before committing to it broadly).
- [ ] Integration tests (DB persistence, AccessibilityService, sensor lifecycle, UI state) — blocked on the same Room/Robolectric-or-device gap as DAO unit tests above; AccessibilityService/sensor lifecycle additionally need a real device (already known: no local emulator on this machine, see Module 4's Break Reminder entry).
- [ ] End-to-end tests (lock → shake → unlock, risk-scoring difficulty, settings persistence, rotation) — needs a real device.
- [ ] Performance/battery profiling against `PROJECT_DOCUMENTATION.md`'s target metrics — needs a real device.
- [ ] ISO/IEC 25010 validation pass

### Known issues (reported by user, 2026-09-07 — device testing)

- [x] **Battery Optimization Exemption made required, not optional** — `ui/permissions/PermissionSteps.kt`'s `PermissionStep` for it dropped `required = false` (now defaults `true`, matching every other step). Onboarding's "Continue" button now blocks on it like the other required grants (see `OnboardingScreen.kt`'s `allGranted`/`requiredSteps` — both already computed generically off `.required`, no other code changed), and both Onboarding and Settings' Permissions screen drop the "(recommended)" label for this row since `isOptional = !step.required` is also generic. **Rationale**: user reports screen time readings looking wrong and usage not syncing properly, suspects battery permission — plausible root cause: without this exemption, OEM battery managers (already documented elsewhere in this file suppressing Break Reminder's heads-up notification on a vivo/Funtouch device) can freeze or throttle `AppMonitorService`'s background monitoring, silently dropping usage/lock events rather than erroring visibly. `gradlew compileDebugKotlin` verified. **Not yet runtime-verified** — needs the user's device to confirm this actually resolves the sync gap; it may not be the only cause (see below).
- [ ] **Screen time reading might be wrong** — reported by user on-device, not yet reproduced/diagnosed in this session (no device access here). `UsageStatsRepository.getUsageForRange()` (real source behind Home/Reports/Usage Goals) calls `UsageStatsManager.queryUsageStats(INTERVAL_DAILY, ...)`, which is a known-imprecise Android API across OEMs — `INTERVAL_DAILY` buckets don't always align cleanly to midnight-to-midnight and can lag until the OS flushes usage stats, independent of the battery-optimization issue above. Needs on-device investigation: compare Physi-Lock's reported total against Android's own Digital Wellbeing/Settings > Screen time for the same apps/day to isolate whether this is a stale-bucket timing issue, an OEM-specific UsageStatsManager quirk, or something else. **Re-raised 2026-09-09** ("proper reading for apps screen time... should be accurate reading") — same gap, still not diagnosed, see item 11 in the "Global UI/UX & data-accuracy standards" section below.
- [ ] **Usage sometimes doesn't sync properly, possibly device/OS-version-specific** — user isn't certain this is purely the battery permission gap above or a separate device-version quirk. Likely the same underlying cause as the screen-time issue (background monitoring being killed/throttled or `UsageStatsManager` lagging), but not confirmed. Revisit once the battery optimization fix above is runtime-verified — if the gap persists with the exemption actually granted, it points to the `UsageStatsManager`/OEM angle instead.

## UI pending real design (as of 2026-08-28, updated 2026-09-04)

User-provided list of screens still waiting on real (non-self-designed) UI to be fetched/attached — see [[project_remaining_ui_attachments]] for the standing agreement (don't over-polish placeholder visuals meanwhile, keep the underlying logic solid, real design comes later):
- Reflection screen
- Context Alerts screen
- Schedule Block section — still self-designed, no teammate/Figma design provided yet (checked all 3 teammate repo branches 2026-09-04, nothing there either)
- Work Hours section
- ~~Weekly Reports (4-week chart)~~ — done 2026-09-04: ported the teammate's dashed-goal-line chart + icon insight rows from `sprint-2-ui-navigation` (`ReportsScreen.kt`'s `WeeklyUsageChart`/`InsightRow`).
- ~~Whitelist Manager~~ — done 2026-09-04, from a Figma Make snippet the user pasted directly. See `WhitelistManagerScreen.kt`.
- ~~Bedtime Mode~~ — done 2026-09-04, from a Figma snippet the user pasted directly (real backend too, not just UI — see Module 6 above).
- ~~Location Context~~ — done 2026-09-04, real GPS via Leaflet.js map (see Module 7 above).

### Backlog explicitly held for later (as of 2026-09-04, user said "hold everything for later")

User is providing more comparison UI code across several sessions. Do NOT start any of these until the user says go — this list exists so the queue isn't lost between sessions:

- [x] **Real application icons — done 2026-09-04, pulled out of the held backlog per explicit instruction** ("do real app icons first" while everything else stays held). New shared `AppIconAvatar`/`rememberAppIconBitmap` (`ui/components/AppIcon.kt`): `PackageManager.getApplicationIcon(packageName).toBitmap()` (androidx.core-ktx, no new dependency), cached per package via `remember(packageName)`, falling back to the existing initial-letter avatar only when resolution fails (`runCatching` — covers Admin's manually-added apps, which have no real installed package behind their `AppCategory` row, and any since-uninstalled app). Existing state-color coding (locked/unlocked, blocked/allowed) preserved as the avatar's background color, now showing through only as a matte behind the real icon. Wired into every colored-circle-initials picker: `AppLockRulesScreen`, `FocusBlockedAppsScreen`, `WhitelistManagerScreen`, `ScheduleBlockSection`'s Study App Allowlist, and Home's `AppUsageRow` (App Usage Today — small inline icon variant, falls back to the generic `PhoneAndroid` icon instead of an avatar). Also fixed `AdminCategoriesSection`'s `emojiFor()` — a hardcoded substring-matched emoji guess (📸 for "instagram", 📱 fallback for anything unmatched) — replacing a fabricated per-app icon with the real one, consistent with this project's standing rejection of fabricated data elsewhere. `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified** — needs a real device to confirm icons actually render (emulator/build-machine only confirms it compiles).
- [x] **Launcher/home-screen excluded from usage totals — fixed 2026-09-04**, found while reviewing the icon work above (user asked why Home's App Usage Today would ever show the system home screen). Root cause: `UsageStatsRepository.getUsageForRange()` read raw `UsageStatsManager.queryUsageStats()` with no exclusion list — Android counts the launcher's own visible time (between app switches, widget/search use) as real foreground time, same as any app. This wasn't just cosmetic: it fed into `HomeViewModel._todayScreenTimeMinutes` (the total compared against the daily limit and shown on the risk ring), Reports' daily/weekly totals, Top Apps, Category Breakdown, and `UsageGoalsViewModel`'s current-usage numbers — all via the same shared repository method. Fixed centrally: `UsageStatsRepository` now resolves the current default launcher package once (`Intent(ACTION_MAIN).addCategory(CATEGORY_HOME)` → `resolveActivity`) and excludes just that specific package from `getUsageForRange`'s results — narrower than filtering on `ApplicationInfo.FLAG_SYSTEM`, which would also catch legitimate OEM-preinstalled apps (Camera, Phone) a user actually uses. `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified** — needs a real device to confirm the resolved launcher package actually matches and disappears from the totals.
- [x] **Reports page — Risk Score panel built 2026-09-06.** Comparison code (`UsageTab`) had been pasted and compared against real `ReportsScreen.kt`/`ReportsViewModel.kt` 2026-09-04. Re-checked against current code before building (the 2026-09-04 note was stale on two points): the "Weekly tab is a coming-soon placeholder" finding was already wrong by the time this was picked up — Weekly has been a real 4-week chart since 2026-08-27 (see `WeeklyBreakdownContent`/`buildWeeklyBreakdown`); and the "AI Insights panel is 100% hardcoded" finding described a section that doesn't exist in real Reports — real's "AI Insights" card already shows genuine LR-II predictions (`todaysPredictions`), not fabricated strings. That left exactly one genuinely new, buildable piece: the **Risk Score bottom-sheet panel** (tap a "Risk: X%" badge → circular meter + 4 behavioral indicators). Built it: new tappable `RiskScoreBadge` next to the "Usage Reports" header opens `RiskScoreSheet` (custom scrim-sheet, same pattern as `EndFocusSessionSheet`, not Material3's `ModalBottomSheet`), reusing the shared `CircularProgressRing` Home's risk ring already uses. `ReportsViewModel` now runs the same Module 2 Random Forest classifier Home's risk ring does (`RiskFeatureExtractor`/`RiskScoringEngine`) to back `riskLevel`/`riskScorePercent`. The 4 indicators are: screen time today vs. daily limit, doomscroll episodes today, excessive-usage hours flagged today (out of hours LR-II has scored so far), and bypass attempts today. The last one corrects the 2026-09-04 finding that it "has no real counter anywhere in this app" — it does: `MotionInterventionLogDao.countBypassAttemptsAfter`, already read by `RiskFeatureExtractor` for Home's own risk score, just not surfaced in Reports before. `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified.**
- [x] **Intervention page(s) — the one lift-worthy piece done 2026-09-06: Deep Work duration picker.** Comparison code (`InterventionTab`) had been pasted and compared against the real app 2026-09-04. It's a *new unified-tab concept* that doesn't exist in the real app — real equivalents are scattered across screens, and most of its cards conflict with a real model or need a product decision, not a straight port (see below, still held). `DeepWorkCard`'s duration picker (1/1.5/2/3hr) was the one clean exception — a legitimate small enhancement, no conflicting model. Built: `DeepWorkRoute` no longer auto-starts a fixed 60-min session on entry — when no session is active yet it now shows a new `DeepWorkDurationPicker` (1/1.5/2/3hr, matching the mockup's options) before calling `DeepWorkViewModel.startSessionIfNeeded`; re-entering mid-session (session already active) skips straight to the real timer screen unchanged. Ordinary back navigation works on the picker (nothing has started yet), while the existing "no silent exit" shake-gate stance is preserved once a session is actually running. `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified.**
  - **Still held, need a decision before building — not started:** `AdaptiveLockCard`'s master on/off + live "currently auto-locked" list is a new concept (real only has per-app "Adaptive difficulty" toggles in App Lock Rules, no silent master switch or live view). `FocusModeCard`'s fixed-duration + inline app-picker + break-duration is a materially different model from real Focus Mode's open-ended "run until you end it" — can't be ported without deciding which model wins. `UsageLimitsCard` (per-app soft caps with progress bars) is new scope beyond Usage Goals' per-category/daily limits today. `ChallengesSummaryCard` fully overlaps the real Move screen (XP/streak/challenges) already — no new ground there, nothing to build. Likely also overlaps the Lock Overlay entry below and the still-missing Overuse Intervention screen (tapping an over-limit app on Home currently just opens App Lock Rules, no real challenge-gated intervention flow exists).
- [x] **Settings page — the one lift-worthy piece done 2026-09-06.** Comparison code (`SettingsTab`) had been pasted and compared against real `SettingsScreen.kt` 2026-09-04; real was already ahead of that mockup everywhere except one thing: the mockup's `ProfileEditPage` showed a live password-requirements checklist (✓/✗ per rule as you type) that real `EditProfileScreen.kt` lacked. Rather than inventing new rules, reused the existing `PasswordRequirementsChecklist` composable (`ui/components/AuthComponents.kt`) already shown on `CreateAccountScreen` — same focus-gated presentation (`newPasswordFieldFocused`), same 4 requirements (8 chars, upper, lower, digit). Purely advisory, matching how registration already treats it: the actual submit gate stays the pre-existing "min 6 characters" check (Firebase Auth's real minimum) — not tightened to require all 4, since registration itself doesn't enforce them either, only displays them. `gradlew compileDebugKotlin` verified. **Not yet runtime-verified.** Everything else the mockup had (Profile card, Mode selector, Notifications & Privacy, Account section, Focus Mode Blocked Apps, Motion Lock Sensitivity, Location Context/Bedtime Mode) was already real/more built-out — no further action needed on this item.
- [ ] **Lock Overlay redesign — Bedtime's hard-block piece done 2026-09-06, rest still held.** Full `LockOverlay` demo code was pasted 2026-09-04, covering 6 trigger types: `usage-goal`, `adaptive` (AI risk), `focus-mode`, `deep-work`, `bedtime` (hard block), `doomscrolling` (reflection prompt with "5 more minutes"/"Take a Break"). Per explicit instruction, built just the one gap that had no real screen at all and no conflicting design decision: Bedtime Mode's actual hard block. `AppMonitorService`'s Bedtime branch previously only did `performGlobalAction(GLOBAL_ACTION_HOME)` + a throttled notification (`handleScheduleBlock`, unchanged and still used by Class/Work Mode below it). It now launches a new `BedtimeLockActivity`/`BedtimeLockScreen` (`ui/lock/`) instead — same `startActivity(NEW_TASK|CLEAR_TASK)` mechanism `LockActivity` already uses for App Lock Rules' challenge lock, registered in the manifest the same way. Per the user's "make it uniform with the UI" instruction, this is styled with the app's real light theme (`BackgroundLight`/`DeepOlive`/`SageAccent`/`CardCream`/`Nunito`, matching `BedtimeModeScreen` and the rest of Settings/Home/Reports) instead of `LockScreen`'s one-off dark-blue background or a new visual style borrowed from the mockup. Shows the real blocked app's icon/name (`AppIconAvatar`) and the real configured wake time; unlike `LockActivity`, there's no unlock challenge — Bedtime is a hard block by design, so the only action is a "Return to Home Screen" button that finishes the activity (the OS shows the launcher next, since the task was already cleared). `gradlew compileDebugKotlin`/`assembleDebug` both verified, including manifest merge. **Not yet runtime-verified** — needs a real device with the system clock inside a configured bedtime window to confirm the lock screen actually launches instead of the old home-kick.
  - **Focus Mode / Deep Work overlays also done 2026-09-06**, as part of a larger held-backlog planning session (plan covers 7 work items total: this one, Overuse Intervention, Student Mode Pomodoro, Work Mode redesign, Wellness Nudges [done], Home visual pass, Delete Account [done]). Per decision, these two silent-home-kick trigger types become full-screen violation overlays too — purely informational, no bypass, same treatment as Bedtime's. New shared `ModeLockActivity`/`ModeLockScreen` (`ui/lock/`) — a generalized version of `BedtimeLockActivity`/`BedtimeLockScreen` parameterized by title/message, so one screen serves both (Bedtime's own files are untouched, avoiding any regression risk on an already-shipped screen). `AppMonitorService`'s Focus Mode branch (`handleFocusBlock`/`postFocusBlockNotification`, plus their notification channel/throttle constants) is deleted entirely and replaced with `startActivity(ModeLockActivity, title="Focus Mode Active", ...)`; the Deep Work branch (previously routed through `handleScheduleBlock`) gets the same replacement, `title="Deep Work Active"`. Both drop their old notification entirely, matching the Bedtime precedent — ending the session (Focus) or the shake-exit gate (Deep Work) stays the only real way back in, this only replaces the silent kick with an honest explanation. Class Mode and Work Mode's `handleScheduleBlock` calls are unchanged (out of scope for this item). `gradlew compileDebugKotlin`/`assembleDebug` both verified, including manifest merge. **Not yet runtime-verified** — needs a device with an active Focus/Deep Work session and a blocked app to open.
  - **`usage-goal`/`doomscrolling` trigger types resolved into a new real feature 2026-09-06 — Risk-Tiered Overuse Intervention.** The manuscript's own Digital Wellbeing/Self-Regulation section describes reflection prompts and motion-responsive unlocking as ONE integrated flow, triggered together by the doomscroll classifier and the Random Forest risk model — exactly matching a separately-pasted `OveruseInterventionPage` mockup (risk-tiered Shake/Rotate/Walk challenges: LOW = Shake×3, MODERATE = Shake×3→Rotate×3, HIGH = 5-min cooldown→Shake×8→Walk×7). Built for real, not simulated: new `ui/challenge/OveruseInterventionActivity.kt`/`OveruseInterventionScreen.kt` drive each step with the actual `RotationalArmDetector`/`StepChallengeDetector` sensors (same real accelerometer/gyroscope/step-sensor detection App Lock Rules and Deep Work's exit gate already use), not the mockup's "tap to register" buttons — `StepChallengeDetector` gained a `walkStepsOverride` param (mirroring `RotationalArmDetector`'s existing `repsOverride`) so a caller can request an exact step count independent of `ChallengeSensitivity`'s tiers. Risk tier is supplied by the caller, not recomputed here (`AppMonitorService` already has a live cached level; `HomeViewModel` already computes its own for the risk ring). On completion: reuses the existing `AppMonitorService.grantTemporaryUnlock`/`CHALLENGE_UNLOCK_DURATION_MS` (20 min) and logs a real `MotionInterventionLog(triggerType="OVERUSE_INTERVENTION", userResponse="UNLOCKED", xpEarned=15/30/50)`. "Emergency Access" is always visible (per the mockup's own "always visible, per UX rules" note) and grants no unlock — it only dismisses and logs `userResponse="BYPASSED"`, same bypass-logging spirit `LockActivity.onStop()` already uses.
    - **Doomscrolling wiring**: `AppMonitorService.checkDoomscrolling`'s positive-detection branch no longer posts a passive notification (`postDoomscrollAlertNotification`, its channel, and its constants are deleted) — it now launches a new `ui/reflection/DoomscrollReflectionActivity`/`Screen` (light-themed, matching the real `ReflectionScreen`'s tone, not the dark active-challenge theme). "5 More Minutes" calls a new `AppMonitorService.snoozeDoomscrollAlerts(durationMs)` (a new companion `doomscrollSnoozeUntil`, same cross-context pattern `grantTemporaryUnlock`/`temporaryUnlocks` already use) — this only suppresses re-alerting for 5 minutes, since doomscrolling was never a blocking mechanism to begin with; it's separate from the existing `DOOMSCROLL_ALERT_COOLDOWN_MS` (which guards a *positive* detection from re-alerting on the same continuing binge). "Take a Break" launches `OveruseInterventionActivity` for the same package.
    - **Home wiring (`usage-goal`)**: `AppUsageRow`'s over-limit tap (`HomeScreen.kt`) previously just opened App Lock Rules ("there's no real unlock action for a soft-threshold app" — no longer true). It now opens `OveruseInterventionActivity` directly, using the same real risk level `HomeViewModel` already computes for the risk ring.
    - `gradlew compileDebugKotlin`/`assembleDebug` both verified, including manifest merge (3 new activities registered: `OveruseInterventionActivity`, `DoomscrollReflectionActivity`, plus `ModeLockActivity` from the item above). **Not yet runtime-verified** — needs a real device to trigger a doomscroll episode and to have an app cross the 3-hour "OVER" threshold, and to confirm the real sensor detectors register reps correctly for each tier.
  - **Still held, need a decision before building — not started:** `adaptive` duplicates the real `LockActivity`/`LockScreen`/`ChallengeActiveScreen` challenge flow already, nothing to build. The mockup's "Take a Break" / temporary-snooze concept for Focus/Deep Work specifically still doesn't exist — needs real design (how long, does it re-lock after, is it per-app or global) before that could be added on top of those two overlays.
- [x] **Home screen visual pass — done 2026-09-06**, re-checked against a re-pasted `HomeTab` mockup as part of the same held-backlog planning session as the items above. Finding: every section (Screen Time card w/ ring + yesterday delta, Predictive Overuse banner, Risk score + 3 action tiles, Usage Goals + Daily Reflection cards, App Usage Today) was already real and already close to the mockup structurally — confirmed by a theme-token audit finding near-exact hex matches already (`DeepOlive`=`#3d4928`, `SageAccent`=`#9cb06d`, `Orchid`=`#c4a8d0`, `CardCream`=`#f5f3eb`, `TertiaryTan`=`#dad5bb`, `BackgroundLight`=`#fefefe`, `SecondarySage`=`#bac892`, `MutedText`=`#536136`), and corner radii (24dp big cards, 15-18dp mid/small cards) already within 1-2dp of the mockup's `rounded-3xl`/`rounded-2xl`. So this narrowed to one real gap: the mockup's ring draws a "sandwich" stroke (a wider dark-olive outline underneath the normal track/progress arcs, so a thin outline peeks out on both edges) that the shared `CircularProgressRing` component didn't have. Added a new `outlineColor: Color? = null` param — defaults to `null` (byte-for-byte the original single-stroke geometry, so every pre-existing call site is visually unchanged) and opts in the mockup's effect when set. Applied to Home's Screen Time ring and Risk ring, and to Reports' Risk Score sheet ring for consistency (both show the same risk-score concept) — left the challenge-flow rings (`ChallengeActiveScreen`, `ChallengeProgressContent`, `OveruseInterventionScreen`) alone since they're outside this item's scope. The tap-through on over-limit apps (the other real gap this mockup exposed) was already resolved by the Overuse Intervention work item above. `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified** — needs a device/emulator to visually confirm the ring outline renders correctly.
- [x] **Work Mode redesign — done 2026-09-07**, against a separately re-pasted `WorkModePage` mockup (distinct from Student Mode's own Pomodoro-timer mockup — these two turned out to need different treatments, not one shared design). Fully replaces `WorkModeScreen.kt`'s old per-day `ScheduleBlockSection` usage:
  - **Work Hours**: new `UserConfiguration.workHoursStartMinute`/`workHoursEndMinute` (DB 24→25), same shape as `bedtimeStartMinute`/`EndMinute` — a single daily window applied Mon-Fri only via a new `AppMonitorService.isWithinWorkHours()` (mirrors `isWithinBedtimeWindow`'s wrap-past-midnight logic plus a weekday check), replacing the old `activeScheduleBlock() != null` condition in both the Work Mode block branch and `isQuietHours()`. Matches the mockup's static "Monday – Friday" label, which has no day picker of its own.
  - **Deep Work Blocks**: named sub-windows that auto-start/end the app's *existing* real Deep Work Mode session, not a new blocking concept — new `DeepWorkSchedule` entity/DAO (`label`, `startMinute`, `endMinute`, `active`) and a new `AppMonitorService.checkDeepWorkSchedules()` running on a 1-minute loop (decoupled from accessibility events, since a window can start with no app switch happening). `DeepWorkSession` gained a `triggeredBy` field ("MANUAL"/"SCHEDULE") so the checker only ever auto-ends a session it started itself, never touching one the user started by hand from Home; ending one credits `focusCreditBalanceMinutes` at the same 1-min-per-5 rate `DeepWorkViewModel.endSession` already uses.
  - **Break Reminder**: no new backend — confirmed `breakReminderIntervalMs`/`breakReminderIntervalPresets`/`SettingsViewModel.setBreakReminderIntervalMinutes` already existed and already offered exactly the mockup's 15/30/45/60/90 options (made `breakReminderIntervalPresets` `internal` so this screen could reuse it directly instead of duplicating the list). Just rendered as an inline picker on the new screen.
  - **Blocked apps list**: shown read-only, a deliberate deviation from the mockup's per-app toggle — Work Mode's blocking is Admin-governed (the same Social Media/Entertainment category set Focus Mode blocks), not User-owned like Focus Mode's own `FocusBlockedApp`, so a real toggle here would hand the User an authority Work Mode has never had.
  - **Dropped entirely**: the mockup's "Auto-activates based on calendar schedule" toggle — no Calendar API integration exists or is in scope; the real mechanism is unchanged (active whenever now falls in the configured window and `userMode == WORK_MODE`).
  - `ScheduleBlockSection`/`AddScheduleBlockDialog`/`ScheduleBlock` entity are **not yet deleted** — `StudentModeScreen.kt` still uses them for Class Schedule until the Student Mode Pomodoro item below also lands; cleanup is deferred until both are done, per the original plan.
  - `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified** — needs a device to confirm Work Hours window blocking and Deep Work Block auto-start/end actually fire on a real clock.
- [x] **Student Mode Pomodoro-timer redesign — done 2026-09-07.** The largest single piece of that session's 7-item plan, and the last one — completing it means the whole held-backlog plan is now done (Overuse Intervention, Focus/Deep Work overlays, Work Mode redesign, Wellness Nudges, Home visual pass, Delete Account, and this). Fully replaces `StudentModeScreen.kt`'s old `ScheduleBlockSection`/`AllowlistSection` usage with a real Pomodoro timer:
  - **New `PomodoroSession` entity** (DB 25→26): one row spans a full run from Start to Reset, cycling WORK/BREAK phases automatically. Same survives-navigation/process-death principle as `FocusSession`/`DeepWorkSession` (remaining time computed from a persisted `phaseStartTimeMillis` wall-clock anchor, not a live-only counter) — extended with a real pause: `pausedAt` freezes the computation, and resuming shifts `phaseStartTimeMillis` forward by exactly the paused duration so the countdown picks up precisely where it left off.
  - **New `PomodoroViewModel`**: `activeSession` is a Room `Flow`; `remainingSeconds` ticks once a second by `collectLatest`-ing it — writing a phase-advance to the DB when a phase hits zero causes the `Flow` to re-emit, which naturally restarts the tick loop against the fresh phase/target, rather than needing a separate transition watcher.
  - **New `PomodoroBlockedApp` entity/DAO**: a genuinely different model from the old Class Schedule's allowlist-inversion (block everything except allowlisted) — this is a blocklist (specific apps blocked), matching the mockup's own "Blocked During Study" list. **Design decision, not in the mockup**: blocking only applies during the WORK phase, not BREAK — matches real Pomodoro technique (a break is meant to be an actual break), documented as an explicit choice since the mockup's static demo data didn't specify this either way.
  - **`AppMonitorService` wiring**: new `pomodoroPhase`/`pomodoroBlockedPackages` cached fields (same live-Flow-collector pattern as Deep Work), a new priority-chain branch (`pomodoroPhase == "WORK" && packageName in pomodoroBlockedPackages`) using the same `ModeLockActivity` full-screen overlay Focus/Deep Work use — a Pomodoro session is a real user commitment like Deep Work, not a passive schedule, so it gets the same treatment (title "Study Session Active").
  - **UI** (`PomodoroSection.kt`): an embedded timer card (ring countdown via `CircularProgressRing`, Start/Pause/Resume/Reset, session counter) plus a fullscreen expand view modeled on `DeepWorkScreen`'s fullscreen pattern (not locked/forced the way Deep Work's is, since Pomodoro was never a hard-block mode). Session Settings (Study 15/20/25/30/45/50 / Break 5/10/15/20 presets) only take effect for the *next* session, matching the mockup's own behavior where changing presets mid-run doesn't retroactively resize the phase already in progress.
  - **Cleanup**: with both this and the Work Mode redesign done, `ScheduleBlockSection.kt` (which held `ScheduleBlockSection`/`AddScheduleBlockDialog`/`AllowlistSection` — the latter also went fully unused once Student Mode stopped calling it) and the `ScheduleBlock` entity/DAO were deleted entirely, along with `AppMonitorService`'s `activeScheduleBlock()`/`scheduleBlocks` cache. `AllowlistedApp`/`allowlistedAppDao` stay alive (Bedtime Mode + the standalone Whitelist Manager screen, which has its own real UI, not the deleted `AllowlistSection`) — fixed two now-stale/wrong doc comments and one **user-facing text bug** this cleanup would have caused: Whitelist Manager's own screen copy said "This applies to Class Mode only," which became false the moment Class Mode's `ScheduleBlock`-based blocking was removed — corrected to say Bedtime Mode.
  - `gradlew compileDebugKotlin`/`assembleDebug` both verified after every step, including the final state. **Not yet runtime-verified** — needs a device to confirm the timer survives navigation/process death, pause/resume math is correct, and WORK-phase blocking actually fires.
- [x] **Wellness Nudges — made real 2026-09-06, no new content invented.** The user pointed out this placeholder toggle was really just describing content the app already has: the rotating `focusQuotes` line in `FocusModeScreen.kt` and `deepWorkMessages` in `DeepWorkScreen.kt` (both shown during a session), plus Daily Reflection. Rather than designing new nudge content from scratch, the Settings toggle now gates the existing quotes: new `UserConfiguration.wellnessNudgesEnabled` (DB 23→24), `SettingsViewModel.setWellnessNudgesEnabled`, and a new `wellnessNudgesEnabled: StateFlow<Boolean>` on both `FocusModeViewModel`/`DeepWorkViewModel` (reading the same config) threaded down to each screen — when off, that quote line simply isn't composed. Daily Reflection stays its own separate feature with its own Home entry point, not folded under this toggle. Subtitle is a constant instructional line per explicit instruction ("Toggle on to show mindful quotes during Focus & Deep Work sessions"), not an On/Off state readout. `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified** — needs a device to confirm toggling actually hides/shows the quotes in both screens.
- [x] **Delete Account's actual Firebase deletion — built for real 2026-09-06**, per explicit go-ahead as part of a larger held-backlog planning session (see the plan at the top of this session for the other 6 work items still pending: Overuse Intervention, Focus/Deep Work overlays, Student Mode Pomodoro, Work Mode redesign, Wellness Nudges, Home visual pass). New `FirebaseAccountRepository.deleteAccount(currentPassword, googleIdToken)` mirrors `changePassword`'s exact reauth-then-act pattern — password-based accounts reauthenticate with `EmailAuthProvider`/the re-entered password; Google-linked accounts (no password provider) need a *fresh* Google ID token instead, via a new `AuthViewModel.getFreshGoogleIdToken()` (same Credential Manager prompt `signInWithGoogle` uses, `setFilterByAuthorizedAccounts(true)` since this is a reauth of an already-linked account, not a new sign-in). New `DeleteAccountResult` sealed class (`Success`/`WrongPassword`/`NeedsGoogleReauth`/`Error`), same shape as the existing `ChangePasswordResult`. Cleans up the Firestore `users/{uid}` profile doc + `usernames/{username}` lookup doc as best-effort before deleting the Auth user (a failed Firestore cleanup doesn't block the deletion — there's no recoverable account either way once Auth is gone). On success, `PhysiLockDatabase.clearAllTables()` wipes the entire local Room DB — deliberate full-wipe scope, not per-table filtering: of 18 entities only `CachedAccount`/`LoginEvent` reference an account at all, and this app has no account-switching UI (single-account-per-device), so nothing else would be left orphaned. `SettingsScreen.kt`'s `ConfirmSheet` "Delete Account?" now leads into a new `DeleteAccountReauthSheet` (same scrim+bottom-card visual pattern as `ConfirmSheet`) — a password field for password-based accounts, or a "Confirm with Google" button that re-triggers the Google prompt for Google-linked ones — before the real deletion fires; on success, reuses the existing `onLogout()` callback (same sign-out + navigate-to-login flow Sign Out already uses). `gradlew compileDebugKotlin`/`assembleDebug` both verified. **Not yet runtime-verified** — needs a real Firebase test account (both a password-based and a Google-linked one, to exercise both reauth paths) — do not test against a real user's production account.

### Global UI/UX & data-accuracy standards (added 2026-09-09 — not started)

User review of the current build found visual drift from the source Figma designs and reiterated the app's standing "no fabricated data" rule. Apply as a design-system pass across screens as they're touched — not necessarily one big rewrite in one sitting:

- [ ] **No stale/placeholder data anywhere** — every value shown on-device (screen time, risk score, streaks, stats, etc.) must be real, live, correctly-computed data, never sample/hardcoded/stale. Overlaps the two open device bugs already tracked above ("Screen time reading might be wrong", "Usage sometimes doesn't sync properly").
- [ ] **Home screen must match the Figma layout exactly** — same colors and same section sizes as the given UI. Specific gap flagged: the Focus Mode / Deep Work Mode / Lock Apps tiles currently render noticeably larger than the Risk Score tile; all should be uniform section sizes matching Figma, not just visually close. (Extends the "Home screen visual pass" item above, which only fixed the ring-outline stroke.)
- [ ] **No emoji icons** — replace with real icon assets/vector icons. Emoji confirmed still present as of 2026-09-09 in `ReportsViewModel.kt`, `AboutScreen.kt`, `ReflectionScreen.kt`, `UsageGoalsScreen.kt`, `CreateAccountScreen.kt`, `ChallengeActiveScreen.kt` — Reflection's mood-picker emoji may be legitimate content rather than a decorative icon, needs a per-file call rather than a blanket strip.
- [ ] **Use only the app's real design tokens** — the two typography fonts (`Nunito` for UI text, `DmMono` for numeric/mono display, both in `ui/theme/Type.kt`) and the real palette (`ui/theme/Color.kt`: `DeepOlive`, `SageAccent`, `Orchid`, `SoftSand`, `MutedText`, `CardCream`, `BackgroundLight`, `SecondarySage`, `TertiaryTan`, `Warning`, `Danger`/`ErrorRed`, etc.) — no ad-hoc fonts or off-palette colors anywhere.
- [ ] **Button color convention**: a Save/Submit (confirm) action gets the stronger brand color; its paired Cancel gets a visually lighter color — not equal visual weight between the two.
- [ ] **Save/Submit needs a visible in-progress state**: loading spinner → checkmark → "Saved" label, then settle into a lighter/grayish palette tone (candidate: `TertiaryTan`/`SecondarySage` family — pick from the real palette, don't invent a new gray) — never an instant silent state change with no feedback.
- [ ] **Figma layout is the baseline everywhere**, except sections added beyond Figma's original scope (Deep Work, Location Context map, Bedtime Mode, Pomodoro, etc.) — those keep their current self-designed layout.

**Why:** matches this session's ask to record backlog items with commit/branch tracking — these are new, not yet started, no commits yet.

**How to apply:** don't start broad implementation without the user naming which screen to prioritize first (same standing rule as the rest of this backlog). Overlaps [[project_remaining_ui_attachments]] (Schedule Block still needs its own Figma attachment) and the Module 8 known-issues section above.

#### Second batch, added 2026-09-09 (Plan Mode research pass — explicitly recorded as backlog, not built; see [[feedback_backlog_vs_build]])

Each item below was verified against real code before recording (not just transcribed from the user's request) — several turned out different from what the user assumed. Scope decisions the user already confirmed, to apply whenever an item is picked up: build the logo/launcher-icon treatment against the existing `logo.png` now, final art swaps in later with no code changes needed; username/password rules should hard-block registration, not just show an advisory hint; loading-indication work should cover Home/Reports skeletons in addition to the startup splash; full-page→modal conversion is deferred — user will name specific screens later.

- [ ] **Username & password validation, hard-gated** — `CreateAccountScreen.kt` currently has **zero** username format/length validation (confirmed: truly unbounded, any characters, any length) and the password checklist (`AuthComponents.kt`'s `passwordRequirements` — 8 chars/upper/lower/digit) is advisory-only, never actually gating submission. Add: username regex `^[A-Za-z0-9_-]{3,32}$` (3–32 chars, letters/digits/underscore/hyphen), a 5th password requirement ("at least one special character"), and make both a real hard block on Create Account's submit handler. Also apply the same username-format check to `EditProfileScreen.kt`'s username field (a rename currently bypasses any rule entirely) and gate its Change Password section the same way.
- [x] **Fix email verification enforcement — done 2026-09-10.** Contrary to the user's assumption, a full Firebase native-link email verification flow already existed (`VerifyEmailScreen.kt`, `sendEmailVerification`/`checkEmailVerified` in `AuthViewModel.kt`/`FirebaseAccountRepository.kt`) — but it was only a one-time gate shown right after fresh registration. Confirmed bug: `VerifyEmailScreen`'s back button logs the user out and returns to Login, and neither `AuthViewModel.login()` nor the app's session-resume path (`getCurrentAccount()`, called from `MainActivity`'s startup check) ever re-checked `isEmailVerified` — so a user who backed out once could log back in with the same unverified account and reach Home permanently, no re-prompt ever. Fixed in `MainActivity.kt` with a new shared `proceedPastAuth(account, verifiedStage)` suspend helper, called from all three places an `Account` reaches the UI post-auth: `LoginScreen`'s `onLogin`, both `onGoogleSignIn` call sites (Login and Register screens), and the session-resume `LaunchedEffect`. It calls `authViewModel.checkEmailVerified()`, routes to `AppStage.VERIFY_EMAIL` (not `ONBOARDING`/`HOME`) when false, and skips the check entirely for `Role.ADMIN` (console-provisioned, never go through this flow). No special-casing needed for Google sign-in — Firebase marks Google-authenticated users verified automatically, so the same check just passes for them. Deliberately fails **open** (treats as verified) if `checkEmailVerified()` itself throws, since the only realistic throw case is session-resume with no network — the offline-login cache (`HybridAccountRepository`/`CachedAccount`) has no verified-flag to check against, and blocking a previously-verified user's offline session over a check that can't run would break the existing offline-login feature; enforcement still applies whenever the app is actually online. `gradlew compileDebugKotlin` verified clean (only pre-existing unrelated deprecation warnings). **Not yet runtime-verified** — needs a real device pass: register a fresh account, back out of `VerifyEmailScreen` without clicking the emailed link, confirm logging back in re-routes to `VERIFY_EMAIL` instead of reaching Home.
- [ ] **Logo & launcher icon — corrected diagnosis 2026-09-09.** Earlier note above ("unmodified Android Studio default template") was imprecise — corrected after extracting and viewing the actual asset pixels. What's really there: the adaptive icon's **background** layer (`ic_launcher_background`) is already the real orchid photo (same image as `drawable/logo.png`, the clean in-app logo used on splash/login/landing/onboarding), but the **foreground** layer (`ic_launcher_foreground.xml`) was never replaced — it's still Android Studio's stock robot-mascot vector, sitting on top of the orchid on every density. This is not a personal/theme difference: confirmed identical (orchid background + robot silhouette on top) on `Sprint2`, `teammate/sprint-2-ui-navigation`, and `teammate/gikan-kang-rizza`, including their exported `ic_launcher-playstore.png` — so it's a repo-wide asset bug present in every branch checked, not something specific to this build's theme. This is also why it shows up **in notifications**, not just the home-screen icon — `AppMonitorService.kt`'s `setSmallIcon(R.mipmap.ic_launcher_round)` (6 call sites) loads the same broken flattened raster directly; notification icons aren't re-composited from the adaptive-icon XML at runtime, so the already-baked-in robot pixels show as-is. Fix: remove/replace the foreground vector's robot path (make it empty/transparent, since the background alone is already the correct logo image) and regenerate the per-density raster `ic_launcher.png`/`ic_launcher_round.png` fallbacks to match, or build the adaptive icon fresh from `drawable/logo.png` as originally planned (padded to the safe zone, solid brand-color background) — either approach removes the robot from both the launcher icon and every notification. The in-app logo's separate bordered-container treatment (4 bare `Image` call sites — `MainActivity`'s splash, `LandingScreen`, `AuthComponents.AuthHeader`, `OnboardingScreen`) is unaffected by this and still queued as its own piece.
- [ ] **App Lock screen icons + accessibility haptics** — `LockScreen.kt` (the real forced-lock screen shown by `AppMonitorService`) currently has **zero icons**, plain text on a flat dark-blue background — inconsistent with `ModeLockScreen.kt`/`BedtimeLockScreen.kt`, which already use a real `Icons.Filled.Lock` in a `CardCream` circle + `AppIconAvatar`; bring `LockScreen` up to that same pattern. Separately, confirmed **zero haptic/vibration feedback exists anywhere in the entire codebase** (grep for `Vibrator`/`VibrationEffect`/`HapticFeedback` returns nothing) and the `VIBRATE` manifest permission is missing — addresses the "what if the user is impaired" accessibility question directly: add a vibrate-on-locked / vibrate-on-unlocked pattern across `LockActivity`, `ModeLockActivity`, `BedtimeLockActivity`, `OveruseInterventionActivity`, and Deep Work's shake-exit gate.
- [x] **Routing audit — done 2026-09-09, no gaps found.** Checked `NavGraph.kt`: all 9 registered `Screen` routes have matching `composable` destinations, no dead routes, no unmatched `navigate()` calls. Settings' About/EditProfile/BedtimeMode/etc. use a deliberate local-boolean sub-screen pattern instead of `NavGraph` routes — confirmed intentional architecture, not a bug. Runtime click-through still needs a real device (already tracked in the Module 8 QA backlog above) — this was a structural/compile-time pass only.
- [ ] **Save-action feedback (reusable component)** — directly implements the save-button-animation rule from the first batch above. `EditProfileScreen.kt`'s Save button and its Change Password "Update Password" button both currently pop the screen silently on success with **zero loading or confirmation state** (confirmed in `SettingsScreen.kt`'s `onSave` handler — sets `showAccountEditor = false` immediately on success, inline red text on failure, nothing in between). Build one reusable `SaveActionButton` (idle → loading spinner → checkmark+"Saved" → settle to a lighter palette tone) and apply it to both.
- [ ] **Privacy Policy** — confirmed genuinely absent; `AboutScreen.kt` has no Privacy Policy section or link anywhere (only an inline "Is my data private?" FAQ answer). Add a new linked sub-screen (same local-state pattern as `showAboutScreen`), content fact-checked against real data handling the same way About's FAQ already was — what's actually collected (Firebase Auth/Firestore account+profile vs. local-only Room DB for usage/behavior data — these are distinct), optional Wi-Fi/GPS only when Context Alerts is enabled, no third-party sharing beyond Firebase's own infrastructure, and a pointer to the real Delete Account flow (`2eb9196`).
- [ ] **Loading indication — startup + Home/Reports skeletons** — `MainActivity.kt`'s `SessionCheckSplash()` currently shows only a static logo with no spinner/progress indicator at all. Neither `HomeViewModel` nor `ReportsViewModel` expose any loading flag today (confirmed via grep — pure Room `Flow`/`StateFlow`, no `isLoading`). Needs: a pulse animation on the startup logo (reusing the same `rememberInfiniteTransition` technique `DeepWorkScreen.kt` already uses, for consistency) plus a small progress indicator; and a new `initialLoadComplete` flag on each ViewModel driving a new reusable skeleton/shimmer component (`SkeletonBox` — no shimmer pattern exists anywhere in the app yet) shaped to match each screen's real card layout so nothing jumps when real content swaps in.
- [ ] **Reports charts — tap instead of hover.** Confirmed there's no actual hover logic anywhere in the app to "change" — `WeeklyUsageChart`/`DailyUsageChart` in `ReportsScreen.kt` are currently purely decorative `Canvas`/`Box` drawings with **zero interactivity of any kind**, not leftover web-hover code. Add tap-to-reveal: `detectTapGestures` hit-testing each plotted point on the line chart, `clickable` on each bar, both showing the exact value in a small anchored label/tooltip on tap.
- [ ] **Navigation transitions** — confirmed `NavGraph.kt`'s `NavHost` has no `enterTransition`/`exitTransition` at all (instant cut across all 9 destinations); app-wide, only 3 files use any Compose animation API today (`AboutScreen.kt`, `DeepWorkScreen.kt`, `PomodoroSection.kt`), everything else is static. Add a uniform slide+fade transition set once at the `NavHost` level, plus wrap Settings' internal local-boolean sub-screen swaps (About/EditProfile/BedtimeMode/etc.) in `AnimatedContent`/`Crossfade` so top-level and in-Settings navigation feel consistent rather than only half the app animating.
- [ ] **Accurate screen-time reading** — re-raised 2026-09-09, same underlying gap as the "Screen time reading might be wrong" known issue above (not a new/separate bug) — see that entry for the `UsageStatsManager`/`INTERVAL_DAILY` investigation angle already recorded.
- **Deferred, not on this list per explicit user decision**: converting some full pages into a modal/bottom-sheet for short selections — user will name the specific screens later.

**How to apply:** this whole batch came out of a Plan Mode session explicitly used for research + backlog-recording, not implementation — see [[feedback_backlog_vs_build]]. Don't start building any item here until the user names which one to build now. Each bullet already carries enough file-path/pattern detail (existing composables/utilities to reuse, exact lines checked) to execute directly once picked.
